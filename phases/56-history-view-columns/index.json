{
  "project": "기사 작성기",
  "phase": "56-history-view-columns",
  "scope": "docs/news.md 114~115행 신규 스펙(이력보기)을 구현한다 — 이력보기/송고이력보기 모달을 '수정시간/제목/수정자/상태/버전' 5열 기본값으로 바꾸고, 모달 안에서 우클릭 → '이력 목록설정'으로 다른 메타데이터 컬럼을 추가/제거(이력 종류별 localStorage 영속)할 수 있게 한다. 스펙의 '제목'·'버전'·'상태'는 ArticleHistory에 저장 컬럼이 없으므로 백엔드 서비스가 이력 로그에서 파생해 응답에 싣는다(스키마 변경 0 — DB 비파괴). 배부(ADR-008) 무관, 앱 내 타이머·egress 0.",
  "baseline": "브랜치 feat-56-history-view(feat-0-mvp 분기, HEAD 2022f55) 실측: npm test 777/777 pass, npm run test:web 89 files / 2218 tests pass, npm run lint clean, npm run build clean.",
  "order": "backend 패스(step0 순수 파생 모듈 → step1 model → step2 service → step3 라우트 계약) → web 패스(step4 순수 컬럼 카탈로그 → step5 fakeModel 계약 동기화 → step6 ListPage 결선). 파일 소유는 step 간 겹치지 않는다: step0=src/services/historyMeta.js, step1=src/models/articleHistoryModel.js, step2=src/services/articleService.js, step3=test/server.test.js(라우트 무변경 예상), step4=web/src/view/historyColumns.js, step5=web/src/test/fakeModel.js+web/src/model/contract.test.js, step6=web/src/view/ListPage.jsx(+테스트, CSS 무변경).",
  "decisions": "(1) 제목·버전·상태는 백엔드 파생 — sendOnly 필터가 서버에서 걸리므로 클라이언트는 승계 계산에 필요한 행(스냅샷 edit 행)을 볼 수 없다. 파생은 반드시 sendOnly 필터 '전에' 전체 이력 기준으로 수행한다. (2) 목록 응답에 markupVersion blob은 절대 싣지 않는다(기존 경량 계약 유지) — 제목만 200자 상한으로 파생해 싣는다. (3) 기존 articleHistoryModel.queryByArticle 계약(blob 미포함)은 손대지 않고, 스냅샷 조회용 메서드를 additive로 추가한다(tick 멱등 판정·엠바고 사이클 경로가 queryByArticle을 공유하므로 blob 로딩을 그 경로에 밀어넣지 않는다). (4) 버전은 '최초 저장 본문 = v1' 기준 1-base 정수, 스냅샷 행마다 증가. (5) 컬럼 설정은 이력 종류('history'|'sendHistory')별로 localStorage에 저장(목록 페이지 columnConfig 선례와 동형, 저장 키는 분리 — 'yh.historyColumnConfig'). (6) [리뷰 반영] 제목 v1 구간 예외 — 편집 스냅샷이 0건인 기사는 현재 Article.markupVersion이 곧 v1 본문이므로(그 뒤 본문이 바뀐 적 없다는 뜻), 그때에 한해 서비스가 현재 본문을 v1Body로 주입해 제목을 파생한다. 스냅샷이 1건 이상이면 현재 본문은 최신 버전이지 v1이 아니므로 무시한다(v1 구간은 '' → 뷰 '—'). create→send만 있는 흔한 경로에서 제목이 비는 문제를 닫는다. (7) [리뷰 반영] '다른 기사의 메타데이터' 해석 — 이력 이벤트 필드(종류·동작·전이·이전/이후상태·기사아이디·이력번호·본문스냅샷)를 '이력' 그룹으로, 우클릭한 기사 자체의 메타데이터 소수(작성자·부서·작성시간·송고시간)를 '기사' 그룹으로 카탈로그에 함께 넣는다(총 17컬럼, 기본 표시 5). 기사 그룹 값은 모든 이력 행에 동일하게 반복 표시되지만, ListPage.showHistory(article, …)가 이미 목록 행 객체를 들고 있어 추가 조회는 0건이다(N+1 없음). key 충돌(이력 createdAt=수정시간 vs 기사 createdAt=작성시간)을 피하려 기사 그룹 key에는 article 접두어를 붙인다. (8) [리뷰 반영] 이력 목록설정 모달은 이력 모달의 '형제'이면서 JSX 상 '뒤'에 배치한다 — .yh-modal__backdrop의 z-index가 60 단일 값이라 형제 중 DOM 뒤쪽이 위에 그려진다. CSS(z-index·새 클래스)는 도입하지 않는다. (9) [리뷰 반영] 커밋 위생 — 작업 트리에 사용자 소유 미커밋 파일(docs/news.md·.claude/skills/**·phases/49-mini-backlog-cleanup/step0.md·phases/50-hygiene-cleanup/**)이 이미 있으므로, 각 step의 diff scope AC는 'step 시작 전 git status --porcelain 스냅샷 대비 증분'으로 판정하고(절대 목록 비교 금지), git add -A/git add . 및 미커밋 사용자 파일의 restore/checkout/stash/clean을 전 step에서 금지한다. docs/news.md는 이 phase에서 커밋하지 않는다. (10) 알려진 잔여(별도 phase) — fakeModel의 sendOnly 필터 조건(action==='send')이 서버(eventType==='status' && action==='send')와 미세하게 다르다. 이번 phase에서는 건드리지 않는다.",
  "steps": [
    { "step": 0, "name": "history-derive", "status": "completed", "summary": "src/services/historyMeta.js 신설(순수 파생 코어) + test/historyMeta.test.js 22케이스. (a) 시그니처: snapshotTitle(markupVersion)→string, decorateHistoryRows(rows, snapshots, { v1Body } = {})→새 배열(입력 순서 유지, 행 얕은 복사 + {title,version,status}), MAX_HISTORY_TITLE_LEN=200. (b) version은 1-base 정수(최초 저장 본문=v1), 증가 판정은 hasSnapshot 플래그만(스냅샷 blob 유무 무관), 스냅샷 행 자신은 증가 후 값. (c) title은 직전(더 오래된) 스냅샷에서 승계·없으면 ''; status는 전이 행 toStatus 승계, 첫 전이 이전 구간은 그 전이의 fromStatus 역승계, 전이 없으면 ''(빈 값은 항상 '' — null 금지, 표시 폴백은 뷰 책임). (d) 제목은 첫 텍스트 블록 줄 trim 후 200자 절단(bodyTitle 동형 — 텍스트 블록만, 깨진 JSON은 평문 취급). (e) v1Body는 스냅샷 0건일 때만 v1 구간 제목 파생에 사용(스냅샷 1건 이상이면 무시). 반환 행에 markupVersion 없음. 변이 검증 4종 전부 red 확인." },
    { "step": 1, "name": "history-model", "status": "completed", "summary": "src/models/articleHistoryModel.js에 querySnapshotsByArticle(articleId) additive 추가 — SELECT id, markupVersion FROM ArticleHistory WHERE articleId = ? AND markupVersion IS NOT NULL AND markupVersion != '' ORDER BY id DESC(파라미터 바인딩만). 반환 shape [{ id, markupVersion }], 필터는 hasSnapshot 판정과 동형(빈 문자열 제외), 정렬 id DESC 결정적. 기존 insert/queryByArticle/querySnapshotById 무변경(경량 계약·삭제 함수 부재 회귀 green). test/articleHistoryModel.test.js 5케이스 추가, 변이 검증 2종 red 확인. npm test 800/800." },
    { "step": 2, "name": "history-service", "status": "completed", "summary": "articleService.queryHistory 결선 — (a) 반환 행 shape: 기존 9필드(id, articleId, eventType, action, fromStatus, toStatus, actorUserId, createdAt, hasSnapshot) + 파생 {title, version, status}, markupVersion 미포함. (b) 파생(decorateHistoryRows)은 sendOnly 필터 '전에' 전체 이력 기준으로 계산 후 필터(조건 eventType==='status'&&action==='send' 유지). (c) 스냅샷 조회는 typeof historyModel.querySnapshotsByArticle==='function' 가드 + try/catch 격리(실패 시 빈 스냅샷 — 제목만 비고 버전·상태 정확). (d) v1Body는 rows.length>0 && snapshots.length===0일 때만 articleModel.getById로 조회(try/catch 격리) — 그 외 조회 0회. 테스트 11케이스 추가(스파이 카운터는 queryHistory 직전 리셋), 변이 검증 5종 red 확인(단, 변이4는 케이스 7-2 대신 7-3이 red — 7-2는 step0 모듈의 anySnapshot 가드가 이중으로 막고 있고 그 층은 step0 변이4/케이스21이 잠근다). npm test 811/811." },
    { "step": 3, "name": "route-contract", "status": "completed", "summary": "(a) server/index.js 무변경 — 라우트는 controllers.article.queryHistory 위임만 하므로 step2의 넓어진 반환 shape이 그대로 통과했다(신규 테스트 즉시 green). (b) HTTP 경계에서 잠근 계약: 각 item에 title(string)/version(number)/status(string) 존재, 최신 편집 제목·버전(3) 정확, item에 markupVersion 키 부재 + 응답 문자열 전체에 편집 본문 표식('대외비본문표식-편집') 미등장, v1 경로(생성 즉시 송고)의 version 1·title 파생 + 생성 본문 둘째 줄 표식('대외비본문표식-생성') 미등장(v1Body blob 누출 스캔 — 게이트 지시 반영), sendOnly=1 item이 전체 조회 같은 id item과 deepEqual, 미인증 401 + items 부재, 이력 없음 빈 배열·단건 스냅샷 라우트는 기존 케이스 실행으로 회귀 확인. test/server.test.js 3케이스 추가, 변이 검증 2종(파생 제거→케이스 1·1-1·3 red / blob 주입→blob 스캔+기존 경량 목록 케이스 red) 확인. npm test 814/814, response-secrets 무수정 green." },
    { "step": 4, "name": "history-columns", "status": "completed", "summary": "web/src/view/historyColumns.js 신설(순수 뷰 모듈 — React/DOM/transport 비의존) + historyColumns.test.js 23케이스. (a) HISTORY_COLUMNS 17컬럼(frozen, 원소도 frozen): history 그룹 13 = createdAt수정시간·title제목·actorUserId수정자·status상태·version버전·eventType종류·action동작·transition전이·fromStatus이전상태·toStatus이후상태·articleId기사아이디·id이력번호·hasSnapshot본문스냅샷, article 그룹 4 = articleAuthor작성자·articleDepartment부서·articleCreatedAt작성시간·articleSentAt송고시간. 기본 표시 5 = createdAt·title·actorUserId·status·version(스펙 순서). (b) 저장 키 'yh.historyColumnConfig'(목록 페이지 yh.columnConfig와 분리), 스코프는 이력 종류('history'|'sendHistory')별, 저장 shape { [kind]: { visible } }, 로드 시 { ...base.visible, ...saved.visible } 병합(새 컬럼은 기본값). gap 없음. (c) historyCellText(key, row, article) — 이력 그룹은 row만·기사 그룹은 article만 읽는다. 특수 표기: version=`v{n}`·hasSnapshot=Y/N·transition=`from→to`(한쪽만 있으면 그 값만, 화살표 없음)·시간류는 listFormat.formatDateTime 재사용·빈 값/알 수 없는 key=HISTORY_EMPTY('—'). (d) 기사 그룹 key는 article 접두어로 이력 행 createdAt(수정시간)과 기사 createdAt(작성시간)의 충돌을 피한다. 변이 검증 4종 red 확인. 참고: 브랜치 web 실측 기준선은 89 files/2213(계획 기재 2218은 이 트리에서 재현 불가) → step4 후 90 files/2236(+23). backend 814/814·lint·build clean." },
    { "step": 5, "name": "fake-history-contract", "status": "completed", "summary": "fakeModel.queryHistory를 서버 /history 계약과 동기화 — 목록 반환 시 rest 구조분해로 markupVersion(blob)만 제외하고(원본 시드 무변형 — getHistorySnapshot이 같은 배열에서 본문을 읽는다), title/version/status는 시드 값 그대로 통과(계산·기본값 주입 없음 — 파생 규칙의 진실은 백엔드 historyMeta). sendOnly 필터 조건(action==='send')은 알려진 잔여 드리프트로 무변경. contract.test.js에 3케이스 추가(blob 미노출 전체+sendOnly / 파생 필드 pass-through+기존 9필드 보존 deepEqual / 시드 blob 잔존+스냅샷 정상 반환), 기존 케이스 무수정 green(ListPage 이력 3건·WriterPage 기사이력비교 포함). 변이 검증 2종 red 확인(blob 제외 원복→케이스1 red / delete 원본 변형→케이스3 red). AC: test:web 90 files/2239(+3), npm test 814/814, lint·build clean." },
    { "step": 6, "name": "history-modal", "status": "completed", "summary": "ListPage.jsx 이력/송고이력 모달을 historyColumns 카탈로그 기반으로 결선(CSS 무변경·View 계층만). (a) 모달 state { title, kind, article, items } — showHistory(article, title, kind)가 kind('history'|'sendHistory')에서 sendOnly를 도출하고, 컬럼 설정은 모달을 '열 때' loadHistoryColumnConfig(kind)로 세팅(histColConfig state). aria-label('이력보기'/'송고이력보기')은 유지. (b) 우클릭 진입점은 이력 모달 콘텐츠 컨테이너(.yh-modal) 전체 — 이력 0건/컬럼 0개로 표가 없어도 목록설정 진입 가능(막다른 골목 없음). (c) '이력 목록설정' 모달(aria-label 분리)은 이력 모달의 형제로 JSX상 '뒤'에 렌더 — 백드롭 z-index 60 동률에서 DOM 뒤쪽이 위에 그려진다(DOM 순서로만 해결, z-index 신설 없음). 백드롭/닫기는 설정만 닫고, 이력 모달을 닫는 모든 경로(closeHistoryModal)가 설정 열림 state도 리셋(고아 모달 금지). 토글은 toggleHistoryColumn→state→saveHistoryColumnConfig(kind) 즉시 저장. (d) 셀은 historyCellText(col.key, row, historyModal.article) — 기사 그룹 값은 우클릭한 목록 행 객체에서 오고 추가 조회 0건(케이스 15가 queryArticles/getArticle spy 미호출로 잠금). 상태는 배지가 아닌 텍스트, formatDateTime 직접 호출·listFormat import 제거. 컬럼 0개면 '표시할 컬럼이 없습니다 — 우클릭 목록설정에서 선택하세요.' 안내. ListPage.test.jsx 16케이스 추가(전부 within(dialog) 스코프·설정 체크박스도 within(설정 dialog), 모달 오픈 후 bodyRows 미사용), 기존 이력 3건(L333~384) 무수정 green. 변이 검증 5종 red 확인(기본 title 제거→1·2 포함 10 red / 우클릭 table 이동→5·13 포함 12 red / 설정 자식화→11만 red / 설정 앞배치→16·11 red / 저장 제거→8만 red). AC 연속 2회 동일: test:web 90 files/2255(+16), npm test 814/814, lint·build clean — flake 없음." }
  ]
}
