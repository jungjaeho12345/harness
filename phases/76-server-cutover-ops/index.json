{
  "project": "기사 작성기",
  "phase": "76-server-cutover-ops",
  "scope": "포팅 로드맵(`docs/porting-plan-cpp-spring.md` §7) **P3 '서버 전환 운영'** 중 **이 리포 안에서 구현·검증 가능한 부분**을 소유한다. 산출물 다섯이다: ① **실운영 시나리오 하네스**(`scripts/operation-scenario.mjs`) — 동결된 REST 계약으로 **작성→송고→배부→수집**을 한 대상 서버(Node 또는 Spring)에 대해 자동 구동한다. ② **배부 스풀 바이트 대조**(Node(SQLite)와 Spring이 **동일 seed 행**에서 출발해 `POST /api/distribution/tick`만으로 쓴 스풀 파일을 **정규화 후 바이트 대조** · diff 0). 서버 벽시계로 tick 시점에 찍히는 `distributedAt` 값과 파일명 타임스탬프만 정규화하고 나머지 원문(키 순서·이스케이프·숫자 표기·값)은 그대로 비교한다 — 그것이 계약 패리티(HTTP 응답)가 못 보던 **파일 레벨** 축이다. 작성→송고는 `createdAt`·`sentAt`·`articleId`가 서버 파생이라 라이브 대조가 불가능하므로 seed로 고정하고, 그 라이브 통과 증명은 시나리오 하네스의 단일 대상 모드(①)가 소유한다. ③ **클라이언트 서버 타겟 스위치**(Electron 셸 배포 기본 서버 URL `NEWS_SERVER_URL` — 저장된 serverUrl이 없을 때만 기존 정규화·health 프로브·fail-safe 경로를 통해 주입 · 병행 운영 창에서 패키지 클라이언트를 Spring으로 미리 가리키는 편의). ④ **Node 은퇴 예고 경로**(opt-in `NODE_SERVER_DEPRECATED` 기동 배너 — **응답·계약 무변경** · sqlite 분기 **제거 금지**(롤백 레버) · 실제 은퇴는 런북). ⑤ **컷오버 런북 정본화**(`docs/ops-mysql.md`에 P3 컷오버 실행 체크리스트 — `GRANT DELETE ON news.ReceiverConfig` 선결 조건 · 정지 창 순서 · 운영 cron tick 전환 · 클라이언트 재지정 · Node 은퇴 체크리스트 · 롤백 포인터). **운영 데이터의 실제 컷오버 실행은 이 phase가 하지 않는다** — 정지 창·백업·사람의 승인·root가 붙는 사건이며 로드맵상 P3의 사람 몫이다. 이 phase는 그 사건을 **자동으로 검증하는 도구**와 **사람이 따르는 절차**까지만 소유한다. **ADR-008 불변**: 시나리오 하네스는 아키텍처 다이어그램의 「외부 cron / 운영 tick pull」 역할을 하는 **외부 트리거**다 — 앱 안에 타이머·egress·재시도·큐를 만들지 않고 `POST /api/distribution/tick`(body 무시)을 HTTP로 호출할 뿐이다. **DB 비파괴**: 시나리오·대조는 임시 `DATA_DIR`와 ephemeral `harness_ct_<16hex>`에만 바인딩하고 리포 `news.db`·`uploads/`·운영 `news`·`news_stage`에 **절대 쓰지 않는다**(실행 전후 `news.db` md5 무변 단언).",
  "environment": "**두 실행 환경을 구분한다.** (A) **Node/SQLite 축**(step0·1·4·5의 AC)은 순수 Node 도구라 **컨테이너에서 그대로 실행**된다(`node`·`npm`·`node:sqlite` v22). 절대경로·MySQL·JDK 불요. (B) **Spring/MySQL 축**(step2의 `--dual`, step3 full verify, step7 마감)은 phase 75가 확립한 **개발 머신 실행 환경**을 그대로 쓴다: 포터블 Temurin **JDK 25.0.4.1+1 = `D:/agents/tools/jdk-25.0.4.1+1`**(유일 기준선 JDK — `server-spring/pom.xml`의 `java.version`은 25다 · `jdk-21`을 쓰지 마라) · Maven은 `server-spring/mvnw`·`tools/news-migrator/mvnw` 래퍼(시스템 java가 낮아 `JAVA_HOME` 필수) · MySQL **8.0.46** 로컬 인스턴스 · 자격은 리포 밖 `D:/agents/secrets/news-mysql.env`(9키)이고 `docs/ops-mysql.md` §3의 **한 줄씩 읽는 형태**로 싣는다(`set -a; . 파일`은 URL의 `&` 때문에 깨진다). `server-spring`의 `mvnw verify`는 decisions(14)에 따라 **MySQL이 없으면 skip이 아니라 fail**한다 — 그래서 코드만 빠르게 확인하려면 `-Dtest=<클래스>`로 대상 테스트만 골라 MySQL 요구를 피한다(그 형태는 `MysqlConfiguredGuardTest`를 포함하지 않는다). `--db mysql` 계열은 패스마다 ephemeral `harness_ct_<16hex>`를 쓰므로 `NEWS_CT_MYSQL_*`만 싣고 `NEWS_DB_*`는 싣지 않는다(env 통째 로드는 kind/URL 모순 거부를 부른다).",
  "order": "step0 spool-canon(순수 정규화 lib + 단위 테스트) → step1 scenario-driver(단일 대상 작성→송고→배부→수집 구동 + 스풀 매니페스트 방출 · Node 자체 기동 AC) → step2 scenario-dual-compare(Node vs Spring 동일 시드 배부 스풀 **바이트 대조 diff 0** · ephemeral MySQL 선택 모드) → step3 schemaguard-mysql-message(mysql 모드 거부 문구를 `migrate` 처방으로 정정 · 계약 무영향) → step4 client-server-target(Electron 셸 배포 기본 서버 URL) → step5 node-deprecation-marker(Node opt-in 은퇴 배너 · 응답 무변경 · sqlite 분기 유지) → step6 cutover-runbook-final(컷오버 실행 체크리스트·GRANT·cron·클라 재지정·Node 은퇴·롤백 문서) → step7 closeout(마감 실측 · forward_notes · 등록). **근거**: (a) **아래에서 위로**: 정규화 단위 → 단일 구동 → 이중 대조 → 서버 문구 → 클라 스위치 → Node 예고 → 절차 → 마감. (b) **컨테이너 실행 가능성으로 격리**: step0·1·4·5는 Node만 필요하므로 어디서든 검증되고, Spring/MySQL 의존은 step2·3·7에만 모여 실패 원인이 섞이지 않는다. (c) **계약·응답 무회귀가 매 step의 실질 게이트**: 이 phase는 새 라우트·새 계약 관측을 열지 않는다 — Node `server/**`·`src/**`와 `contract/**`는 동결 정본이고 step3·5의 유일한 코드 변경은 **관측되는 응답을 바꾸지 않는다**(step3=예외 메시지 문자열 · step5=기동 로그). 따라서 `npm test` 1328과 `--parity` 313관측 diffs 0이 무회귀 판정으로 상속된다. (d) **step6(문서)이 step1·2·5 뒤인 이유**: 런북이 참조하는 검증 도구(시나리오 하네스)와 Node 예고 플래그가 먼저 존재해야 절차가 실물을 가리킬 수 있다. (e) **step7이 마지막인 이유**: 컷오버 미실행·GRANT 미부착 같은 정직한 공백을 forward_notes로 넘기려면 그 앞 산출물이 전부 확정돼 있어야 한다.",
  "decisions": [
    "(1) **이 phase는 운영 데이터를 옮기지 않는다.** `docs/ops-mysql.md` §11 런북과 §12 결과표가 이미 컷오버 명령을 스테이징에서 실측했다. step6은 그 위에 **P3 실행 체크리스트**(정지 창·순서·GRANT·cron·클라 재지정·Node 은퇴)를 얹어 정본화할 뿐이고, `migrate --target news` 같은 운영 대상 명령은 **미실행·사람 몫**으로 남긴다. AC로 만들 수 있는 것은 절차 문서의 존재·링크·필수 토큰뿐이다.",
    "(2) **[GRANT 선결 조건 — 반드시 반영] `GRANT DELETE ON news.ReceiverConfig`는 아직 운영 `news`에 붙지 않았고 root가 필요하며 계약 하네스는 그 부재를 볼 수 없다**(하네스는 `news_ct`=ALL로 돈다). 없으면 `DELETE /api/receiver-config/:id`가 계약이 동결한 `200 {ok:true,changes:1}` 대신 **500 `internal-error`**다(Node는 200). 이 phase는 그 grant를 **자동으로 붙이지 않는다**(root·운영 DB). 처리 방식: ① step6 런북에 **컷오버 go/no-go 필수 항목**으로 박고 `SHOW GRANTS` 육안 판정 절차를 명시한다(패리티 green이 이 부재를 덮으므로 자동 게이트로 대체 불가) ② 이미 있는 자동 방어선(`NewsAppMysqlWireTest`·`MinimumPrivilegeBoundaryTest` — 스테이징 자격에서 실제 거부/허용을 실증)을 런북이 인용한다 ③ step2 `--db mysql`은 ephemeral `harness_ct_*`(ALL 권한)에서 돌아 이 축을 **구조적으로 못 본다** — 그 한계를 step2 문서와 forward_notes에 정직히 적는다.",
    "(3) **배부 스풀 대조는 서버 벽시계 필드 하나만 정규화한다 — 그리고 그 전제는 seed로 세운다.** 스풀 payload(`src/services/spoolWriter.js` CONTENTS_FIELDS)는 `createdAt`(작성 시 `articleService.js` `nowISO()` · L150)·`sentAt`(송고 시 `nowISO()` · L227)·`embargoAt`·`secondEmbargoAt`를 싣고, `articleId`는 서버가 난수로 생성한다. 라이브 작성→송고를 두 서버에서 각각 돌리면 `createdAt`·`sentAt`·`articleId`가 **구조적으로 갈려** diff 0이 원천 불가능하다(초기 계획의 오류 — 검토 반영). **실측한 결정성 경계**: `embargoAt`·`secondEmbargoAt`는 **요청 본문이 주는 절대 문자열**이고 서버가 파생하지 않는다(`articleService.create`가 `pick(dto, CONTENTS_FIELDS)`로 그대로 저장 · L153) → 결정적. `createdAt`·`sentAt`·`distributedAt`만 서버 `nowISO()`다. **해법(step2)**: 두 서버가 **동일 seed 행**(고정 `articleId`·`createdAt`·`sentAt`·`embargo` 절대값·본문 + 활성 수신처)에서 출발하게 하고 **`POST /api/distribution/tick`만** 호출한다 — 그러면 tick 시점에 찍히는 **`distributedAt` 값과 파일명의 그 타임스탬프, 그 둘만** 볼러타일이라 그 둘만 정규화한다. 나머지 원문(키 순서·JSON 이스케이프·숫자 표기·공백·값)은 **그대로** 비교하고 키를 정렬하지 마라 — 정렬·추가 정규화는 두 서버의 직렬화·MySQL 왕복 divergence(계약 패리티가 못 보는 파일 레벨 축)를 조용히 삼킨다. seed는 `src/db/schema.js`로 만든 임시 DB에 **시나리오 전용 헬퍼**로 넣고 공유 `src/db/seed.js`의 `seedUsers`는 바꾸지 않는다(313관측 baseline 보호). **정직한 경계**: 이 대조는 작성→송고를 우회하므로 그 라이브 통과는 **step1**(단일 대상 전체 루프)이, create/send 응답 패리티는 **계약 스위트**(313관측)가 각각 소유한다 — step2는 **스풀 writer 직렬화 파리티**만 격리 판정한다.",
    "(4) **시나리오 하네스는 ADR-008을 어기지 않는다.** 배부는 `POST /api/distribution/tick`(body 무시) **외부 트리거 1회 호출**로만 일으킨다 — 앱 안에 `@Scheduled`·타이머·재시도·백오프·큐·네트워크 egress를 추가하지 않는다. 하네스 스크립트 자체는 앱 코드가 아니라 아키텍처 다이어그램의 「외부 cron / 운영 tick pull」 역할이다. `Adr008DisciplineTest`·`RoutePolicy`·`ReasonStatus`·`contract-run.mjs`는 어느 step도 고치지 않는다.",
    "(5) **Node `server/**`·`src/**`는 롤백 레버다 — 최소·비행동 변경만 허용한다.** step5의 Node 은퇴 배너는 **opt-in(`NODE_SERVER_DEPRECATED=1`, 기본 off) 기동 로그 한 줄**이고 **어떤 HTTP 응답·헤더·상태도 바꾸지 않는다**. sqlite 분기·`node:sqlite`·`TempNewsDb` 계열을 **제거하지 않는다**(제거는 롤백 능력과 313관측 무회귀 판정을 함께 잃는다 · P3 후속 판단). 계약 정본(`contract/**`·`docs/api-contract/**`·`scripts/contract-run.mjs`·`scripts/contract-diff.mjs`)·`package.json`·`pom.xml`은 **무접촉**(새 의존성 0).",
    "(6) **클라이언트 스위치는 편의이지 신뢰 경계 변경이 아니다.** `NEWS_SERVER_URL`은 **저장된 serverUrl이 없을 때만** 적용되고 기존 `normalizeServerUrl`(자격·미허용 스킴 거부)·health 프로브·fail-safe(하향 미승격)를 **그대로 경유**한다. 원격 페이지에 Node 권한을 주지 않는다(ADR-011 승계). 실제 재지정의 정본 경로(설정 화면·배포 config·동일 포트 교체)는 step6 런북이 소유한다.",
    "(7) **컨테이너 검증 가능성을 스코프 격리 기준으로 쓴다.** step0·1·4·5는 Node/SQLite만 필요하므로 AC가 컨테이너에서 그대로 돈다. step2 `--dual`·step3 full verify·step7 마감은 phase 75가 확립한 개발 머신 환경(포터블 JDK 25 · 로컬 MySQL 8.0.46 · 리포 밖 env)을 쓴다 — 그 AC는 phase 75 README·`docs/ops-mysql.md` §3의 검증된 형태를 인용한다. 오케스트레이터가 다른 환경에서 실행하면 그 축은 blocked로 기록하고 사유를 남긴다."
  ],
  "excluded": [
    "(a) **운영 `news` DB로의 실제 데이터 컷오버 실행** — 정지 창·백업·root·사람의 승인이 붙는 별개 사건(P3 사람 몫). 절차는 step6이 정본화하지만 실행은 하지 않는다.",
    "(b) **`GRANT DELETE ON news.ReceiverConfig` 부착** — root·운영 DB. step6 런북 go/no-go 항목으로만 다룬다(decisions (2)).",
    "(c) **sqlite 분기·`node:sqlite`·`sqlite-jdbc` 제거, Node 서버 실제 종료** — 롤백 레버라 유지한다(decisions (5)). 제거 판단은 P3 후속.",
    "(d) **helmet 등가 보안 헤더 11종·HTTPS 강제** — ADR-013이 남긴 공백이고 P3 소유지만 이 phase의 컷오버 검증 범위 밖이다. forward_notes로 넘긴다.",
    "(e) **커넥션 풀 확대·동시성 튜닝** — 별도 ADR과 `LogsStreamWireTest` 항목 22 재설계가 필요하다(phase 75 excluded (c)·decisions (10)).",
    "(f) **부트 백필 2종 이식**(`backfillEmptyDepartments`·`backfillHistoryTitles`) 및 운영 cron **구현** — cron 전환은 URL 재지정 절차(문서)이고 백필 이식 판단은 P3 후속이다.",
    "(g) **`User.userId` 769자 축 해소**(phase 75 forward_notes (3)④) — Node 응답까지 바뀌므로 계약 동결 해제와 함께 결정해야 한다. 이 phase는 손대지 않는다.",
    "(h) **C++ 클라이언트·Qt 관련 일체**(P4~P8) — 스코프 밖."
  ],
  "steps": [
    { "step": 0, "name": "spool-canon", "status": "completed", "summary": "scripts/lib/spoolCanon.mjs (순수 정규화 lib) + scripts/lib/spoolCanon.test.mjs (node --test, 9케이스). STAMP='<STAMP>'. canonicalizeSpoolName: 파일명 끝 `_<YYYYMMDDTHHMMSSsssZ>.json` 스탬프만 STAMP로 치환(패턴 불일치 시 원문 유지). canonicalizeSpoolContent: 정규식으로 distributedAt '값'만 치환, JSON.parse 재직렬화 없이 키순서·이스케이프·공백 보존. readSpoolManifest: 주입 fs(readdir/readFile)로 재귀 평탄화, key='<수신처slug>/<articleId>'(마지막 '_' 앞) 오름차순. diffManifests: only-in-a/only-in-b/name/content diff; content diff는 원문 대신 length+sha256 지문만(위생). AC: node --test 9 pass, npm run lint 통과, npm test 1328(1327 pass·1 skip·0 fail) 무회귀." },
    { "step": 1, "name": "scenario-driver", "status": "completed", "summary": "scripts/operation-scenario.mjs — 동결 REST 계약으로 작성→송고→배부→수집 전체 루프를 단일 Node 서버(임시 DATA_DIR createSchema+seedUsers · 임시 DIST_SPOOL_DIR · 랜덤 COLLECTION_TOKEN · HOST=127.0.0.1 · 랜덤 loopback PORT)에 자체 기동으로 구동하고 step0 readSpoolManifest로 스풀 매니페스트를 --out/stdout 방출. 시퀀스는 contract/cases/default 정본 미러링: D 로그인→POST /api/distribution-targets(Z,활성 press)→POST /api/articles(D,embargoAt 1h前·yh-editor 블록+'(끝)')→action:send(→DES)→POST /api/distribution/tick(Z 외부 트리거 1회·body 무시, ADR-008 앱 무변경)→POST /api/receiver-config(Z)→POST /api/collection/receive(토큰·loopback→200)→GET /api/articles 확인. 각 단계 status·스풀 매니페스트 비어있지 않음(활성 수신처 slug 항목)을 단언. 데이터 안전: 리포 news.db/uploads 무변+부재(생성 0) 스냅샷 단언 · 세션·토큰·비밀번호는 로그/매니페스트 미포함. 인자 가드는 cliArgs.flagValue 재사용(--server node만 지원, spring/dual 자리만 남김). AC(컨테이너 실측): node scripts/operation-scenario.mjs --server node exit 0(9단계 전부 ok·spoolEntries=1) · npm run lint 통과 · npm test 1328(1327 pass·1 skip·0 fail) 무회귀 · 실행 후 news.db·uploads 여전히 부재." },
    { "step": 2, "name": "scenario-dual-compare", "status": "blocked", "blocked_reason": "축 B(개발 머신) 실행 환경이 이 컨테이너에 없어 load-bearing AC를 실행·검증할 수 없다. (1) jar 준비 AC `cd server-spring && ./mvnw -B -q package -DskipTests`(+ `tools/news-migrator`)는 pom `java.version=25`를 요구하는데 이 컨테이너는 JDK 21.0.10만 있어 컴파일 불가. (2) sqlite 이중 대조 `node scripts/operation-scenario.mjs --dual --db sqlite`는 Spring jar 기동(JDK 25)이 선결 조건이라 (1)에 막힘. (3) `--dual --db mysql`은 로컬 MySQL 8.0.46 인스턴스 + 리포 밖 `news-mysql.env`(NEWS_CT_MYSQL_*)를 요구하는데 이 컨테이너는 MySQL 서버/클라이언트 부재(`which mysql/mysqld`=없음)·secrets 부재(`news-mysql.env` 없음)다. 따라서 `--dual`/`--server spring`/`--db` 코드 자체를 여기서 blind로 작성하지 않고 개발 머신으로 이연한다 — 이 코드는 실제 Spring 서버 대상으로 diff 0을 실측해야만 검증되므로(스풀 writer 직렬화 파리티는 라이브 대조로만 참/거짓이 갈린다), 컨테이너에서 코드만 써두면 unverifiable green이 되어 phase의 유일한 파일 레벨 대조 게이트가 공허해진다(decisions (3)(7) · step2 '실행 환경 주의'). 개발 머신이 해야 할 일: JDK 25로 jar 2개 빌드 → `--dual` 코드 작성(TDD) → `--db sqlite` diff 0 → MySQL+news-mysql.env 로드 후 `--db mysql` diff 0·잔존 harness_ct_* 0. 정직한 공백: `--db mysql`은 news_ct=ALL ephemeral DB에서 돌아 `GRANT DELETE ON news.ReceiverConfig` 부재를 구조적으로 못 본다(decisions (2))." },
    { "step": 3, "name": "schemaguard-mysql-message", "status": "blocked", "blocked_reason": "server-spring의 Java 변경(SchemaGuard.verify() 처방 문장을 this.mysql로 dialect 분기)이라 AC를 이 컨테이너에서 컴파일·실행할 수 없다. 대상 테스트 AC `cd server-spring && ./mvnw -B -Dtest=SchemaGuardTest,MysqlSchemaGuardTest test`조차 pom `java.version=25`를 요구하는데 이 컨테이너는 JDK 21.0.10만 있어(MySQL-free 대상 테스트도) 컴파일이 안 된다. 전체 게이트 `./mvnw -B clean verify`는 그 위에 로컬 MySQL 8.0.46 + NEWS_APP_MYSQL_*/NEWS_CT_MYSQL_* 자격(MysqlConfiguredGuardTest — decisions(14): MySQL 없으면 skip이 아니라 fail)까지 요구하는데 이 컨테이너는 MySQL·secrets 부재다. TDD로 실측해야 하는 변경(테스트 먼저 → 문구 분기 구현)이므로 JDK 25 실행 환경에서 써야 검증된다 — blind 작성 금지(step3 '실행 환경 주의'). 개발 머신이 해야 할 일: JDK 25로 (a) MysqlSchemaGuardTest에 mysql 대상 처방('migrate'/tools/news-migrator 가리킴 · sqlite 문구 미포함) 케이스 추가 + SchemaGuardTest sqlite 처방 유지 재단언 → (b) SchemaGuard.java 126행 처방을 this.mysql로 분기(예외 타입·문제 목록·앞부분·this.target 표기 불변) → (c) -Dtest 대상 테스트 통과 → (d) full clean verify 0 fail/0 skip. 계약 무영향(부팅 예외 텍스트일 뿐 · HTTP 응답 미변경 · 313관측·npm test 무회귀)." },
    { "step": 4, "name": "client-server-target", "status": "completed", "summary": "client/lib/clientConfig.js에 순수 리졸버 resolveBootServerUrl({savedServerUrl, envServerUrl}) 추가 — 저장값 우선(있으면 env 미참조), 없을 때만 배포 env 기본값을 기존 normalizeServerUrl 경유로 적용(자격·미허용 스킴·경로/쿼리는 거부·버림 → null|origin). client/main.js 결선: bootConfig 직후 bootServerUrl = resolveBootServerUrl({savedServerUrl: bootConfig.serverUrl, envServerUrl: process.env.NEWS_SERVER_URL})를 1회 계산해 secure-origin 스위치 판정과 whenReady 부팅 분기 양쪽에서 재사용 — 저장값 있으면 무동작(기존 경로 그대로), 없고 env 유효하면 앱 창(createAppWindow의 loadURL·did-fail-load fail-safe 기존 경로), 아니면 setup. 새 네트워크 호출·재시도·타이머 0(ADR-008/011). env는 config.json에 자동 저장 안 함(매 부팅 재계산). test/client-server-target.test.js 신설(node --test, 8케이스). AC(컨테이너 실측): node --test 8 pass · npm test 1336(1335 pass·1 skip·0 fail, +8 무회귀) · npm run lint 통과." },
    { "step": 5, "name": "node-deprecation-marker", "status": "completed", "summary": "server/deprecationBanner.js — 순수 헬퍼 deprecationBanner(env): NODE_SERVER_DEPRECATED가 정확히 '1'일 때만 은퇴 예고 배너 문자열(정본=Spring/MySQL·롤백은 docs/ops-mysql.md 안내) 반환, 그 외/미설정/undefined는 null(기본 침묵·opt-in). 배너에 세션·토큰·비밀번호·실데이터·절대경로 없음(위생). server/index.js 결선: app.listen 콜백에서 기존 logService.info('API server on …') 유지한 채 const banner=deprecationBanner(process.env); if(banner) logService.warn(banner) 한 줄 추가 — HTTP 응답·헤더·상태·라우트·미들웨어·sqlite 분기·node:sqlite·백필·ADR-012 잠금·createApp 무접촉(롤백 레버 유지 · ADR-016 · decisions (5)). 타이머/스케줄러 추가 없음(ADR-008). test/node-deprecation-banner.test.js 신설(node --test, 7케이스). AC(컨테이너 실측): node --test 7 pass · npm test 1343(1342 pass·1 skip·0 fail, +7 무회귀) · npm run lint 통과 · 실기 스모크: NODE_SERVER_DEPRECATED=1 기동과 미설정 기동 모두 GET /api/health=200 {\"ok\":true} 동일(계약 불변). logService는 링 버퍼 전용(ADR-007)이라 두 모드 모두 stdout에 기동 라인 미출력 — 배너는 warn/SSE 경로로 흐른다." },
    { "step": 6, "name": "cutover-runbook-final", "status": "completed", "summary": "docs/ops-mysql.md에 §13 'P3 컷오버 실행 체크리스트'를 추가(별도 문서 신설 없이 정본 확장 · §11 명령/§12 결과표를 링크로 재사용하고 명령 본문은 중복하지 않음). 13-0 go/no-go 게이트 표(G-1 GRANT·G-2 시나리오 하네스·G-3 대상 비어있음·G-4 백업·G-5 판본), 13-1 GRANT 게이트(GRANT DELETE ON news.ReceiverConfig는 계약 필수 조건·root·미실행 P3 몫 · SHOW GRANTS 육안 판정만 유효 · 계약 하네스와 --dual --db mysql은 news_ct=ALL로 돌아 못 봄 · 부재 증상=수집 설정 삭제 500 while parity green · NewsAppMysqlWireTest·MinimumPrivilegeBoundaryTest 인용), 13-2 시나리오 하네스 검증 게이트(node scripts/operation-scenario.mjs --dual --db mysql ephemeral 스풀 diff 0 · ADR-008 외부 tick pull), 13-3 정지 창 순서(§11 링크), 13-4 클라 재지정 3경로(동일 host:port 교체·설정/NEWS_SERVER_URL·브라우저 SPA + 각 되돌림), 13-5 cron(tick) 전환(ADR-008 외부 pull·양쪽 동시 tick 금지 중복 배부 방지), 13-6 Node 은퇴(NODE_SERVER_DEPRECATED=1 · sqlite 분기 유지 롤백 레버 · ★배너는 logService.warn/ADR-007 링 버퍼·앱 로그 스트림으로 흐르고 stdout/콘솔에는 안 나온다는 정확성 주석 · §11-6 갈림 경고 재인용), 13-7 롤백 포인터(§9/§11-7). 정직한 미실행 표시(§12 규약)로 운영 대상 명령은 P3 사람 몫 명기. AC(컨테이너 실측): grep 6게이트 전부 통과(operation-scenario·GRANT DELETE ON·NODE_SERVER_DEPRECATED·NEWS_SERVER_URL·distribution/tick·SHOW GRANTS) · npm run lint 통과 · npm test 1343(1342 pass·1 skip·0 fail 무회귀) · node --test spoolCanon.test.mjs 9 pass. 코드 변경 0(문서 전용)." },
    { "step": 7, "name": "closeout", "status": "blocked", "blocked_reason": "마감 게이트의 축 A(컨테이너 가능)는 실측 완료·green이나, 축 B(개발 머신 · 포터블 JDK 25 · 로컬 MySQL 8.0.46 · 리포 밖 news-mysql.env)는 이 컨테이너 부재로 실행 불가라 phase 마감 게이트를 닫을 수 없다. === 축 A 실측(2026-09-05 · 컨테이너 JDK 21·MySQL 없음) === (1) `node --test scripts/lib/spoolCanon.test.mjs test/client-server-target.test.js test/node-deprecation-banner.test.js` → tests 24 · pass 24 · fail 0 · skip 0 (3 suites). (2) `node scripts/operation-scenario.mjs --server node` → exit 0 · 9단계 전부 ok(health·로그인·수신처·작성·송고 DES·tick scanned=1 distributed=1 mineStatus=DPS·수신설정·수집 receive 200·GET articles matched=1) · spoolEntries=1 · data-safety=ok(news.db·uploads 무변·부재). (3) `npm run lint` → exit 0(무경고). (4) `npm test` → tests 1343 · pass 1342 · skip 1 · fail 0(54 suites · ~25s). 판정: 기준선 1328 + step4 client-server-target 8 + step5 node-deprecation-banner 7 = 1343 · 회귀(감소) 0. (5) `npm run build` → vite v6.4.3 exit 0 · 121 modules · dist/assets/index-COYNfnZU.js 415.06 kB(gzip 127.74) · built 1.60s. DB 비파괴: 컨테이너에는 news.db·uploads가 .gitignore로 부재이고 실행 후에도 생성 0(operation-scenario data-safety 단언). === 축 B 미실행(blocked) === jar 2개 빌드(mvnw=JDK 25)·`--dual --db sqlite/mysql` 스풀 diff 0·`mvnw clean verify` 0 fail/0 skip·`spring-contract.mjs --parity`/`--db mysql --parity` 각 313관측 diffs 0은 개발 머신 필수. 이 컨테이너는 JDK 21(pom 요구 25)·MySQL 부재·news-mysql.env 부재. 또한 축 B 마감은 step2(`--dual`)·step3(SchemaGuard) 산출물이 개발 머신에서 먼저 확정돼야 성립한다 — 그 둘도 blocked. 개발 머신+사람이 남긴 일은 forward_notes 참조." }
  ],
  "forward_notes": [
    "(1) **축 A 마감 실측 전문(2026-09-05 · 컨테이너 · JDK 21 · MySQL 없음 · Node/SQLite 축만)**: `node --test scripts/lib/spoolCanon.test.mjs test/client-server-target.test.js test/node-deprecation-banner.test.js` → tests 24 / pass 24 / fail 0 / skip 0 · `node scripts/operation-scenario.mjs --server node` → exit 0 · 9단계 ok · spoolEntries=1 · data-safety=ok · `npm run lint` → exit 0 · `npm test` → 1343 (pass 1342 / skip 1 / fail 0) = 기준선 1328 + step4(8) + step5(7) · 회귀 0 · `npm run build` → vite v6.4.3 exit 0 · 121 modules · index js 415.06kB(gzip 127.74kB) · 1.60s. DB 비파괴: 컨테이너에 news.db·uploads 부재(.gitignore) · 실행 후 생성 0.",
    "(2) **축 B 미실측(개발 머신 필수 · 이 컨테이너 실행 불가)**: jar 2개 빌드(`server-spring`·`tools/news-migrator`의 `./mvnw -B -q package -DskipTests` · JDK 25) · `node scripts/operation-scenario.mjs --dual --db sqlite`(스풀 diff 0) · `--dual --db mysql`(diff 0 · 잔존 harness_ct_* 0) · `cd server-spring && ./mvnw -B clean verify`(0 fail/0 skip · MySQL 요구) · `node scripts/spring-contract.mjs --parity`·`--db mysql --parity`(각 313관측 diffs 0 무회귀). 실행 환경: 포터블 Temurin JDK 25.0.4.1+1(`D:/agents/tools/jdk-25.0.4.1+1`) · 로컬 MySQL 8.0.46 · 리포 밖 `news-mysql.env`(docs/ops-mysql.md §3 한 줄씩 로드). 미실측=미실측으로 남긴다(추측 green 금지).",
    "(3) **개발 머신에서 먼저 완결해야 할 이 phase의 코드 산출물(둘 다 blocked)**: step2 scenario-dual-compare(`scripts/operation-scenario.mjs`에 `--dual`/`--server spring`/`--db` 추가 — 동일 seed 행 + tick만으로 Node↔Spring 스풀 바이트 대조 diff 0 · TDD · 실제 Spring 대상 실측으로만 검증) · step3 schemaguard-mysql-message(`SchemaGuard.verify()` 처방 문장을 this.mysql로 dialect 분기 · MysqlSchemaGuardTest 케이스 추가 · TDD). 둘 다 JDK 25(step3은 -Dtest 대상 테스트로 MySQL 회피 가능 · full verify는 MySQL 필요) 환경에서 작성·검증한다 — blind 작성 금지.",
    "(4) **P3의 남은 사람 몫(이 phase가 하지 않는 운영 사건)**: 운영 `news`로의 실제 데이터 컷오버 실행(`migrate --target news` · 정지 창·백업·root·승인) · `GRANT DELETE ON news.ReceiverConfig` 부착(root · 미부착이면 `DELETE /api/receiver-config/:id`가 계약 동결 200 대신 500 internal-error) · 운영 cron(tick) 실제 재지정 · Node 서버 실제 종료 시점 판단. 절차 정본은 `docs/ops-mysql.md` §13(P3 컷오버 실행 체크리스트).",
    "(5) **하네스가 못 보는 축의 방어선(GRANT DELETE)**: `--db mysql`은 news_ct=ALL ephemeral `harness_ct_*`에서 돌아 `GRANT DELETE ON news.ReceiverConfig` 부재를 구조적으로 못 본다(decisions (2)). 유일 방어선은 컷오버 go/no-go의 `SHOW GRANTS` 육안 판정(자동 게이트로 대체 불가) + 스테이징 자격 실증 테스트 `NewsAppMysqlWireTest`·`MinimumPrivilegeBoundaryTest`.",
    "(6) **정직한 공백 / 후속 P3 항목**: helmet 등가 보안 헤더 11종·HTTPS 강제(excluded (d) · ADR-013 공백) · 커넥션 풀 확대·동시성 튜닝(excluded (e) · LogsStreamWireTest 22 재설계) · sqlite 분기·`node:sqlite`·`sqlite-jdbc` 제거 및 Node 서버 실제 종료 판단(excluded (c) · 롤백 레버라 유지) · 부트 백필 2종(`backfillEmptyDepartments`·`backfillHistoryTitles`) 이식 및 운영 cron 구현(excluded (f)) · `User.userId` 769자 축 해소(excluded (g) · 계약 동결 해제 필요) · `DistributionTargetService.checkName`의 `String.trim()` 단일 출처화(phase 75 forward_notes (6)⑦).",
    "(7) **다음 사람이 먼저 열 파일**: `docs/ops-mysql.md` §13(P3 컷오버 실행 체크리스트 — go/no-go·GRANT·정지 창·클라 재지정·cron·Node 은퇴·롤백) · `scripts/operation-scenario.mjs`(검증 게이트 · `--dual` 추가 지점) · `server-spring/src/main/java/harness/news/db/SchemaGuard.java`(step3 처방 분기 지점) · `phases/76-server-cutover-ops/step2.md`·`step3.md`·`step7.md`(잔여 AC 정본)."
  ]
}
