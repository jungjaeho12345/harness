{
  "project": "기사 작성기 (article production system)",
  "phase": "security-hardening",
  "steps": [
    {
      "step": 0,
      "name": "session-cookie-transport",
      "status": "completed",
      "goal": "세션 토큰을 HttpOnly/Secure/SameSite 쿠키로도 발급·검증하도록 서버 transport를 전환한다. 헤더 x-session-id는 하위호환으로 계속 수락(쿠키 우선). 세션 스토어/도메인 로직은 무변경, CORS는 credentials 허용으로 확장.",
      "dependsOn": [],
      "dbImpact": "none",
      "summary": "server/index.js transport만 전환. 쿠키명 SESSION_COOKIE_NAME='yh.sid'. sessionCookieOptions(env) 빌더: httpOnly 항상 true, maxAge 1h(보조 — 만료의 단일 진실은 sessionService), path '/'; env 분기 — production은 sameSite:'none'+secure:true(cross-site SPA:5173↔API:3001 전송 필수, None은 Secure 동반 필수), 그 외(dev/test http)는 sameSite:'lax'+secure:false(http localhost에서 쿠키 전송 보장, None은 Secure 없이 브라우저가 거부하므로 lax). cookie 파서는 cookie-parser 의존성 추가 대신 36줄짜리 parseCookies 헬퍼를 직접 구현(ADR 외부 의존성 최소화 철학 존중). sidFrom(req) 우선순위: 1)파싱 쿠키[yh.sid] 2)x-session-id 헤더(하위호환). sessionOf는 sidFrom→touchSession으로 검증된 세션에서만 신원/role 도출(req.body.role 불신 — ADR-004 유지). /api/login은 body {ok,sessionId,user} shape 그대로 유지하며 res.cookie 추가; /api/logout은 invalidate 후 clearCookie. CORS credentials:true + 명시 origin allowlist(localhost/127.0.0.1:5173) 유지, allowedHeaders에 x-session-id 하위호환 유지. SSE(/api/stream) ?session= 폴백은 무변경(step2 소관). 신규 test/session-cookie.test.js 8개 추가, 기존 server.test.js 14개 무회귀. dbImpact none."
    },
    {
      "step": 1,
      "name": "https-enforcement",
      "status": "completed",
      "goal": "프로덕션에서 HTTPS를 강제한다(HSTS via helmet + http→https 리다이렉트 + trust proxy). step0의 Secure 쿠키와 정합. 로컬/테스트(NODE_ENV!=='production')는 무영향.",
      "dependsOn": [0],
      "dbImpact": "none",
      "summary": "server/index.js transport만 변경. enforceHttps(env) 빌더: 비프로덕션은 no-op 미들웨어(next 통과 — 로컬/테스트 http app.listen(0) 무영향), 프로덕션만 평문→https 308 리다이렉트(308은 메서드·바디 보존, GET/POST 모두 안전). proto 판정은 req.secure(trust proxy=1로 X-Forwarded-Proto 반영)로, 리다이렉트 Location의 host는 req.get('host')로 클라이언트 요청 Host 보존. createApp은 프로덕션에서만 trust proxy=1(첫 1홉만 신뢰 — 'true' 무제한 신뢰는 X-Forwarded-* 스푸핑으로 강제·레이트리밋 우회 위험이라 회피). HSTS는 helmet 기본이 https 응답에만 유효하므로 프로덕션에서만 의미. 미들웨어 순서: enforceHttps를 helmet/cors/도메인보다 앞에 둬 평문이 도메인까지 도달 못하게 함. test/https-enforcement.test.js 신규(env 분기 단위 + 프로덕션 리다이렉트/HSTS/인증 정상 + 비프로덕션 회귀가드). dbImpact none. 무회귀 백엔드 194 pass. (rawFetch를 fetch→node:http.request로 작성 — fetch가 Host를 금지 헤더로 무시해 프록시 뒤 임의 Host(example.com) 모사가 불가했던 테스트 결함을 수정, 리다이렉트의 Host 보존을 실제로 검증.)"
    },
    {
      "step": 2,
      "name": "sse-cookie-auth",
      "status": "completed",
      "goal": "SSE(/api/stream)의 평문 ?session= 쿼리 토큰 폴백을 제거하고 쿠키(우선)+x-session-id 헤더로만 인증한다(sidFrom 재사용). 프론트 withCredentials 적용은 step4 후속.",
      "dependsOn": [0],
      "dbImpact": "none",
      "summary": "server/index.js의 /api/stream 라우트만 변경(transport). sid 도출을 인라인 'req.get('x-session-id') || req.query.session'에서 step0의 sessionOf(req)→sidFrom(req)(쿠키 yh.sid 우선→x-session-id 헤더 폴백)으로 교체하고 평문 ?session= 쿼리 인증 폴백을 제거. 인증 실패는 기존대로 401, 인증 통과 후 SSE 응답 헤더/ready 이벤트/bus.on('change') 무효화 배선·req.on('close') 정리는 무변경(ADR-005 무효화 신호 방식 유지 — 행 데이터 미포함). 보안 근거: 쿼리 토큰은 URL·서버/프록시 액세스 로그·브라우저 히스토리·Referer로 새는 표면이라 HttpOnly 쿠키로 대체해 그 표면을 닫음. 헤더(x-session-id) 폴백은 유지 — 하위호환. cross-origin EventSource의 쿠키 첨부(withCredentials:true)와 프론트 ?session= 제거는 step4 소관(이 step은 서버가 쿼리를 더 이상 신뢰하지 않게 만드는 것까지). 신규 test/sse-auth.test.js 6개(쿠키 통과/헤더 통과/쿼리 401/무인증 401/위조 401/슬라이딩 touchSession) — node:http.request로 쿠키·헤더 직접 제어, 첫 SSE 청크만 읽고 소켓 종료(step1 rawFetch 패턴). 기존 server.test.js의 ?session= 의존 stream 테스트는 x-session-id 헤더 방식으로 갱신(폴백 제거가 의도된 변경). AC(lint/build/test) 통과, 백엔드 무회귀 200 pass(194→200, +6). dbImpact none."
    },
    {
      "step": 3,
      "name": "account-lockout",
      "status": "completed",
      "goal": "로그인 실패 누적 시 계정 잠금. User 테이블에 additive 컬럼(failedLoginCount, lockedUntil) 추가. userService/userModel에서 잠금 판정·증가·리셋. 기존 IP 레이트리밋·bcrypt 타이밍 평준화·active='N' 차단과 공존, DB 비파괴.",
      "dependsOn": [],
      "dbImpact": "additive (User 테이블 컬럼 추가 — failedLoginCount, lockedUntil. 행 삭제/DROP 없음)",
      "summary": "계정(userId) 단위 잠금을 IP 레이트리밋과 독립 방어층으로 추가. [스키마] src/db/schema.js SCHEMA.User에 additive 컬럼 2개만 추가 — failedLoginCount(TEXT DEFAULT '0'), lockedUntil(TEXT, ISO-8601 UTC 자동해제 시각). createSchema의 멱등 ALTER ADD COLUMN 패턴으로 기존 행 보존(DROP/DELETE 없음). Contents의 편집잠금 lockYN과 이름 충돌 회피(계정 전용 이름). [정책상수] userService.js에 LOCKOUT_THRESHOLD=5, LOCKOUT_DURATION_MS=15분. 근거: news.md에 계정 단위 구체 수치 없어 보수적 기본값 채택. 자동해제(시간경과) 방식 — 영구잠금은 관리자 해제경로가 필요해 범위를 키우므로 회피, lockedUntil 한 컬럼으로 단순화(별도 플래그 불요). [모델] userModel.COLUMNS 화이트리스트에 두 컬럼 추가 — 기존 findById가 읽고 update가 쓰며, 임의 컬럼 주입은 화이트리스트가 차단. 신규 메서드 없이 기존 update 재사용. [서비스] userService.login: findById→hash=row.password||DUMMY_HASH로 없는 사용자도 bcrypt 1회(타이밍 평준화 유지). 잠금검사(row && lockedUntil && now()<Date.parse)는 bcrypt 비교 후 수행해 잠긴 경로도 비교 1회 — locked 반환. 실패 시 n=Number(failedLoginCount??'0')+1 → String(n) 재저장(문자열 '0'+1 누적버그 회피), n>=임계치면 lockedUntil=now()+DURATION stamp. 단 응답 reason은 invalid-credentials로 일관(계정 존재/잠금 임박 미노출 — 사용자 열거 방어). 성공+active=Y면 카운터 0 리셋·lockedUntil 비움. active=N은 기존대로 inactive. now 주입(createUserService({userModel, now}) — Date.now() 직접 호출 금지, sessionService 패턴 일관). SAFE_FIELDS에 잠금필드 미포함(비밀번호와 동급 내부상태 — 응답 미노출). [transport] server/index.js 무변경 — STATUS_BY_REASON.locked=409 기존 계약 유지(중복 추가 안 함), /api/login은 fail()로 reason 매핑만(로직 재구현 없음), IP 레이트리밋 15분/10회 그대로. locked HTTP 코드 결정: 기존 409 유지(409 의존 테스트·프론트 영향범위 보존 — 423/429 변경은 회귀 위험으로 미채택). [테스트] 신규 test/account-lockout.test.js 8개(가짜 시계+in-memory userModel 주입: 임계치 미만 증가/임계치 도달 후 올바른 PW도 locked/시간경과 후 성공·리셋/성공 시 리셋/잠금필드·password 응답 미노출/active=N inactive 회귀/없는 사용자 미잠금·행 미생성/문자열 누적버그 회귀). schema.test.js +2(additive 컬럼 존재·lockYN 미존재, 기존 행 보존·기본값). server.test.js +1(409 locked 매핑·sessionId/user 미노출). dbImpact additive. AC lint/build/test 전부 통과, 백엔드 무회귀 211 pass(200→211)."
    },
    {
      "step": 4,
      "name": "frontend-cookie-session",
      "status": "pending",
      "goal": "프론트엔드 Model/httpModel을 쿠키 세션과 정합하도록 전환한다(fetch credentials:'include', EventSource withCredentials, sessionStorage 토큰 의존 제거). 응답에서 sessionId를 더 이상 신뢰하지 않고 /api/session 신원으로 복원.",
      "dependsOn": [0, 2],
      "dbImpact": "none"
    }
  ]
}
