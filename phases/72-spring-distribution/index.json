{
  "project": "기사 작성기",
  "phase": "72-spring-distribution",
  "scope": "C++/Spring 포팅 로드맵(docs/porting-plan-cpp-spring.md §7) **P1 'Spring 서버 API 패리티'의 네 번째 도메인 묶음 후반부 — 배부 실행(distribution execution)**을 소유한다. `phases/71-spring-collection`이 세운 공유 자산(계약 하네스의 프로파일 축·`failclosed`·`DIST_SPOOL_DIR` 주입 · ADR-008 정적 게이트 `Adr008DisciplineTest`)을 **상속**해, 남은 10 라우트 중 **3개**를 구현한다: `distribution-tick`(`POST /api/distribution/tick`) · `distribution-failures`(`GET /api/distribution/failures`) · `distribution-retry`(`POST /api/distribution/retry`). 완료 판정은 **계약 케이스 2파일이 Spring 대상에서 green + Node 리포트 대비 diff 0**이 되는 것이다: `contract/cases/minimal/distribution-disabled.contract.js`(스풀 미설정 축) · `contract/cases/default/distribution-tick.contract.js`(실배부 축). 여기 걸리는 계약 = **ADR-008 아키텍처의 전면 이식**(파일 스풀 outbound · 외부 cron tick pull — 앱 내 주기 타이머 0 · 배부 축 네트워크 egress 0 · 자동 재시도·백오프·큐 0) · **스풀 쓰기**(`DIST_SPOOL_DIR` 미설정 = 배부 전면 비활성, 기본값 하드코딩 금지 · `.tmp` → rename 원자 게시 · 슬러그 재검증 · outbound 필드 allowlist 18+2 · `distributedAt` 주입) · **엠바고 정책**(순수 모듈 — `new Date()` 없음, now는 항상 ISO 문자열 인자 · required ⊆ distributed → DPS, 1건 이상 → EPS, 아니면 DES · EPS→DES 역행 금지 · 사이클 경계 `latestSendId` · 안전 기본값) · **tick 계약**(Z 전용 · **body 미판독** · 프로세스 내 single-flight · 반환 6키 · 실패 투영 화이트리스트 4필드 · throw 금지) · **실패 원장·재전송**(append-only · 미해소 판정은 (articleId,targetId,action) 그룹의 최대 id 행이 `distribute-failed` · 재전송 가능 사유 3종 · 같은 사이클 중복 억제 · 입력은 `historyId` 하나 · 409 토큰 4종 · 서버측 장애 3토큰은 라우트에서 500 재매핑) · **송고 즉시 배부**(ADR-008 (4) — 엠바고 미설정 DPS는 송고 시 press+nonpress 전량). Node 서버(`server/**`·`src/**`)·계약 스위트(`contract/**`)·계약 명세(`docs/api-contract/**`)·정본 러너(`scripts/contract-run.mjs`·`scripts/contract-diff.mjs`)는 **한 줄도 고치지 않는다**.",

  "baseline": "**이 phase는 `phases/71-spring-collection`의 머지 결과 위에서 시작한다.** 브랜치는 **`feat-72-spring-distribution`**이고, **`feat-0-mvp`의 71a 머지 커밋에서 분기한다 — 이 한 경로뿐이다.** 71a가 쓰는 `feat-71-spring-distribution`을 재사용하거나 그 위에 쌓지 마라(같은 브랜치를 두 phase가 가리키면 71a PR과 충돌하고, 스택 PR 바텀업 머지에서 `--delete-branch`가 다음 PR을 auto-close시킨 전례가 있다). **기준선 수치는 step0이 71a 머지 트리(`825ce48`)에서 직접 재측정한 값이다(2026-08-25 — 71a 마감 보고값과 전부 일치했다)**: Java `mvnw -B verify` **772 tests / failures 0 / errors 0**(jar 35,664,982 B) · 계약 하네스 **5 프로파일**(default·minimal·auth-negative·prod-cookie·**failclosed**) · `--parity` exit 0 **236관측**(default 176 · minimal 48 · auth-negative 4 · failclosed 5 · prod-cookie 3) diffs 0 · `--dual-run` exit 0 동일 236관측 diffs 0 · `npm test` **1328 pass / 0 fail** 불변 · Spring 구현 라우트 **29/39**(`HandlerInventoryTest`) · lint·build clean · `contract-inventory-check --require-spec-paths` exit 0(routes=39 spec-paths=39/39) · Node 대상 `--require-full-coverage` exit 0(profiles=5 cases=274 covered=39/39). **환경 주의**: IDE(Eclipse/JDT)가 `server-spring/target/**`에 JDK 25(class file 69) 산출물을 남기면 `mvnw -B verify`가 surefire fork에서 즉사한다 — `target/`은 git 무추적 빌드 산출물이므로 `mvnw -B clean verify`로 1회 리셋하면 된다(소스 무관). 71a 종료 시점 Spring 구현 라우트는 **29개**이고 이 phase 종료 시점에는 **32개**가 된다(스텁 0 — `HandlerInventoryTest`가 정확 집합을 잠근다). **이 phase가 구현하는 3 라우트는 이미 `RoutePolicy` 표에 `AuthClass.ADMIN`(requiresSession=true)으로 올라 있다 — RoutePolicy를 고칠 일이 없다.** **71a에서 상속하는 것(재설계 금지)**: ① `scripts/spring-contract.mjs`의 `host`/`spool`/`token` 축과 `failclosed` 프로파일 — `default.spool=true`라 **Spring 인스턴스에 `DIST_SPOOL_DIR`이 이미 주입돼 있다**(이 phase에서 그 스풀이 처음 쓰인다) ② `Adr008DisciplineTest`와 그 **예외 2파일 목록**(`HttpApiSourceFetcher.java`는 71a가 채웠고 **`SpoolWriter.java`는 이 phase step3이 채운다** — 이름은 이미 등재돼 있다) ③ 별도 `@ConfigurationProperties` 분리 관례(`CollectionProperties`가 선례 — 이 phase는 `SpoolProperties`를 만들고 `AppProperties`와 그 9개 테스트 호출부는 **무접촉**). 툴체인(Windows): 리포 루트 `d:/agents/harness` · 포터블 JDK 21 `D:/agents/tools/jdk-21.0.12+8` · Maven은 `server-spring/mvnw` 래퍼. 프레시 체크아웃이면 `npm ci`를 먼저 돌린다. Spring 인스턴스 포트는 **[15000, 20000)**. **무접촉(수정·`git add` 금지)**: `contract/**` · `docs/api-contract/**` · `scripts/contract-run.mjs` · `scripts/contract-diff.mjs` · `server/**` · `src/**` · `web/**` · `client/**` · `test/**` · `package.json` · 리포 `news.db` · `uploads/` · `docs/news.md` · `spikes/**` · `.claude/skills/**`. **가변**: `server-spring/**` · `server-spring/README.md` · `docs/ADR.md`(ADR-013 ④ 실측 1문장 — **이 phase 마감에서 1회만**) · `scripts/spring-contract.mjs`(scope 표 2행) · `phases/72-spring-distribution/**` · `phases/index.json`. 전 step은 diff scope를 **자기 step 시작 시점의 `git status --porcelain` 스냅샷 대비 증분**으로만 판정한다(절대 목록 비교 금지, `restore`/`checkout`/`stash`/`clean` 금지). 커밋은 **명시 경로만** `git add`(`-A` 금지).",

  "order": "step0 history-distribution-queries → step1 embargo-policy → step2 failure-ledger → step3 spool-writer → step4 distribution-service → step5 embargo-status-service → step6 send-distribution-hook → step7 tick-service → step8 retry-service → step9 distribution-http(**green — 2파일**) → step10 parity-closeout. 근거: (a) **계약 파일 간 픽스처 의존을 먼저 조사했다.** 배부 2파일이 부르는 픽스처 라우트는 **전부 이미 구현돼 있다**: `distribution-disabled.contract.js`는 `distribution-targets` 4라우트(phase 70)뿐이고, `distribution-tick.contract.js`는 `distribution-targets` 4라우트 + `createArticle`(POST /api/articles) + `POST /api/articles/:id/action`(송고) + `GET /api/articles/:id`(phase 69)다. **수집 라우트를 픽스처로 부르는 배부 계약 파일은 없다** — 이 사실이 phase 절단의 근거다. (b) **green 지점은 step9 한 곳이고 그 안에 두 축이 있다.** 컨트롤러 파일이 하나이고 스텁 금지 규율상 3 라우트를 절반만 결선할 수 없어 minimal 축과 default 축이 같은 step에 온다 — 대신 step9의 검증 절차가 **`--profile minimal`(인가·503·failures)를 먼저 green으로 만들고 그 다음 `--profile default --files .../distribution-tick.contract.js`(실배부)**를 보게 해 실패 원인을 두 축으로 분리한다. (c) **step0~8은 계약 green이 없는 9 step 연속 구간**이다(이것이 phase를 절단한 직접적 이유다). 그 구간의 판정은 **Java 와이어/단위 테스트 + 이미 green인 scope의 무회귀(`--parity` diffs 0 · 관측 수 불변) + 지정된 진단 실행의 실패 서명 일치**로 한다. (d) **아래에서 위로**: 리포지토리 조회 → 순수 판정 2종(엠바고·실패원장) → 파일 쓰기 어댑터 → 도메인 서비스 → 상태 반영 → 송고 훅 → 오케스트레이션(tick·retry) → 컨트롤러. (e) **송고 훅(step6)은 tick(step7)보다 앞이다**: 두 경로가 `ArticleEmbargoService`(step5)를 공유하고, 송고 훅이 먼저 붙어야 default 프로파일의 `--parity`가 '배부가 결선된 Node vs 배부가 결선된 Spring'을 비교하게 된다(한쪽만 결선된 상태로 오래 두지 않는다). (f) **scope 표는 green이 되는 step(step9)에서만 늘린다.**",

  "decisions": [
    "(1) **이 phase는 `71-spring-collection`의 후반부이며 그 자산을 상속한다.** 재설계 금지 목록과 머지 순서는 baseline에 있다. 요약: **하네스 프로파일 축·`failclosed`·`Adr008DisciplineTest`는 71a 소유**이고 이 phase는 **`SpoolWriter.java`를 그 예외 목록의 빈 자리에 채우는 것** 외에 게이트를 손대지 않는다. **`docs/ADR.md` ADR-013 ④의 phase 실측 문장은 이 phase 마감에서 1회만** 추가한다(71a는 ADR을 고치지 않았다).",

    "(2) **ADR-008 정적 게이트의 예외를 넓히는 것은 그 자체가 아키텍처 결정이다.** 71a가 `theExceptionListIsExactlyTwoFiles`로 예외 목록의 **크기와 구성**을 단언해 두었다. 이 phase의 정상 경로에서 그 테스트 파일은 **변경 0**이어야 한다(`SpoolWriter.java`가 이미 목록에 있다). 변경이 필요하다고 느껴지면 그것은 예외 확대이며 **별도 근거와 리뷰가 필요하다** — step 증분 목록에 조용히 얹지 마라.",

    "(3) **`DIST_SPOOL_DIR` 미설정 = 배부 전면 비활성이며 기본값을 하드코딩하지 않는다.** 설정은 **별도 `@ConfigurationProperties` 레코드 `SpoolProperties`**(`app.distribution.spool-dir=${DIST_SPOOL_DIR:}`)로 바인딩한다 — `AppProperties`는 record이고 `new AppProperties(...)` 호출부가 테스트 9곳(`config/AppPropertiesTest.java` 8 · `web/AllowedOriginsTest.java` 1)이라 거기에 필드를 더하면 그 두 파일이 반드시 함께 바뀐다(71a `CollectionProperties` 선례). **빈 값·공백이면 스풀 루트가 없다**(경로 추정 금지 — cwd·DATA_DIR 하위 등). 결선 규칙(Node `src/controllers/index.js` 71~130행 동형): 스풀 루트 없음 → `SpoolWriter` 없음 → `DistributionService` 없음 → ① tick은 **인가 통과 후** `spool-disabled` ② retry는 **어떤 DB 조회도 하기 전에** `spool-disabled` ③ **failures(조회)는 스풀 설정과 무관하게 항상 결선된다**(200) ④ 송고 훅은 결선 없음(minimal 프로파일의 결정성 전제 — phase 69 decisions (2)). **판정 지점은 단 하나**여야 한다(두 곳이 갈라지면 tick과 retry의 503 조건이 어긋난다).",

    "(4) **`ReasonStatus`에 7토큰을 추가하고, 재전송의 서버측 장애 3토큰은 라우트에서만 500으로 재매핑한다.** 추가: `spool-disabled`(503) · `tick-failed`(500) · `no-failure`(404) · `status-changed`(409) · `kind-changed`(409) · `stale-cycle`(409) · `retry-in-flight`(409). 전부 Node 표(server/index.js 322~352행)에 있고 이 phase에서 **HTTP로 도달 가능**하다 — 뒤 4개는 실패 원장을 직접 시드한 Java 와이어 테스트가 도달시킨다(도달 테스트 없이 표만 늘리는 것은 phase 69 decisions (19) 위반이다). **`spool-write-failed`·`invalid-spool-dir`·`invalid-article-id`는 전역 표에 넣지 마라** — `POST /api/distribution/retry`에서만 500이다(Node server/index.js 775~782행). `invalid-spool-dir`는 **배부 대상 CRUD의 입력 검증 거부(400)와 같은 토큰**이라 전역화하는 순간 `distribution-targets.contract.js`가 red다(로그인 `locked` 423 선례와 같은 이유로 Node도 전역화하지 않았다). **표와 라우트 폴백은 한 쌍으로 검증한다**(phase 69 forward_notes (14)).",

    "(5) **`POST /api/distribution/tick`은 body를 읽지 않는다 — 구조로 강제한다.** 핸들러 메서드에 `@RequestBody`·`json.readBody(request)` 호출을 **두지 않는다**(있으면 role·시각·대상 목록 주입 경로가 생기고 ADR-004가 무너진다). 잠금은 2층: ① 계약이 주입 body를 보내고 응답 shape·`at`·`distributed` 멤버십으로 무효를 단언 ② **리플렉션 단언** — tick 핸들러 시그니처에 `@RequestBody` 파라미터가 0개임을 Java 테스트가 확인한다. `actorUserId`는 **검증된 세션에서만** 온다.",

    "(6) **엠바고 판정 모듈은 순수하다 — `Clock`도 주입받지 않는다.** `EmbargoPolicy`는 `src/services/embargoPolicy.js`(207행) 1:1 이식이며 **시각을 인자로만 받는다**(`now`는 항상 ISO-8601 UTC 문자열). 클래스에 `Clock` 필드를 두는 순간 tick의 '실행당 1회 시계 읽기' 불변식이 깨진다. **안전 기본값의 방향이 계약이다**: now 파싱 불가 → `[]`(조기 배부 금지) · 사이클 경계 미확정 → 전체 이력을 센다(넓게) · id를 알 수 없는 distribute 행 → 이번 사이클에 **포함해서** 센다 · `EPS→DES` 역행 금지 · `MUTABLE_STATUSES`(DES·EPS) 밖은 절대 건드리지 않는다.",

    "(7) **`Date.parse` 의미론은 전부 이식하지 않는다 — 범위를 명시하고 나머지는 '미도래'(안전 방향)로 수렴시킨다.** 신설 `NodeInstants.parseIsoMillis(String)`가 덮는 범위: ① `Instant.parse` 가능한 Z 표기 ② `OffsetDateTime.parse` 가능한 오프셋 표기 ③ `YYYY-MM-DD` 날짜만(= UTC 자정, JS와 동일). 그 밖(오프셋 없는 날짜-시각 `YYYY-MM-DDTHH:mm`, 레거시 문자열)은 **null**이다. 근거: (i) 이 시스템이 쓰는 값은 전부 ISO UTC다(`web/src/view/WriterPage.jsx`의 `localInputToIso`가 유일한 입력 경로) (ii) 오프셋 없는 날짜-시각을 JS는 **로컬 시간**으로 해석하는데 이식하면 서버 TZ가 판정에 들어와 결정성이 무너진다 (iii) 틀리는 방향이 **안전측**이다(파싱 불가 → 미도래 → 배부 안 함 → tick 응답 `invalid` 배열에 표면화). forward_notes에 기록한다.",

    "(8) **송고 훅은 Spring에서 동기 실행하되 응답 status는 배부 이전 값이다.** Node는 fire-and-forget이라 `applyAction`이 **배부 전 status**(`finalStatus`)를 즉시 돌려준다. Spring이 동기로 하면 응답이 **배부 후 승격 status**로 바뀔 수 있다(2차 엠바고만 설정된 기사: 송고 DES → press 배부 → `syncEmbargoStatus` → EPS). **규칙: `applyAction`의 반환 status는 훅 실행 여부와 무관하게 `finalStatus`다.** 동기를 택하는 이유는 (i) `@Async`가 정적 게이트에 걸리고 (ii) 비동기는 `--dual-run` 자기 결정성을 깨뜨리기 때문이다. **배부 실패가 송고를 되돌리지 않는다**(훅 전체 예외 격리). 훅은 **이력 기록 뒤**에 온다(사이클 경계 `latestSendId`가 이번 배부보다 앞에 놓여야 한다).",

    "(9) **송고 시 배부 kind 결정은 `distributionKindsForSend` 문자 그대로다.** `DPS` + 엠바고 미설정 → `['press','nonpress']` · `DPS` + 엠바고 설정 → **이미 배부된 kind에만**(정정본) · `DES` + `!embargoAt && secondEmbargoAt` → `['press']` · 그 외 → `[]`. **여기서 시각 비교를 하지 마라**(시점 판정은 tick). 이 판정의 '이미 배부된 kind'는 **`distributedKinds`(전체 이력)**이고 승격 판정(`syncEmbargoStatus`)은 **`cycleDistributedKinds`(이번 사이클)**다 — **두 함수는 서로를 대체하지 않는다. 통일하지 마라**(phase 69 forward_notes (13) 계열의 함정).",

    "(10) **phase 69의 `embargoAware`/`embargoSet`은 리팩터링하지 않고, 등가성은 관측 가능한 경로로 잠근다.** Node도 `articleService.js` 218행에서 인라인 판정을 쓰므로 두 곳이 갈린 것이 정본이다. 그리고 `ArticleLifecycleService.embargoSet`은 **`private static`**이라 같은 패키지 테스트도 부를 수 없다 — 가시성을 넓히는 것은 '그 파일을 고치지 마라'와 충돌한다. **처분(② 검토 판정 반영, 선택지 (b))**: `applyAction`의 **관측 가능한 DES 진입**으로 등가성을 잠근다 — `embargoAt`/`secondEmbargoAt` 2컬럼의 4×4 조합(값 있음·빈 문자열·null·공백)마다 D 세션 송고 후 상태가 **`DES`가 되는 것과 `EmbargoPolicy.requiredKinds(contents).size() > 0`이 동치**임을 단언한다. **private 접근·가시성 확대·소스 리팩터링 전부 하지 않는다.**",

    "(11) **스풀 파일의 필드 allowlist는 20키이고 블랙리스트가 아니다.** Contents **18키**(`articleId,title,author,coAuthor,department,departmentCode,category,region,attribute,keyword,externalComment,attachmentFile,referenceFile,createdAt,sentAt,embargoAt,secondEmbargoAt,status`) + Article **1키**(`markupVersion`) + 서버 주입 `distributedAt`. **`internalComment`와 편집 잠금 5컬럼은 의도적으로 제외**한다 — 새 컬럼이 생겨도 기본값은 미노출이다. **pick 의미론**: `값 != null`일 때만 담는다 = **SQL NULL 컬럼은 키 자체가 빠진다**(API 투영의 'NULL 키 보존'과 정반대 — 두 규칙을 섞지 마라). 조립 순서는 `LinkedHashMap`으로 재현하고 Java 테스트가 **키 순서**를 단언한다(외부 전송기가 읽는 파일이라 순서도 산출물이다).",

    "(12) **원자적 게시와 경로 합성 방어는 `SpoolWriter` 단일 지점이다.** ① `rootDir` 없으면 `spool-disabled` ② 저장된 값이라도 신뢰하지 않고 phase 70의 `SpoolDir.sanitizeSpoolDir`로 **재검증**(빈 결과 → `invalid-spool-dir`, 복제 금지) ③ `articleId`는 `^[A-Za-z0-9_-]{1,64}$`(아니면 `invalid-article-id`) ④ 파일명 `<articleId>_<compactStamp>.json` ⑤ **같은 디렉토리 안** `.<name>.tmp`에 쓰고 `Files.move(..., ATOMIC_MOVE)` — **일반 move로 폴백하지 마라**(원자성 보장을 조용히 잃고 외부 전송기가 부분 파일을 집어간다) ⑥ 내용은 **UTF-8 명시**(Windows 기본 인코딩은 UTF-8이 아니다) ⑦ **throw하지 않는다**(모든 실패는 `{ok:false, reason}`).",

    "(13) **tick 응답은 화이트리스트 투영이고 서버 파일시스템 경로가 한 글자도 실리지 않는다.** 반환 6키 `{ok,at,scanned,distributed,failed,invalid}`(재진입 스킵 시에만 `skipped:'in-progress'`가 더해진 7키 — 계약은 직렬 실행이라 그 경로를 관측하지 못하므로 **Java 테스트가 유일 방어선**이다). `distributed` 원소 3키 · `failed` 원소 **4키**(실물 실패 항목은 `spoolDir`을 갖고 있다) · `invalid` 원소 2키. 사유는 **고정 토큰만**(`tick-failed`·`status-changed`·`no-active-target`·`spool-write-failed`) — 예외 메시지를 쓰면 경로가 실려 화이트리스트가 우회된다. 계약이 응답 전체 문자열에서 `spoolDir`·슬러그·`.json`·**경로 구분자** 부재를 단언한다.",

    "(14) **tick은 throw하지 않고 프로세스 내 single-flight다.** 후보 조회 실패 → `{ok:false,reason:'tick-failed'}`(라우트가 500) · 기사 단위 예외는 그 기사만 `failed`에 고정 토큰으로 남기고 스캔을 계속한다. 재진입은 스캔 없이 스킵 응답이고 **플래그 해제는 `finally`**다(실패 반환 경로에서 안 풀리면 tick이 영구 무력화된다). **다중 인스턴스 중복은 앱이 막지 않는다** — ADR-008 (3)의 '외부 cron 단일 트리거' 운영 규율이 소유한다(분산 락 금지).",

    "(15) **실패 원장 판정은 순수 파생이고 규칙은 한 벌뿐이다.** 그룹 키는 **(articleId, targetId, action) 3원소**, 그룹에서 **id가 가장 큰 행**이 `distribute-failed`면 미해소·`distribute-retry`면 해소. 판정에 참여 못 하는 행은 **조용히 무시**(throw 금지). 결과는 historyId DESC이고 **경로성 필드를 절대 담지 않는다**. `RETRYABLE_FAILURE_REASONS`는 **정확히 3개**(`spool-write-failed`·`invalid-spool-dir`·`invalid-article-id`) — `status-changed`·`spool-disabled`를 담으면 영원히 해소되지 않는 항목이 쌓인다. 재전송 게이트와 목록이 이 파생 하나만 쓴다 — **복제하면 인가 우회**다(목록에 없는 실패로 재전송이 통과한다).",

    "(16) **스캔 상한은 목적마다 다르고 통일하면 결함이 된다.** 세 상수의 값은 Node 소스에서 **실측**한다: ① 표시용 목록 창(`DEFAULT_LIST_LIMIT`/`MAX_LIST_LIMIT`, `distributionRetryService.js`) ② 재전송 게이트의 미해소 조회(`RETRY_SCAN_LIMIT` — articleId 스코프 + 사실상 무제한. 표시용 창을 쓰면 오래된 실패가 `no-failure`로 오거부되어 **복구 불가**가 된다) ③ 배부 중복 억제 판정의 이력 조회(`FAILURE_DEDUP_SCAN_LIMIT`, `distributionService.js` 30행 — 창 밖으로 밀린 최신 실패를 놓치면 억제가 무의미해진다. 한 기사로 좁혀져 있어 비용은 작다). **셋을 하나로 합치지 마라**, 그리고 ②③ 각각에 '창을 좁히면 red'인 테스트를 둔다.",

    "(17) **재전송 게이트 순서가 계약이고, 어떤 거부 경로에서도 스풀 쓰기가 일어나지 않는다.** (a) `spoolWriter` 유무 → `spool-disabled`(**DB 무접촉**) (b) `historyId` 정규화 실패 → 즉시 `no-failure`(**어떤 이력 조회도 하지 않는다** — 전역 스캔 봉쇄) → 배부 이벤트 단건 조회 → **미해소 집합 멤버십**(보안 핵심) (b') `stale-cycle` (c) `retry-in-flight`(수신처 단위 키, 해제는 `finally`) (d) `not-found`/`inactive` (e) `kind-changed`(**엄격 비교**) (f) `not-found` (g) `status-changed` (h) 쓰기. **게이트 거부는 이력을 남기지 않는다**(시도조차 하지 않았으므로 사실 기록이 아니다). 입력은 `historyId` 하나뿐이고 articleId·targetId·kind는 **그 실패 행에서만** 도출한다(ADR-004).",

    "(18) **Node 의미론 파생은 단일 출처만 쓴다(phase 70 review_gate high-1 승계 — 이 phase의 최우선 금지사항).** 숫자 판독은 **`NodeNumber.toNumber`**, 공백 제거는 **`NodeString.trim`**이다. 해당 지점: `GET /api/distribution/failures`의 `limit`(Node `Number(req.query.limit)`) · `POST /api/distribution/retry`의 `historyId`(Node `Number(body.historyId)`) · 실패 원장의 `targetId` 정규화. **반복 쿼리 키는 값 리스트를 그대로** 넘긴다: `?limit=1` → `'1'` → 1 · `?limit=1&limit=2` → 배열 → `Number(['1','2'])`는 `'1,2'` → NaN → 기본값 → **200**(첫 값으로 접지 마라).",

    "(19) **다중 문장 쓰기의 트랜잭션 규율.** `ArticleHistoryRepository.insert`는 **이미 `GeneratedKeyHolder`로 삽입과 id 회수를 한 문장에서 처리한다**(111~119행) — phase 70에서 실측 재현된 'id 오배정'은 `last_insert_rowid()`를 **별도 문장으로** 부르던 `ReceiverConfigRepository`·`DistributionTargetRepository`의 문제였고 이력 원장에는 해당하지 않는다. 이 phase에서 `TransactionTemplate`이 필요한 자리는 **논리적으로 함께 성립해야 하는 두 문장**이다: 승격 status 쓰기 + 그 이력 insert(step5) · 재전송 성공의 `distribute-retry` 이력 + `distributedAt` 갱신(step8). **동시 삽입 id 귀속 테스트는 회귀 잠금으로 유지한다**(이력 id가 재전송 식별자이므로 뒤바뀌면 남의 실패를 재전송한다 — 지금 안전하다는 사실을 테스트가 지킨다). **단, 한 배부 호출 전체를 하나의 트랜잭션으로 묶지 마라**: 수신처 A 성공 후 B 실패에서 A의 이력까지 롤백되면 '나간 파일은 있는데 기록은 없는' 상태가 되어 다음 tick이 중복 배부한다(회수 불가).",

    "(20) **`GET /api/distribution/tick`은 이미 404이며 새 특수처리를 만들지 않는다.** `GlobalErrorHandler.handleNotFound`가 `HttpRequestMethodNotSupportedException`을 `HtmlErrors.notFound`(404 · text/html)로 이미 매핑한다(phase 70이 `DELETE /api/distribution-targets/:id`를 404로 동결하면서 도입했다). 따라서 POST 매핑을 붙여도 그 경로의 GET은 **404**이고 계약의 `x-distribution-tick-get` 관측이 성립한다. **`HtmlErrors`는 package-private이라 컨트롤러 레벨 '좁은 처리'는 애초에 불가능하다.** 처분: step9는 **실측으로 404를 확인만** 하고, 만약 405가 나오면 그것은 새 처리를 만들 신호가 아니라 **기존 매핑이 왜 안 걸렸는지를 규명해야 한다는 신호**다(핸들러 매핑·필터 순서·예외 타입 중 무엇이 달라졌는가). 39 라우트 전체의 405/415 정책은 미동결이므로 **건드리지 마라**(phase 68 forward_notes (13)).",

    "(21) **계층 배치(ADR-006·ADR-013 승계).** 컨트롤러는 (a) 세션 토큰 판독(쿠키 우선·`x-session-id` 폴백) (b) 게이트/서비스 호출 (c) shape 매핑만 한다. 서비스는 인가·검증·투영·시계를 소유하고 **서블릿 타입을 import하지 않는다**. **컨트롤러가 `ContentsRow`를 알면 안 된다**(`ControllerProjectionBoundaryTest`가 red를 낸다). 생성자 주입만. 인가는 `Authorization`에 capability 2개(`RUN_DISTRIBUTION_TICK`·`MANAGE_DISTRIBUTION_FAILURE` → `[Z]`)를 추가해 재사용하고 **role은 오직 세션에서 재도출한 값**만 쓴다(ADR-004).",

    "(22) **와이어 포맷 단일 지점 승계.** JSON 응답은 반드시 `JsonHttp` 한 지점으로만 쓴다 — Spring MVC 메시지 컨버터로 값을 반환하면 `Content-Type`의 세미콜론 뒤 공백 1바이트가 사라져 전 관측이 diff가 된다.",

    "(23) **SSE 변경 신호는 이식하지 않는다(phase 69 decisions (17)·71a decisions (13) 승계).** Node는 tick 성공(배부 1건 이상)·retry 성공에서 `app.notifyChange('status')`를 부른다. 계약 2파일이 스트림을 관측하지 않고 Spring에는 SSE가 없다. **seam(빈 인터페이스·dead config)도 만들지 마라** — 발행 지점을 정확한 조건과 함께 forward_notes에 기록한다.",

    "(24) **기준값은 계획서가 아니라 Node 리포트 실측에서 읽는다.** 각 step의 첫 작업은 리포 **밖** 임시 경로에 같은 파티션의 Node 대조 리포트를 뽑아 `status`·`bodyKeys`·`values`를 확인하는 것이다. 계획서의 수치·문자열을 그대로 믿고 구현하지 마라.",

    "(25) **DB 비파괴(절대 규칙).** ① **DDL 0** ② **행 삭제 0**(이 phase는 `DELETE FROM`을 하나도 추가하지 않는다) ③ **이력은 append-only**(`UPDATE ArticleHistory`·`DELETE FROM ArticleHistory` 금지 — 해소는 새 `distribute-retry` 행으로만 표현한다. `LEDGER_MUTATIONS` 스캔이 상수 펼침까지 본다) ④ Contents 갱신은 **present-only**(`distributedAt` 한 컬럼 / `status` 한 컬럼 — 본문·잠금·sentAt을 함께 쓰지 마라) ⑤ **리포 `news.db`·`uploads/` 무접촉**(Java 테스트는 `@TempDir` 임시 DB·임시 스풀만 쓴다) ⑥ **`RequiredSchema`를 넓히지 않는다**(필요한 컬럼이 phase 69·70에서 이미 등재돼 있다 — `Contents.distributedAt` · `ArticleHistory.targetId`·`reason` 포함)."
  ],

  "excluded": [
    "(a) **수집 2 라우트**(collection-receive·collection-pull)와 그 계약 3파일 · fail-closed 정책 · 파서 · 아웃바운드 어댑터 — **`phases/71-spring-collection` 소유**(이 phase의 전제이며 이미 머지돼 있어야 한다).",
    "(b) **하네스 프로파일 축·`failclosed` 신설·ADR-008 정적 게이트 설계** — 71a 소유. 이 phase는 상속하고 `SpoolWriter.java`로 예외 목록의 빈 자리를 채울 뿐이다.",
    "(c) **미디어·업로드·사진 4 라우트** — 후속 phase. `PathPolicyWireTest`의 스텁 금지 프로브(`GET /api/media/search`)를 **손대지 마라**.",
    "(d) **SSE 2 라우트**(stream·logs-stream) — 신호 발행 지점만 forward_notes로 인계한다(decisions (23)).",
    "(e) **articles-translate 1 라우트** — 후속 phase.",
    "(f) **실패 원장이 있는 상태의 계약 관측** — `distribution-tick.contract.js` 서문이 명시하듯 failures 목록의 비어 있지 않은 상태·retry 성공·409 4종·500 3종은 **API만으로 결정적으로 만들 수 없다**(수신처 CRUD가 잘못된 spoolDir를 애초에 거부한다). 서버·파일시스템 권한을 조작해 실패를 유도하지 마라(환경 의존이라 재현되지 않는다). 그 축은 **실패 이력을 리포지토리로 직접 시드한 Java 와이어/단위 테스트**가 소유한다.",
    "(g) **`--require-full-coverage`(Spring 대상)** — 이 phase 종료 시점 구현 라우트 **32**, 미구현 **7**(media-search·upload·photos-create·photos-search·articles-translate·stream·logs-stream). P1이 전부 끝난 뒤에만 켠다.",
    "(h) **계약 스위트를 `npm test`에 편입하는 것**.",
    "(i) **`docs/news.md`·`docs/api-contract/**`·계약 파일 수정** — 이 phase는 `server-spring/README.md`와 **ADR-013 ④의 실측 1문장**만 갱신한다.",
    "(j) **`RoutePolicy` 표 수정** — 3 라우트가 이미 `AuthClass.ADMIN`으로 등재돼 있다.",
    "(k) **39 라우트의 405/415 정책 변경** — 미동결 축이며 전 라우트의 판정을 함께 움직인다(decisions (20)).",
    "(l) **자동 복구 장치 일체** — 자동 재시도·지수 백오프·재시도 큐·데드레터·워커 스레드풀. ADR-008 (6)이 명시적으로 배제했고 71a의 정적 게이트가 기계로 막는다.",
    "(m) **분산 락·다중 인스턴스 조정** — tick의 single-flight는 **프로세스 내**뿐이다(decisions (14)).",
    "(n) **경로 정규화 정책 변경** — phase 69 forward_notes (8) 누적 항목이다(새 결함으로 보고하지 마라)."
  ],

  "open_questions": [
    "(a) **송고 훅(step6) 포함 여부 — ② 검토에서 '포함 유지'로 판정됐다.** 리뷰어가 계약 노출면을 직접 확인했다: 회귀 위험은 실질 0이고, 빼면 `ArticleEmbargoService`가 tick 한쪽에서만 결선되는 반쪽이 된다. **정확한 미관측 축은 '승격 후 status 반환' 하나다** — `distribution-tick.contract.js` 197행이 송고 응답의 `status === 'DES'`를 단언하지만 그 기사는 1차 엠바고만 설정돼 `kindsForSend → []`라 훅의 부수효과가 0이다. 즉 훅의 존재 자체는 계약이 보고, **훅이 승격 status를 반환에 반영하는지 여부만** 계약이 못 본다(decisions (8) — 그 축은 Java 테스트가 유일 방어선이다). step6의 1순위 AC(`--parity` 연속 2회 diffs 0)를 유지한다. **마감 결과(2026-08-26)**: 포함했다 — 신설은 순수 판정 `SendDistribution.kindsForSend` 하나이고 `ArticleLifecycleService` 증분은 생성자 + 훅 1자리이며 반환문 `finalStatus`는 무수정이다. 미관측 축은 step6에서 실증했다(반환 status를 승격 후 값으로 바꾸는 변이를 심어도 당시 236관측 `--parity`가 diffs 0으로 통과했고 `ArticleLifecycleServiceTest` 1건만 red였다 — 그 테스트가 유일 방어선이다). 마감 재측정에서도 265관측 diffs 0(연속 2회)이다.",
    "(b) **`GET /api/distribution/tick`의 404 — 판정 완료.** `GlobalErrorHandler`가 이미 405를 404로 떨구므로 **404가 성립**하고 새 특수처리는 만들지 않는다(decisions (20)). step9는 실측 확인만 한다. 405가 나오면 기존 매핑이 왜 안 걸렸는지를 규명한다. **마감 결과(2026-08-26)**: 실측으로 확인했다 — `GET /api/distribution/tick`은 **404 `text/html; charset=utf-8`**이고(리포트 관측 `x-distribution-tick-get` · `caseId=distribution-tick/method-not-exposed` · `jsonBody:false`) 새 특수처리는 **0줄**이다.",
    "(c) **문서 갱신 범위.** 기본 결정은 `server-spring/README.md`(구현 라우트 29→32 · 미구현 7 · env 표에 `DIST_SPOOL_DIR` · ADR-008 게이트의 예외 2파일이 모두 채워졌다는 사실) + **ADR-013 ④에 실측 1문장 추가**(결정 본문·phase 68·69·70 문장 무수정. 71a는 ADR을 고치지 않았으므로 이 문장 하나가 수집·배부 두 phase를 함께 기록한다). 대안: README만. **미판단 시 기본 결정.** **마감 결과(2026-08-26)**: 기본 결정을 그대로 채택했다 — `server-spring/README.md`(구현 라우트 29→**32** · 미구현 **7** · env 표의 `DIST_SPOOL_DIR` 행을 `app.distribution.spool-dir` 배선으로 교체 · ADR-008 예외 **2파일이 모두 찼다**는 사실과 변이 재확인 · 배부 축 미검증 목록 9항목 추가 · 프로파일 설명과 커버리지 수치 갱신)와 `docs/ADR.md` **ADR-013 ④ 1문장**(71a+72를 함께 기록)만 고쳤다. `git diff feat-0-mvp...HEAD --name-only` 전수 목록에 `server/**`·`src/**`·`test/**`·`web/**`·`client/**`·`contract/**`·`docs/api-contract/**`·`scripts/contract-run.mjs`·`scripts/contract-diff.mjs`·`package.json`·`pom.xml`이 **없다**(새 npm 의존성 0 · 새 Maven 의존성 0).",
    "(d) **`ArticleEmbargoService`의 이력 실패 처분.** Node는 이력 insert 실패를 **삼키고** 상태는 남긴다. 기본 결정은 **Node 동형**(삼킨다)이며 `TransactionTemplate`은 '두 문장 사이 커넥션 반납 방지' 목적으로만 쓴다. 대안: 트랜잭션 원자성(이력 실패 시 상태도 롤백) — 더 엄격하지만 Node와 갈린다. **step5의 Node 실측으로 확정한 뒤 그 결정을 테스트로 고정한다. 미판단 시 기본 결정.** **마감 결과(2026-08-26)**: Node 동형(삼킨다)으로 확정했다 — 이력 insert 실패는 기존 `ArticleHistoryRecorder`가 `RuntimeException`으로 삼키고 승격 status는 남는다. `TransactionTemplate`은 상태 UPDATE + 이력 insert 두 문장을 한 커넥션에 묶는 용도로만 쓴다(원자성 강제가 아니다).",
    "(e) **`SpoolWriter`의 같은 밀리초 재기록 처분.** 파일명이 `<articleId>_<compactStamp>.json`이라 시계가 고정되면 같은 이름이 되어 **덮어쓴다**. Node도 같다(실측으로 확인할 것). 기본 결정은 **Node 동형(덮어쓴다)**이며 그 사실을 테스트·주석에 남긴다. 대안: 충돌 시 접미사 추가(divergence). **미판단 시 기본 결정.** **마감 결과(2026-08-26)**: Node 동형(덮어쓴다)으로 확정했고 `SpoolWriterTest.twoWritesAtTheSameInstantOverwriteTheSameFile`이 그 사실을 고정한다(다른 밀리초면 파일 2개 — `twoWritesAtDifferentInstantsPublishTwoFiles`)."
  ],

  "forward_notes": "(1) **마감 실측 전문 — 2026-08-26. A의 9커맨드를 연속 2회 돌려 전부 exit 0이고 수치가 완전히 동일했다(flake 0). 이 수치가 다음 phase의 기준선이다 — 추정치를 섞지 마라.** ① Java `cd server-spring && JAVA_HOME=D:/agents/tools/jdk-21.0.12+8 ./mvnw -B verify` → **1022 tests / Failures 0 / Errors 0 / Skipped 0** BUILD SUCCESS(71a 마감 기준선 **772** 대비 **+250** · 구간 실측 step0 781 · step1 825 · step2 858 · step3 887 · step4 916 · 최종 1022 · 소요 3:28/3:01/3:12) · jar **35,738,248 B**(clean 빌드 2회 동일 · 71a 35,662,714 B 대비 +75,534 B). **jar 크기를 인용할 땐 clean 빌드 기준으로 하라** — 증분(비-clean) 빌드는 같은 소스에서 **35,738,095 B**(153 B 작다)가 나왔고 md5는 실행마다 다르다(byte-reproducible이 아니다). ② 무인자 `node scripts/spring-contract.mjs`(**리포 루트 cwd**) **exit 0 profiles=5** — default files=11 `cases=163 covered=24/39` · minimal files=3 `cases=53 covered=2/39` · auth-negative 1파일 `cases=4 covered=0/39` · failclosed 1파일 `cases=5 covered=0/39` · prod-cookie 1파일 `cases=3 covered=1/39`(인스턴스 기동 health ok 4.9~5.3초) ③ `--parity` **exit 0 265관측 diffs 0**(default **198** · minimal **55** · auth-negative 4 · failclosed 5 · prod-cookie 3 — 71a 236관측에서 배부 **29관측**이 늘었다) ④ `--dual-run` **exit 0 265관측 diffs 0**(두 패스가 서로 다른 프로세스·자원을 썼다는 로그 증거: `[default-a] pid=4760 port=17443 dataDir=.../spring-contract-default-a-9KCPg9/data spoolDir=.../spring-contract-default-a-9KCPg9/spool` 대 `[default-b] pid=22972 port=17194 dataDir=.../spring-contract-default-b-WOowAq/data spoolDir=.../spring-contract-default-b-WOowAq/spool` — 10 인스턴스 전부 pid·port·DATA_DIR이 달랐고 스풀이 주입되는 default·auth-negative는 DIST_SPOOL_DIR도 달랐다) ⑤ `npm test` **tests 1328 / pass 1328 / fail 0**(불변) ⑥ `npm run lint` exit 0 ⑦ `npm run build` exit 0 ⑧ `node scripts/contract-inventory-check.mjs --require-spec-paths` exit 0(**routes=39 methods=GET:16,POST:19,PUT:3,DELETE:1 spec-paths=39/39**) ⑨ **Node 대상** `npm run test:contract -- --require-full-coverage` exit 0(**profiles=5 cases=274 covered=39/39**) — 마지막 항목이 이 phase가 Node 서버·계약 스위트를 한 줄도 고치지 않았다는 최종 증거다. 구현 라우트 **29 → 32**(`HandlerInventoryTest.IMPLEMENTED_ROUTES` **32행** + Boot 기본 `/error`) · 미구현 **7**. **커버리지 함정 승계**: Spring 대상 `covered`는 **프로파일별**이라 배부 3라우트가 붙어도 default 단독은 **24/39**가 정상이다(27/39가 아니다 — 필수 태그가 minimal의 `distribution-disabled`에도 걸려 있다). 39/39는 **Node 전 프로파일 합산에서만** 나온다. **데이터 안전 단언 전건 통과**: 리포 `news.db` 606,208 B · mtime 2026-08-11 15:22 · md5 7247e9e0dfe5cc8cd040ebb1dc9fb967 **무변** · `uploads/` 32파일 6,068,792 B **무변** · 리포 안 스풀 파일·`.tmp`·리포트 **0** · `git status --porcelain` 미추적은 `.vscode/`(IDE 산출물) 하나뿐 · 하네스가 띄운 java 프로세스 **잔존 0**(남아 있는 java 1건은 15:01 기동한 IDE 언어서버로 무관함을 CreationDate로 확인) · 이 step이 OS 임시에 남긴 `spring-contract-*`/`contract-*` **0건**(리포트 대조용 `--keep` 산출물 12개는 확인 후 직접 삭제했다. 직전 step이 남긴 12개는 그대로 뒀고 그 안의 리포트도 비밀 스캔 0건) · **5개 리포트 전문에 64-hex 세션 토큰 0 · 시드 비밀번호(reporter123·desk123·admin123) 0 · 수집 토큰 리터럴 0 · 드라이브 문자로 시작하는 절대경로 0 · 스풀 파일명 0**. **인벤토리 대조(주장이 아니라 파일로)**: 5개 리포트의 routeId 합집합 36개에서 계약 전용 유사 라우트 4개(`x-articles-list-repeated-scalar-key`·`x-distribution-targets-delete`·`x-distribution-tick-get`·`x-undefined-route`)를 빼면 **정확히 32**이고 `IMPLEMENTED_ROUTES` 32행과 일치한다 = **구현했는데 계약이 관측하지 않는 라우트 0개**. `PathPolicyWireTest`의 스텁 금지 프로브(`GET /api/media/search`)는 여전히 미구현을 가리킨다(무접촉). (2) **다음 도메인 묶음 권장 순서와 남은 7 라우트** — ① 미디어·업로드·사진 **4**(`GET /api/media/search` · `POST /api/upload` · `POST /api/photos` · `GET /api/photos/search` — `contract/cases/.../media-upload.contract.js`) → ② SSE **2**(`GET /api/stream` · `GET /api/logs/stream` — `docs/api-contract/sse.md`·`sse-stream.contract.js`) → ③ 번역 **1**(`POST /api/articles/:id/translate`). **각 phase는 계획 단계에서 자기 계약 파일이 부르는 픽스처 라우트가 이미 구현돼 있는지 먼저 확인하라**(phase 69 forward_notes (2) 승계 — 이 phase는 그 조사로 '배부 2파일이 수집 라우트를 부르지 않는다'를 확인했고 그것이 71a/72 절단의 직접 근거였다). SSE는 **와이어 단일 지점 규율이 처음 흔들리는 축**이다(스트림 응답을 `JsonHttp` 한 지점으로 쓸 수 없다 — 그 예외를 어떻게 둘지가 그 phase의 첫 결정이고, 그 결정 없이 코드를 먼저 쓰면 Content-Type 바이트 seam이 조용히 갈린다). Spring 대상 `--require-full-coverage`는 세 묶음이 전부 끝난 뒤에만 켠다. (3) **SSE 신호 발행 지점(이식하지 않았다 — seam도 만들지 않았다. decisions (23))**. Node 실측 조건까지 그대로 적는다: ① `POST /api/distribution/tick` **성공 AND `distributed`가 1건 이상**일 때만 `app.notifyChange('status')`(`server/index.js` 749행 — 변경 0건이면 보내지 않는다) ② `POST /api/distribution/retry` **성공에만** `'status'`(787행 — 거부·실패에는 보내지 않는다: 재조회 낭비 + 실패 오신호 방지) ③ `POST /api/collection/receive`·`/pull` 성공에 `'create'`(71a 인계). 기사 도메인 지점(phase 69 forward_notes (15)①)도 함께 상기하라 — create·derive는 `'create'`, action은 `'status'`, update는 `'update'`, lock 계열은 `'lock'`이다. (4) **`Date.parse` 이식 범위와 divergence(decisions (7) 실행 확정)** — `NodeInstants.parseIsoMillis`가 덮는 것은 ① `Instant.parse` 가능한 Z 표기 ② 오프셋 표기 ③ `^\\d{4}-\\d{2}-\\d{2}$`(UTC 자정)뿐이고, **오프셋 없는 날짜-시각(`YYYY-MM-DDTHH:mm`)과 레거시 문자열은 null**이다(`ZoneId.systemDefault()`를 읽지 않는다). step1이 Node로 실측해 `Date.parse('2026-01-01T09:00')`가 **호스트 TZ에 따라 값이 달라짐**을 확인했다 — 이식하면 서버 TZ가 배부 판정에 들어와 결정성이 무너진다. 틀리는 방향은 **미도래 → 배부 안 함**(안전측)이고 그 사실은 **tick 응답의 `invalid` 배열로 표면화**된다. 함께 기억할 것: **`EPS` + 전량 배부 → `DPS` 승격은 허용된다** — 역행 가드를 `status=='EPS'`만으로 쓰면 그 승격까지 막혀 red다(step1 실측). (5) **수집 축 divergence는 71a가 소유한다 — 참조만 하고 다시 쓰지 마라**(`phases/71-spring-collection/index.json` forward_notes (5)(6): 리다이렉트 미추종 · connect 10초/request 30초 · 중복 `x-collection-token` 결합 · FTP 스풀 미이식 · 부트 경고 미이식 · 정수의 실수 표기). 잔여 위험 한 줄만 여기서 상기시킨다: **요청 단계 무한 대기는 Node와 동일하게 남는다**(`HttpRequest.timeout`은 응답 헤더까지만 덮는다 — 본문을 천천히 흘리는 소스는 Tomcat 워커를 계속 점유한다). 워커 고갈의 영향 범위는 이제 **32 라우트 전부**다. (6) **ADR-008 정적 게이트의 예외 목록 규율** — 예외는 **파일 단위 2개**이고 이 phase로 **두 자리가 모두 찼다**(`service/HttpApiSourceFetcher.java` = 71a 수집 egress · `service/SpoolWriter.java` = 이 phase 배부 스풀 게시). 이 phase는 `Adr008DisciplineTest`를 **0줄** 고쳤다(이름이 선등재돼 있었고, 매칭이 basename이 아니라 `src/main/java` **상대 경로 접미사**이며 '그 이름의 파일이 정확히 1개'까지 단언하므로 등재된 경로에 정확히 두어야 한다). 새 phase가 파일을 예외에 넣으려 하면 **그 자체가 아키텍처 결정**이며 `theExceptionListIsExactlyTwoFiles`가 그 사실을 diff에 드러낸다. **마감 시점 비공허성 재확인(2026-08-26)**: `DistributionTickService.java`에 한정 이름 `@org.springframework.scheduling.annotation.Scheduled`를 심자 `mainSourcesRunNoTimersOrRetries`가 **파일명과 정규식을 짚어 red**(Adr008DisciplineTest 11 tests / 1 failure), 원복 후 HEAD 블롭과 **byte-identical**을 md5로 확인했다. (7) **정적 게이트가 덮지 못하는 벡터와 실질 그물** — 문자열을 끊어 쓰기·리플렉션·리터럴 속 주석은 스캔을 통과한다(71a ④가 우회 12종을 심어 12/12 통과를 실증한 전례가 있다). **실질 방어는 행동 단언**이다: 스풀 **파일 개수**와 파일명 패턴 · 이력 **행 수**와 append-only(`UPDATE ArticleHistory`·`DELETE FROM` 0) · 응답 **키 집합**(tick 6키/재진입 7키 · failures 항목 10키 · retry 성공 5키) · **DB 행 수 감소 0**. 정적 스캔을 늘리기 전에 이 축부터 늘려라(phase 70 gap_found 계열). (8) **미검증(정직한 공백)** — ① **실패 원장이 비어 있지 않은 상태의 계약(HTTP) 관측이 없다**(excluded (f): 배부 대상 CRUD가 잘못된 `spoolDir`를 400으로 먼저 거부하므로 API만으로 결정적으로 만들 수 없다. 서버·파일시스템 권한 조작은 환경 의존이라 하지 않았다 — `DistributionWireTest`·`DistributionRetryServiceTest`가 소유) ② **tick 재진입 스킵 7키**(`skipped:'in-progress'`)는 계약이 관측하지 못한다(직렬 실행 — `DistributionTickServiceTest.aReentrantCallIsSkippedWithoutScanning`과 `DistributionSeamWireTest`가 유일 방어선) ③ **다중 인스턴스 중복 tick**(single-flight는 프로세스 내뿐이고 분산 락은 금지 — '외부 cron 단일 트리거' 운영 규율 소유) ④ **스풀 파일을 외부 전송기가 실제로 집어가는 경로 전체** ⑤ **원자 이동의 원자성 자체는 단위 테스트로 증명 불가** — 잠근 것은 **구현 형태**뿐이다(강등이 아니라 seam이 실재한다: `SpoolWriterTest.theAtomicPublishSequenceIsTempWriteThenAtomicMove`가 `SpoolWriter.SpoolFs` seam으로 `.tmp write → ATOMIC_MOVE` **호출 순서**를 단언한다) ⑥ **엠바고 과거 시각·파싱 불가의 계약 관측**(픽스처가 전부 미래 시각 — phase 69 forward_notes (4)⑨ 승계 · `EmbargoPolicyTest`·`NodeInstantsTest`가 덮는다) ⑦ **두 서버가 같은 `news.db`를 동시에** 여는 상황(하네스는 프로파일마다 DB를 분리한다) ⑧ **비-loopback 바인딩의 실제 원격 접근**(`failclosed`는 0.0.0.0으로 바인드하지만 접속은 언제나 127.0.0.1이다) ⑨ **송고 응답의 status = `finalStatus`**는 계약이 못 보는 축이다(step6에서 승격 후 값으로 바꾸는 변이를 심어도 당시 236관측이 diffs 0으로 통과했고 `ArticleLifecycleServiceTest` 1건만 red였다 — **그 테스트가 유일 방어선**) ⑩ single-flight 플래그의 `finally` 해제는 **`RuntimeException` 가드를 뚫는 `Error` 이탈로만** 관측 가능하다(`theSingleFlightFlagIsReleasedEvenWhenTheScanEscapes`). (9) **누적 이월 — 새 결함으로 보고하지 마라**(68~70이 근거와 함께 폐색했다): 인코딩·경로 파라미터 divergence(69 (8)) · 잠금 획득의 사용자 미대조(69 (12)) · `UserRepository` 바인딩 문자열화(69 (5)(b)) · `CsrfOriginFilter` 공백 Origin(68 (22)(c)) · Boot `/error`의 405·415 shape(68 (13)) · 로그 링 버퍼 내용 차이와 **이력 실패 경고 문구 차이**(69 (16)(g) — 다이제스트 본문을 동결하는 phase가 소유) · `RateLimit-*` 헤더 범위(68 (15)) · **`DistributionTargetService.checkName`의 `String.trim()`**(phase 70 산출물 — JS `trim`은 NBSP·BOM까지 지우므로 `NodeString.trim`으로 옮겨야 할 자리다. 이 phase는 배부 대상 CRUD 무접촉 규율 때문에 고치지 않았고 **배부 대상 CRUD를 다시 여는 phase가 소유**한다). (10) **스캔 상한 3종의 구분(통일하면 결함이 된다 — decisions (16) · 값은 Node 실측)**: ① **표시용 목록 창** `DEFAULT_LIST_LIMIT=200` / `MAX_LIST_LIMIT=1000`(`DistributionRetryService`) ② **재전송 게이트의 미해소 조회** `RETRY_SCAN_LIMIT=1_000_000`(articleId 스코프 · 사실상 무제한 — 표시용 창을 쓰면 오래된 실패가 `no-failure`로 오거부되어 **복구 불가**가 된다. 잠금 테스트 `DistributionRetryServiceTest.theRetryScanIsNotLimitedToTheDisplayWindow`) ③ **중복 억제 판정의 이력 조회** `FAILURE_DEDUP_SCAN_LIMIT=1_000_000`(`DistributionService` — 창 밖으로 밀린 최신 실패를 놓치면 억제가 **무의미**해진다. 잠금 테스트 `DistributionServiceTest.theDedupScanIsNotLimitedToTheDisplayWindow`). 여기에 **모델 기본 창 500**(`ArticleHistoryRepository.queryDistributionEvents`의 기본값)이 하나 더 있다 — 넷을 한 상수로 합치지 마라. (11) **구현 인계(각 step 실측)** — ① `DistributionConfig`가 배부의 **합성 루트**이고 **활성 판정 지점은 `SpoolProperties.rootPath()` 하나**다(`SpoolWriter`·`DistributionService`는 스풀 루트가 있을 때만 만들고 소비자는 전부 `ObjectProvider.getIfAvailable()`; `ArticleEmbargoService`·tick·retry 서비스는 **항상** 만든다). 판정이 두 곳으로 갈리면 tick과 retry의 503 조건이 어긋난다 ② 테스트 지연 seam은 **테스트 소스**의 `GatedSpoolWriter`이고 main에는 `Thread.sleep`이 **0**이다 · `spring.main.allow-bean-definition-overriding`은 `DistributionSeamWireTest` **전용**이다(다른 테스트로 번지게 하지 마라) ③ tick·retry 서비스는 **투영까지 소유**한다(반환 맵이 그대로 응답이라 화이트리스트가 서비스 안에서 닫힌다). 반환을 record가 아니라 `Map` 투영으로 둔 이유는 **record면 `reason:null`이 6번째 키로 새어 나가기 때문**이다(계약이 못 보는 축) ④ `DistributionFailureLog`의 그룹 키는 문자열 연결이 아니라 `record GroupKey` 3원소이고 `targetId`는 **double**이다(Node의 문자열 키 quirk를 이식하지 않았다 — 방향은 과다 보고=안전측) · **`integerId` 술어가 3곳에 흩어져 있다**(한 곳을 고치면 셋을 함께 보라) ⑤ `LowercaseHexEscapes`(스풀 JSON 제어문자 이스케이프)는 71a `CollectionMarkup`의 규칙과 **같은데 그쪽이 private이라 공용화하지 않았다 — 통합 후보** ⑥ `SpoolProperties`는 **공백만 있는 값을 미설정으로 수렴**시킨다(Node truthy 판정과 divergence · 계약 미관측 · 방향은 안전측) ⑦ 1·2차 엠바고가 모두 설정된 기사의 required는 `[press,nonpress]`라 **nonpress 하나로는 EPS 그대로이고 쓰기 0건**이다(원 계획서의 'EPS + nonpress → DPS'는 실측상 틀렸다) ⑧ 같은 밀리초 재기록은 **덮어쓴다**(Node 동형 · `twoWritesAtTheSameInstantOverwriteTheSameFile`). (12) **환경 함정(전 step 재발)** — IDE(Eclipse/JDT)가 `server-spring/target/`에 **JDK 25 클래스**를 남기면 `mvnw -B verify`가 `Tests run: 0` + BUILD FAILURE로 **즉사**한다. 소스 문제가 아니라 **`clean verify` 1회**로 리셋한다(`target/`은 git 무추적). `?? .vscode/`도 IDE 생성물이니 `git add` 하지 마라. (13) **진단 위생(이 step 발견)** — **Java 테스트가 실패하면 assertion 메시지에 임시 인스턴스의 64-hex 세션 토큰이 그대로 실린다**(이 phase 앞 step들의 로컬 로그 2건에서 실측 · 리포 밖 · `@TempDir` 임시 DB의 일회용 토큰이라 실해는 없다). **계약 리포트는 마스킹돼 있어 그대로 공유해도 되지만 surefire 로그는 그렇지 않다** — 붙여 넣기 전에 훑어라.",

  "steps": [
    { "step": 0, "name": "history-distribution-queries", "status": "completed",
      "summary": "`ArticleHistoryRepository`에 읽기 전용 배부 이벤트 조회 2개 추가(`queryDistributionEvents(String,Integer)`·`getDistributionEventById(long)`) — 8컬럼 `LinkedHashMap`(id·articleId·eventType·action·targetId·reason·actorUserId·createdAt) · 어휘 필터 `eventType IN ('distribute-failed','distribute-retry')`를 **두 조회 모두**에 · `ORDER BY id DESC LIMIT ?`(바인딩) · 기본 창 **500**(Node `DISTRIBUTION_EVENTS_DEFAULT_LIMIT` 실측, 정수 ≥1이 아니면 기본값 — 서비스 클램프는 모델에 없다) · `articleId`가 null이면 조건 미부착(전 기사) · `targetId`는 `rs.getLong`+`wasNull`로 **Long 또는 null**(`DistributionTarget.id` 매핑과 같은 표현이라 equals가 성립 — `readRow` 단일 지점에 분기) · 값이 NULL이어도 키 보존. `queryByArticle` 9키·`LIST_COLUMNS` 무변경. Java 테스트 +9(772→781, 리포지토리 클래스 19→28) — 변이 4종 red 실증(단건 어휘 필터 제거·목록에 targetId 누출·`ORDER BY id ASC`·targetId를 String으로 판독). DDL 0 · `DELETE`/`UPDATE` 0(append-only, `NoSchemaSqlInMainSourcesTest` green) · 서비스/컨트롤러 무접촉 · HTTP 표면 무변경(`--parity` 236관측 diffs 0)." },
    { "step": 1, "name": "embargo-policy", "status": "completed",
      "summary": "순수 모듈 2개 신설 — `NodeInstants`(`parseIsoMillis(Object)`: Z표기는 `Iso8601.parseMillis` 재사용 · 오프셋 표기 · `^\\d{4}-\\d{2}-\\d{2}$` 날짜만 → UTC 자정. 그 밖은 null이고 `ZoneId.systemDefault()`를 읽지 않는다 — Node 실측으로 `Date.parse('2026-01-01T09:00')`가 호스트 TZ(+09:00)에 따라 값이 달라짐을 확인, decisions (7)의 divergence를 테스트로 못 박았다) · `EmbargoPolicy`(`src/services/embargoPolicy.js` 207행 1:1 — 상수 2종 `List.of` 불변 · 컬럼은 `Function<String,Object>` 접근자로 받아 `ContentsRow` 미import · `Clock` 필드 0 · `now`는 `Object` 인자 · `status`/`contents`/`rows`/`distributed` 전부 null 허용, `Set.of(...).contains(null)` NPE를 게이트마다 선행 null 검사로 폐색). Node 실측 60여 관측을 표로 옮겨 테스트 43개(`EmbargoPolicyTest` 33 · `NodeInstantsTest` 10) + `ArticleLifecycleServiceTest`에 D-11 등가성 1개. **D-11은 `applyAction`의 반환 status(`finalStatus`)로 단언한다**(되읽기 금지 — step6 송고 훅이 붙으면 `secondEmbargoAt`만 설정된 조합의 저장 status가 EPS로 승격되어 red가 되고, 그 완화가 잘못된 복구다). 4×4 그리드 16칸 전수 = DES 12 / DPS 4로 비공허성 고정. 변이 red 실증 6종: (a) now 파싱실패 폴백을 '전부 도래'로 → `anUnreadableNowDistributesNothing` (b) 순진한 `id > boundary` → `aDistributeRowWithNoIdCountsAsPartOfTheCurrentCycle` (c) 역행금지 삭제 → `anEpsArticleNeverFallsBackToDes` (d) `<=`→`<` → `theDueComparisonIncludesTheExactInstant` (e) `ZoneId.systemDefault()` 폴백 → `dateTimeWithoutAnOffsetIsNull...` 외 3 (f) `embargoSet`을 embargoAt만 보도록 축소 → D-11 그리드(f' trim 변이는 **D-11만** red — 기존 27테스트는 전원 통과, 새 단언이 중복이 아님을 실증). **main은 `ArticleLifecycleService` 0줄 변경**(변이 후 Edit 원복 · `git diff` 공백 확인). Java 781→**825**(+44) · `--parity` exit 0 · 5프로파일 236관측 diffs 0(불변) · `ClockDisciplineTest`·`Adr008DisciplineTest` green · DB·HTTP·파일시스템 무접촉." },
    { "step": 2, "name": "failure-ledger", "status": "completed",
      "summary": "순수 모듈 `DistributionFailureLog` 신설(`src/services/distributionFailureLog.js` 106행 1:1, `harness.news.service`) — 상수 3종(`RETRYABLE_FAILURE_REASONS`는 `List.of` 불변 **정확히 3개**: spool-write-failed·invalid-spool-dir·invalid-article-id) · 그룹 키는 `record GroupKey(articleId,targetId,kind)` **3원소**(문자열 연결을 하지 않아 Node의 NUL 구분자 충돌이 원천적으로 없다) · 그룹의 **최대 id 행**이 failed면 미해소·retry면 해소 · 참여 불가 행(배부 어휘 밖·비정수 id·비문자열 action·정규화 불가 targetId·목록 안 null)은 **조용히 무시**(throw 0) · 반환 `record Failure`는 **정확히 6키**(경로성 필드 0)이고 historyId DESC 불변 목록 · `findUnresolvedFailure`는 판정을 복제하지 않고 `unresolvedFailures`의 첫 매치를 돌려준다(kind 인자 없음 — ADR-004). targetId 판독은 **`NodeNumber.toNumber` 단일 출처**(decisions (18)): 빈 문자열 검사는 **trim 이전**이라 Node 실측대로 `' '`는 수신처 0으로 참여하고, `-0`은 0으로 접으며, `3`/`\"3\"`/`3.0`/`\" 3 \"`/`\"0x3\"`가 한 그룹·`\"3.5\"`는 별도 그룹이다. `integerId`(= `Number.isInteger`)는 `EmbargoPolicy`의 동명 private 술어·문자열 도메인의 `NodeNumber.integerOf`와 부를 수 없어 지역 구현하고 그 사실을 javadoc에 명시했다. Node 정본 실측 40여 관측을 표로 옮겨 테스트 **33개**. 변이 red 실증 5종: (a) 그룹 키에서 kind 제거 → 3원소 테스트 2개 red (b) `Integer.parseInt` 대체 → 6개 red(`\" 3 \"`·`\"0x3\"`·`\"3.5\"`·`\"abc\"`, NumberFormatException으로 'throw 금지' 위반까지 표면화) (c) `status-changed` 추가 → 재전송 사유 테스트 2개 red (d) 서문이 기각한 휴리스틱(순서 무시 해소) → `aFailureRecordedAfterTheRetryReappears` red(신선한 미발송의 무음 소실 재현) (e) `spoolDir` 7번째 컴포넌트 → 6키 단언 red. Java 825→**858**(+33) · `--parity` exit 0 · 5프로파일 236관측 diffs 0(불변) · npm 1328 · DB·HTTP·시계·파일시스템 무접촉(DDL 0 · SQL 0). **step9 인계**: `Failure.targetId`는 Node number와 같은 `double`이라 `DistributionTargetRepository.findById(double)`에 그대로 넘어가지만, HTTP 매핑에서는 정수를 소수점 없이 실어야 한다(Jackson 기본은 `3.0`, Node는 `3`). **환경**: 변이 실험 뒤 `mvnw -B verify`가 `Unresolved compilation problem`(IDE 잔여 클래스)로 실패 — baseline이 예고한 대로 `clean verify` 1회로 리셋했다." },
    { "step": 3, "name": "spool-writer", "status": "completed",
      "summary": "파일 어댑터 `SpoolWriter`(`harness/news/service/SpoolWriter.java` — 71a가 예약해 둔 ADR-008 4군 예외 자리에 **정확히** 채웠다. `Adr008DisciplineTest`는 **0줄 변경**) + 설정 `SpoolProperties`(`@ConfigurationProperties(\"app.distribution\")` record 1필드 · `AppConfig`에 항목 1개 · `application.properties`에 `app.distribution.spool-dir=${DIST_SPOOL_DIR:}` 1줄. **`AppProperties`·그 9 호출부 무접촉**). 활성 판정은 `rootPath()`/`enabled()` **단일 지점**이고 이 클래스는 경로 문자열만 다룬다(파일시스템 접근 0 — 게이트 4군 예외가 아니다). `write(spoolDir,articleId,article,contents)`는 ① 루트 없음→`spool-disabled` ② `SpoolDir.sanitizeSpoolDir` **재검증**(복제 0)→`invalid-spool-dir` ③ `^[A-Za-z0-9_-]{1,64}$`→`invalid-article-id` ④ 실패 전부 `spool-write-failed`이고 **throw 0**(`IOException|RuntimeException` 포획, 사유에 경로·예외 메시지 0). 게시는 같은 디렉토리 `.<name>.tmp` → `Files.move(...,ATOMIC_MOVE)`이며 **폴백 없음**, 파일 연산 3종은 중첩 seam `SpoolFs`(구현체도 같은 파일 — 분리하면 예외가 3개가 된다)로 주입 가능하다. payload는 **20키 allowlist**(Contents 18 → `markupVersion` → `articleId` 덮어쓰기(자리 유지) → `title` 폴백 → `distributedAt`)이고 `LinkedHashMap`으로 키 순서까지 재현하며 null 값은 **키가 빠진다**. Node 실측 반영 2건: (i) `title` 폴백 조건이 `!== undefined`라 Article 컬럼이 SQL NULL이면 `\"title\":null`이 실린다 → `containsKey`로 판정(`Map.get`만 보면 키가 소실) (ii) 같은 밀리초 재기록은 파일명이 같아 **덮어쓴다**(Node 동형 — open_questions (e) 기본 결정 확정). **바이트 동일 대조**: Node 산출 payload(634B/188B)를 Java 리터럴로 옮겨 `byte[]` 비교. 기본 Jackson은 `\\u000B`(대문자)를 써 Node의 `\\u000b`와 index 51에서 갈렸고, `CollectionMarkup`과 같은 소문자 `CharacterEscapes`(9자)를 **전용 매퍼**에 걸어 폐색했다(전역 매퍼·`JsonHttp` 무접촉). 변이 red 실증 6종: (a) allowlist에 `internalComment` → 3개 red (b) null 키 보존 → 8개 red (c) 재검증 제거 → 실물 FS 테스트가 `../outside` 게시 성공을 포착 (d) 최종 경로 직접 쓰기 → **호출 순서 테스트만** red(파일 결과 기반 테스트는 전원 green — seam이 필요한 이유의 실증) (e) 인코딩 → **`Charset.defaultCharset()`는 JDK18+ UTF-8이라 green**, 실제 divergence는 `native.encoding`(MS949)에서 6개 red (f) 전용 매퍼 제거 → 제어문자 바이트 테스트 red. Java 858→**887**(+29) · `--parity` exit 0 · 236관측(176/48/4/5/3) diffs 0 불변 · npm 1328 · DB DDL/DML 0 · 리포에 스풀 파일 0." },
    { "step": 4, "name": "distribution-service", "status": "completed",
      "summary": "배부 실행 서비스 `DistributionService` 신설(`src/services/distributionService.js` 244행 1:1 · `harness.news.service`). 한 호출은 내부 `Call` 인스턴스가 소유해 **억제 컨텍스트가 호출 사이에 남지 않는 것이 구조로 보장**된다(lazy 1회 조회 — 성공만 있으면 원장을 읽지 않는다). 실행 순서는 배경 의사코드 그대로: writer 없음 → `spool-disabled` · `wanted = KINDS ∩ kinds`(상수 순서·중복 접힘·`null` 원소 무해, 비면 **기사 조회조차 안 함**) · 기사/공통정보 없음 → `not-found` · kind마다 `{kind, active:'Y'}` 활성 수신처 순차 처리 · **매 쓰기 직전 TOCTOU 재조회**(`ArticleRepository.findStatus` — 본문 blob을 N번 다시 읽지 않는 경량 조회이고 판정은 Node `getById` 재조회와 동치. 페이로드는 **최초 스냅샷 고정**) · 가드에 걸리면 `i==0`은 `targetId:null` 항목, `i>0`은 남은 수신처 전부를 `status-changed`로 남기고 **남은 kind까지 중단**(tick의 touched 판정 때문에 failed에는 반드시 남긴다) · **`okInKind > 0`일 때만** `distribute` 이력 1행 · `distributed` 1건 이상이면 `distributedAt` **present-only 한 컬럼** 갱신. 실패 영속 조건은 단 하나(`targetId != null` AND `DistributionFailureLog.isRetryableFailureReason`) — `status-changed`·kind 단위 항목은 원장에 남기지 않는다. 중복 억제는 `unresolvedFailures` + `EmbargoPolicy.latestSendId`만 재사용하고(새 규칙 0) **경계 이전 행과의 일치는 중복이 아니다**(그 행만 남기면 재전송이 영구 `stale-cycle`이라 복구 경로가 0). 억제 스캔 상한은 Node 30행 `FAILURE_DEDUP_SCAN_LIMIT` **실측 1,000,000**이며 표시용 창(`DEFAULT_LIST_LIMIT=200`/`MAX_LIST_LIMIT=1000`)·모델 기본 창 500과 **합치지 않았다**(decisions (16)). 통지 어휘는 둘로 분리(`DistributionFailureListener` 4키 — 경로 0 / 이력 실패는 `ArticleHistoryRecorder.HistoryErrorListener`)이고 통지·`failed` 반환은 기록 생략과 무관하게 매번 일어난다. **`TransactionTemplate` 0**(decisions (19) — 이 서비스의 문장은 각자 독립된 사실이고, 호출 전체를 묶으면 부분 성공의 사실 기록이 롤백돼 다음 tick이 중복 배부한다). 빈 배선·`@Service`는 소비자 step(7·8·9) 몫이라 이 step에는 없다. Node 정본 실측 10 시나리오(가짜 모델·가짜 writer로 `createDistributionService` 직접 호출)를 표로 떠 테스트 **29개**(`DistributionServiceTest` — `@TempDir` 임시 DB + 임시 스풀). 변이 red 실증 7종: (a) TOCTOU 재조회 제거 → 6번 red(KILL 기사 3건이 실제로 스풀에 나갔다) (b) 이력 조건 무조건화 → 5번 red (c) 사이클 경계 무시 → 10번 **단독** red (d) `status-changed` 영속 → 8번 **단독** red (e) 호출 전체 트랜잭션(부분 실패 시 사실 기록 롤백) → 4번 red(12개 동반 red) (f) `active` 필터 제거 → 3번 red (g) 억제 상한을 표시용 창 200으로 → 16번 **단독** red. 원복 후 sha256 동일성 확인. Java 887→**916**(+29) · `--parity` exit 0 · 5프로파일 236관측(176/48/4/5/3) diffs 0 불변 · npm 1328 pass/0 fail · `Adr008DisciplineTest`(11)·`ClockDisciplineTest`(4)·`NoSchemaSqlInMainSourcesTest`(8)·`HandlerInventoryTest`(3) green · SQL 0(DDL·`DELETE`·`UPDATE` 직접 문장 0) · 리포 `news.db`·`uploads/` 무접촉 · 리포에 스풀 파일 0 · 컨트롤러/라우트/scope 표 무접촉." },
    { "step": 5, "name": "embargo-status-service", "status": "completed",
      "summary": "엠바고 상태 반영 서비스 `ArticleEmbargoService` 신설(`src/services/articleService.js` 277~300행 1:1 · `harness.news.service`). `syncEmbargoStatus(articleId, extraKinds, actorUserId)` → `record Result(ok,status,reason)`: 없는 기사면 `not-found`(예외 0) · 승격 판정 입력은 **`cycleDistributedKinds`(이번 사이클) ∪ `extraKinds`**(합집합 — 이력만 보면 `DistributionService`가 삼킨 기록 때문에 승격이 누락되고, 힌트만 보면 tick의 self-heal이 무력해진다) · 허용 범위는 `EmbargoPolicy.embargoStatusFor` **한 곳**이 좁힌다(전이표 미사용 — role도 action도 없다) · `next == null`이면 **쓰기 0건·이력 0행** · 승격은 `status` **한 컬럼**(present-only, `distributedAt`은 `DistributionService` 단일 책임) + `eventType='status'`·`action='embargo'` 이력 1행이며 **한 `TransactionTemplate` 경계** 안이다. **open_questions (d) 확정 = Node 동형(삼킨다)**: Node 실측 10번이 이력 insert throw에도 `update` 1회·저장 status 승격·`onHistoryError` 1건을 남겼고, Spring도 `ArticleHistoryRecorder`가 삼켜 트랜잭션이 커밋된다 — 트랜잭션은 '두 문장 사이 커넥션 반납 방지' 목적뿐이며 그 사실을 커넥션 홀더 동일성(`TransactionSynchronizationManager.getResource`) 테스트가 고정한다. **계획서 문구 1건이 실측과 갈렸다**: 작업 A의 'EPS + nonpress → DPS'는 틀렸다 — 1+2차 기사의 required가 `[press,nonpress]`라 nonpress 하나로는 완결되지 않고 **EPS 그대로·쓰기 0건**이며(Node 실측 3번), DPS가 되려면 이번 사이클에 두 kind가 모두 있어야 한다(실측 A·B번). 테스트는 그 실측대로 고정했다. Node 정본 실측 27 시나리오(가짜 모델로 원본 `syncEmbargoStatus` 직접 호출)를 표로 떠 테스트 **22개**(`@TempDir` 임시 DB + 실제 리포지토리 · 행 삭제 0 — 상태 격자는 기사 id를 달리해 만든다). 변이 red 실증 5종: (a) `cycleDistributedKinds`→`distributedKinds` → `pastCycleDistributionsDoNotCompleteAReEmbargoedArticle` **단독** red(거짓 완결 재현) (b) `action='send'` → 3건 red이고 그중 `theHintIsUnionedWithTheCycleHistory`가 **DPS 기대에 EPS**로 실패 — 승격 행이 새 사이클 경계(`latestSendId`)가 되어 직전 사이클의 press 배부가 판정에서 사라지는 **오염 파급**이 실제로 재현됐다(어휘 하나가 다음 tick의 완결 판정을 무너뜨린다) (c) 업데이트에 `distributedAt` 동반 → `thePromotionWritesOnlyTheStatusColumn` 단독 red (d) `next == null`에도 이력 기록 → 8건 red (e) `TransactionTemplate` 제거 → `theStatusWriteAndTheLedgerRowShareOneTransaction` 단독 red. 추가로 (f) `ArticleHistoryRecorder`를 재던지게 하는 **대안 설계(트랜잭션 원자성)** 변이를 심어 `aFailedLedgerInsertKeepsThePromotedStatus`가 error가 되는 것을 확인해 Node 동형 결정의 비공허성을 실증하고 원복했다(`git diff` 공백 확인 — main 기존 파일 0줄 변경). `@Service`는 붙이지 않았다(빈 배선은 소비자 step6·7 몫 — `DistributionService`·`CollectionService` 선례). Java 916→**938**(+22) · `--parity` exit 0 · 5프로파일 236관측(176/48/4/5/3) diffs 0 불변 · npm 1328 pass/0 fail · `Adr008DisciplineTest`(11)·`ClockDisciplineTest`(4)·`NoSchemaSqlInMainSourcesTest`(8)·`ControllerProjectionBoundaryTest`(4)·`HandlerInventoryTest`(3) green · SQL·DDL 0 · `DELETE FROM` 0 · `ArticleLifecycleService` 무접촉 · 리포 `news.db`·`uploads/` 무접촉 · HTTP 표면 무변경." },
    { "step": 6, "name": "send-distribution-hook", "status": "completed",
      "summary": "송고 즉시 배부 훅 결선(ADR-008 (4)) — 신설은 순수 판정 `SendDistribution.kindsForSend(status, contents접근자, already)`(`src/services/articleService.js` 86~94행 1:1 · `harness.news.service`) 하나이고 `ArticleLifecycleService`의 증분은 **생성자 + 훅 한 자리**뿐이다(전이·end marker·`embargoAware`·present-only update·이력 블록 0줄 변경 · **반환문 `finalStatus` 무수정**). 판정표는 Node 실측(2026-08-26 · `node -e`로 원본 27건) 그대로: `DPS`+미설정 → `[press,nonpress]`(already 무시) · `DPS`+설정 → **already와의 교집합**(정정본) · `DES`+1차 미설정&2차 설정 → `[press]` · 그 밖 `[]` · 반환 순서는 언제나 `press`→`nonpress`. 엠바고 설정 판정의 단일 출처는 `EmbargoPolicy.requiredKinds`이고 `DES` 행의 두 컬럼 판정도 그 결과(`press 없음 && nonpress 있음`)로 표현했다 — falsy 의미론(빈 문자열·NULL=미설정, **공백만=설정**)을 두 벌로 만들지 않기 위함이다(4×4 그리드 2개로 등가 고정). **시각 비교 0**(과거·미래·파싱 불가 값의 판정이 같음을 테스트가 잠근다). 훅 결선: 생성자에 `ArticleHistoryRepository` + **선택 의존 2개**(`ObjectProvider<DistributionService>`·`ObjectProvider<ArticleEmbargoService>` — 빈이 없으면 훅 자체가 없다) · 위치는 **이력 기록 뒤**(사이클 경계 `latestSendId` 불변식) · `already`는 `EmbargoPolicy.distributedKinds`(전체 이력)이고 조회 실패·미주입은 `[]` 폴백 · 승격은 `res.ok && !res.distributed.isEmpty()`일 때만 `syncEmbargoStatus(articleId, doneKinds(중복 제거·빈 값 제외), actor)` · **블록 전체 `RuntimeException` 격리**(경고 로그 1줄, payload 0). **`AppConfig` 무접촉이라 지금은 앱 런타임에서 훅이 잠들어 있다**(`DistributionService`·`ArticleEmbargoService`는 아직 빈이 아니다 — 빈 등록은 소비자 step9 몫). **step9가 그 빈을 올리는 순간 default 프로파일이 실배부를 타므로 이 훅의 첫 실전 관측은 step9의 `--parity`다.** **decisions (8) 실증**: 변이 (a)(반환을 배부 후 status로) → `theResponseStatusIsTheOneBeforeDistributionEvenWhenTheHookPromotesTheRow` **단독** red인데 같은 변이에서 **`--parity`는 5프로파일 236관측 diffs 0으로 통과**했다 — open_questions (a)가 예측한 '계약이 못 보는 유일한 축'을 실측으로 확인했고 방어선은 이 Java 테스트 하나뿐이다. **A-2 계약 노출면 조사(결론: 창이 겹치지 않는다)**: default 프로파일의 송고는 `articles-read.contract.js` 1건 + `articles-write.contract.js`(`createSentArticle`) 1건 **총 2건**이고 둘 다 무엠바고이며, 활성 수신처를 만드는 파일은 `distribution-targets.contract.js` 하나인데 러너의 알파벳 순서상 **두 송고보다 뒤**다. 그 뒤 4파일(health·receiver-config·session-guard·users)에는 `/action` 호출이 0이다 → default에서 훅의 부수효과는 Node에서도 Spring에서도 0(수신처가 없어 파일·이력·`distributedAt` 전부 무변)이라 두 대상이 같은 것을 관측한다. 엠바고 송고 3변형과 그 `readStatus` 단언은 `minimal/transitions.contract.js`(스풀 미설정)에만 있어 훅이 결선되지 않는다. **D-11 그리드 보호**: 공용 `setUp()`은 배부 서비스 **미주입**(`provider(null)` 2개)·활성 수신처 0을 유지했고 훅 테스트는 자기 테스트 안에서 `hooked()`로 별도 인스턴스를 만든다(스풀은 별도 `@TempDir spoolRoot`). `desEntryAgreesWithEmbargoPolicyAcrossTheFourByFourColumnGrid`는 **한 글자도 고치지 않았고** 단독 실행 green(1/1)이며, 변이 (a)에서도 유일 실패가 D-3이라 그리드가 훅과 무관함(반환 status 단언)이 실증됐다. 테스트 **+23**: `SendDistributionTest` 10(표 전건·DPS 4×4·DES 4×4·already 이상값·순서·시각 무관·불변) + `ArticleLifecycleServiceTest` 28→41(미설정 전량 2파일+`distributedAt`+이력 2행 · 1차 엠바고 0파일 · **2차만 설정 → press 1파일 + DB EPS인데 반환 DES** · DPS 재송고 이력없음 0파일 · 이력있음 press만 1파일 · 전 수신처 실패 → 송고 ok·승격 없음·`distribute-failed` 1행 · 예외 격리 · 이력 조회 실패 폴백 2종(엠바고 기사는 막고 무엠바고 기사는 배부) · `status/send` id < `distribute` id · R의 송고 0파일 · send 아닌 3액션 0파일 · 미주입이면 기존과 동일). **변이 red 실증**: (a) 위 참조 · (b) `res.ok`만으로 승격 → `promotionNeedsAnActualDistributionNotJustAnOkResult` 단독 red · (d) 훅을 이력 기록 앞으로 → `theSendHistoryRowIsOlderThanTheDistributionItTriggered` 단독 red · (e) try/catch 제거 → `anExceptionFromTheDistributionServiceDoesNotUndoTheSend` error. **계획서와 갈린 실측 2건**: ① **변이 (c)는 원리적으로 관측 불가다** — `already`를 `EmbargoPolicy.cycleDistributedKinds(finalStatus, …)`로 바꿔도 **41/41 green**이다. 훅이 도달 가능한 `finalStatus`는 `DPS`·`DES`·`RDS`뿐인데 `cycleDistributedKinds`는 `CYCLE_SCOPED_STATUSES`(`DES`·`EPS`) 밖에서 `distributedKinds`로 위임하고 `DES` 행은 already를 아예 읽지 않으므로 이 호출부에서 두 함수가 **동치**다(그래도 decisions (9)대로 `distributedKinds`를 쓴다). 대신 **강제로 사이클 스코프가 걸리게 한 변이**는 `resendingAnEmbargoArticleSendsTheCorrectionOnlyWhereItAlreadyWent` 단독 red라 정정본 의미론('역사상 어디로 나갔나')은 잠겨 있다. ② **변이 (b)는 계획이 지정한 D-6(전 수신처 실패)로는 잡히지 않는다** — `embargoStatusFor`가 `next == 현재 status`면 `null`을 주므로 빈 힌트 승격은 무해하다. 그래서 '이번 사이클 `distribute` 이력은 있는데 나간 것은 없는' 대역을 만들어 그 축을 따로 잠갔다(위 (b)의 red가 그 테스트다). Java 938→**961**(+23, failures/errors 0) · `--parity` **연속 2회** exit 0 · 5프로파일 **236관측**(176/48/4/5/3) diffs 0 **불변** · `--dual-run` exit 0 동일 236관측 diffs 0 · npm **1328 pass / 0 fail** · 정적 게이트 green(`Adr008DisciplineTest` 11 — **이 step 증분에 없다** · `ClockDisciplineTest` 4 · `NoSchemaSqlInMainSourcesTest` 8 · `ControllerProjectionBoundaryTest` 4 · `HandlerInventoryTest` 3) · `@Async`·`@Scheduled`·스레드·타이머·재시도 0 · SQL·DDL·`DELETE FROM` 0 · 이력 append-only · 컨트롤러/라우트/`RoutePolicy`/scope 표/`AppConfig`/`SpoolProperties`/Node 소스 **무접촉** · 리포 `news.db`·`uploads/` 무접촉(테스트는 임시 DB·임시 스풀)." },
    { "step": 7, "name": "tick-service", "status": "completed",
      "summary": "엠바고 시점 배부 tick `DistributionTickService` 신설(`src/services/distributionTickService.js` 229행 1:1 · `harness.news.service`). `Map<String,Object> run(actorUserId)`가 **투영까지 소유**한다(컨트롤러는 shape 매핑만 — 반환이 그대로 응답이라 화이트리스트가 서비스 안에서 닫힌다): 정상 **정확 6키**(`ok,at,scanned,distributed,failed,invalid` · `LinkedHashMap`+`unmodifiableMap`) · 재진입 **7키**(`skipped:'in-progress'`) · 거부 **2키**(`{ok:false,reason}`). 원소는 distributed 3키 · failed **4키** · invalid 2키이고 사유는 **고정 토큰만**(`tick-failed`·`status-changed`(=`DistributionService.STATUS_CHANGED` 재사용)·`no-active-target`·기본값 `SpoolWriter.SPOOL_WRITE_FAILED`) — 예외 메시지는 응답에 한 글자도 싣지 않는다. 시계는 `Iso8601.now(clock)` **실행당 1회**(`Clock` 필드는 있으나 판정은 그 문자열 하나를 전 기사에 넘긴다) · 후보는 `EMBARGO_DISTRIBUTABLE_STATUSES` 조회 후 `requiredKinds` 필터(SQL 엠바고 조건 0 — 레거시 DPS 픽업 유지, 비용은 `scanned`) · TOCTOU 1회 재조회 후 status가 바뀌면 **`done` 재계산**(불변식 A) · self-heal 사전 필터는 `CYCLE_SCOPED_STATUSES`를 재사용하지 않고 별도 `SELF_HEALABLE_STATUSES`(질문이 다르다 — decisions (9) 계열) · `status`·`distributedAt` **직접 쓰기 0**(각각 `ArticleEmbargoService`·`DistributionService` 위임). single-flight는 `AtomicBoolean.compareAndSet` + **해제는 `finally`**이고 분산 락·타이머·스레드·재시도 0(ADR-008 (3)(6) · excluded (l)(m)). throw 0: 후보 조회 `RuntimeException` → `tick-failed` 거부 · 기사 단위 예외 → 그 기사만 `failed`에 고정 토큰 후 스캔 계속 · 원인은 `TickErrorListener`(`TickError` 2키, HTTP 미노출)로만. **`@Service` 없음**(빈 배선은 step9). Node 정본 실측 20 시나리오(가짜 모델로 원본 `createDistributionTickService` 직접 호출 · 반환 키 집합·원소 키·호출 순서)를 표로 떠 테스트 **17개**(임시 DB + `@TempDir` 스풀 + `MutableClock` + **실물** DistributionService·SpoolWriter·ArticleEmbargoService). 변이 red 실증 7종(전부 clean verify 재실행): (a) `projectFailure` 원복(실물 항목 그대로) → 9번 **단독** red이고 실패 메시지에 `\"spoolDir\":\"ct-np-slug\"`가 실제로 찍혔다 (b) TOCTOU 후 `done` 재계산 제거 → 13번 **단독** red(스풀 파일 0→1 = 중복 배부 실재) (c) `no-active-target` 제거 → 7번 **단독** red (d) `okKinds.isEmpty()` 가드 제거 → **16번 red**(+7번 동반) — `distributed`에 `kinds:[]`인 거짓 완결 항목이 실제로 올라왔다(② 검토가 추가시킨 축이 비공허함을 실증) (e) 해제를 `finally` 밖으로 → 10번 후반 **단독** red(다음 호출이 영구 `skipped`) (f) 후보 조회 예외 전파 → 11번 **단독** error (g) `at`을 기사마다 재독 → 15번 **단독** red(전진한 시계로 둘째 기사가 조기 배부). 계획서와 갈린 점 1: **변이 (e)를 관측하려면 `RuntimeException` 가드를 뚫는 이탈이 필요**하다(Node도 Java도 예외 경로가 전부 잡혀 정상 반환한다) — `Error`를 던지는 리포지토리로 그 창을 열었고, `Error`는 삼키지 않는다는 기존 규율(`ArticleHistoryRecorder`)과도 일관된다. Java 961→**978**(+17, failures/errors 0) · `--parity` exit 0 · 5프로파일 **236관측**(176/48/4/5/3) diffs 0 **불변** · npm **1328 pass / 0 fail** · 게이트 green(`Adr008DisciplineTest` 11 — **이 step 증분에 없다** · `ClockDisciplineTest` 4 · `NoSchemaSqlInMainSourcesTest` 8 · `ControllerProjectionBoundaryTest` 4 · `HandlerInventoryTest` 3) · SQL·DDL·`DELETE FROM` 0 · 이력 append-only · 컨트롤러/라우트/`RoutePolicy`/scope 표/`AppConfig`/Node 소스 **무접촉** · 리포 `news.db`·`uploads/` 무접촉 · 리포에 스풀 파일 0." },
    { "step": 8, "name": "retry-service", "status": "completed",
      "summary": "배부 실패 조회·재전송 서비스 `DistributionRetryService` 신설(`src/services/distributionRetryService.js` 259행 1:1 · `harness.news.service`). **투영까지 서비스가 소유**한다(step7 tick 선례 — 반환 맵이 그대로 응답이라 화이트리스트가 서비스 안에서 닫힌다): `list(Object limit)`는 `{ok,items}`이고 항목은 **정확히 10키**(계약 `FAILURE_ITEM_KEYS` 정렬 동형) · `retry(Object historyId, String actorUserId)`는 성공 **5키**(`ok,articleId,targetId,kind,at` — `file`·`spoolDir` 0) / 거부 **2키**. 상수 3종은 Node 실측(`DEFAULT_LIST_LIMIT=200`·`MAX_LIST_LIMIT=1000`·**`RETRY_SCAN_LIMIT=1000000`**)이고 표시 창과 게이트 스캔을 **합치지 않았다**(decisions (16)). 게이트 순서는 계획 그대로다: (a) writer 없음 → `spool-disabled`(**DB 무접촉**) → 식별자 정규화 실패 → `no-failure`(**이력 조회 0회** — 전역 스캔 봉쇄) → (b) 단건 조회 + `articleId` 문자열 게이트 + **미해소 집합 멤버십**(`DistributionFailureLog.unresolvedFailures` **단일 출처**, 복제 0) → (b') `stale-cycle`(`EmbargoPolicy.latestSendId` · 경계 null이면 거부 안 함) → (c) `retry-in-flight`(`ConcurrentHashMap.newKeySet()` + `record FlightKey(articleId,targetId)` — 문자열 연결·NUL 0, **해제는 `finally`**) → (d) `not-found`/`inactive` → (e) `kind-changed`(**`Objects.equals` 엄격**) → (f) `not-found` → (g) `status-changed`(`EMBARGO_DISTRIBUTABLE_STATUSES` 단일 출처 · 비문자열 status 선차단으로 `contains(null)` NPE 폐색) → (h) 쓰기. **어떤 거부 경로에서도 write 0회 · 이력 0행**. 실패는 새 `distribute-failed` append(조건은 `isRetryableFailureReason` 단일 술어) + 통지 1회(`RetryFailure` 4키 — 경로 0. `DistributionService.DistributionFailure`와 **어휘 분리**: Node도 콜백 2개·로그 문구 2개다) + 사유 그대로. 성공은 `distribute-retry` append + `distributedAt` **present-only** 갱신을 **한 `TransactionTemplate`**에 둔다(decisions (19) — 목적은 두 문장 사이 커넥션 반납 방지뿐)이고 **이력 insert 실패는 삼킨다**(open_questions (d) 확정 = step5 동형 · Node 실측 B13: `ok:true` 5키·`distributedAt` 유지·`onHistoryError` 1회·예외 미전파 — 스풀 파일은 이미 나갔으므로 되돌릴 수 없다). `limit` 정규화는 **Node 서비스 함수 그대로**(`Number.isInteger` — `'5'`·`true`도 기본값 200)이고 HTTP의 `Number(req.query.limit)` 변환은 step9 라우트 몫으로 남겼다(그 분리가 반복 쿼리 키 NaN 의미론의 전제다). `historyId`는 typeof 게이트 + **`NodeNumber.toNumber` 단일 출처**(빈 문자열 검사는 Node와 같은 자리). `targetId`는 원장 `double`을 JSON에서 **정수로**(`nodeNumber` — step2 인계 사항 해소), 이력 바인딩은 Node와 같은 REAL(`Double`)이라 INTEGER affinity가 접는다. Node 정본 실측 3라운드(상수 3종·`limit` 12종·게이트 20여 시나리오·이력 insert 실패 처분)를 표로 떠 테스트 **26개**(`@TempDir` 임시 DB + 임시 스풀 + 계수 스파이 3종 + 테스트 전용 `BlockingFs` seam — main에 지연 0). 변이 red 실증 9종: (a) `spool-disabled`를 조회 뒤로 → `spoolDisabledIsDecidedWithoutTouchingTheDatabase` 단독 (b) 정규화 제거 → `anInvalidHistoryIdIsRejectedWithoutAnyLedgerQuery`(`1.5`가 실제로 조회로 흘렀다) (c) 멤버십을 '배부 이벤트면 통과'로 완화 → 멤버십 테스트 + `noGateRejectionEverAppendsToTheLedger` 동반 (d) `stale-cycle` 제거 → 같은 2건 (e) `equalsIgnoreCase`+trim → `aRetargetedKindIsRejectedStrictly` 단독 (f) 스캔 상한을 표시 창 200으로 → `theRetryScanIsNotLimitedToTheDisplayWindow` 단독(오래된 실패가 `no-failure`로 오거부 = 복구 불가 재현) (g) 해제를 `finally` 밖으로 → 동시성 테스트 후반 단독 (h) `cycleDistributedKinds`→`distributedKinds` → `kindDistributedCountsOnlyTheCurrentCycle` 단독 (i) **대안 설계(트랜잭션 원자성)**: `ArticleHistoryRecorder`가 재던지게 심어 `aFailedLedgerInsertKeepsTheRetryResultAndTheDistributedAt`가 `IllegalStateException`으로 **error** — 재전송 성공이 500이 되는 divergence를 실증하고 원복(두 파일 sha256 동일). Java 978→**1004**(+26, failures/errors 0) · `--parity` exit 0 · 5프로파일 **236관측**(176/48/4/5/3) diffs 0 **불변** · npm **1328 pass / 0 fail** · 게이트 green(`Adr008DisciplineTest` 11 — **이 step 증분에 없다** · `ClockDisciplineTest` 4 · `NoSchemaSqlInMainSourcesTest` 8 · `ControllerProjectionBoundaryTest` 4 · `HandlerInventoryTest` 3) · 타이머·자동 재시도·백오프·재시도 큐 0(ADR-008 (6)) · SQL·DDL·`DELETE FROM` 0 · 이력 append-only · `@Service` 없음(빈 배선은 step9) · 컨트롤러/라우트/`RoutePolicy`/scope 표/`ReasonStatus`/`AppConfig`/Node 소스 **무접촉** · 리포 `news.db`·`uploads/` 무접촉 · 리포에 스풀 파일 0. **step9 인계**: ① 라우트가 `Number(req.query.limit)`·`Number(body.historyId)`를 적용해 넘겨야 한다(서비스는 `Number.isInteger`/typeof 게이트만 소유) ② 성공 반환에는 `reason` 키가 없고 거부는 2키다(반환 맵을 그대로 실으면 shape이 맞는다) ③ 500 재매핑은 `spool-write-failed`·`invalid-spool-dir`·`invalid-article-id` 3토큰뿐이고 전역 `ReasonStatus`에 넣지 마라 ④ 목록은 스풀 미설정에서도 200이다." },
    { "step": 9, "name": "distribution-http", "status": "completed",
      "summary": "배부 실행 3라우트 결선(`DistributionController` · `harness.news.controller`) + **빈 배선 신설**(`DistributionConfig` · `harness.news.service` — `SessionConfig`/`LogConfig` 합성 루트 선례). **이 step이 step4~8의 서비스를 처음 컨텍스트에 올린다**: `SpoolWriter`·`DistributionService`는 스풀 루트가 있을 때만 만들고(없으면 `@Bean`이 `null` → 소비자는 전부 `ObjectProvider.getIfAvailable()`), `ArticleEmbargoService`·`DistributionTickService`·`DistributionRetryService`는 **항상** 만든다(설정 판정 지점은 `SpoolProperties.rootPath()` 하나 — tick/retry의 503은 서비스가 스스로 낸다. 목록은 스풀 미설정에서도 200). 그래서 step6의 송고 훅이 이 step에서 **처음 실전 발화**했고 `--parity`가 그 축까지 관측했다(diff 0). 컨트롤러는 토큰 판독(`SessionTokens`)·게이트·shape 매핑만 하고 `ContentsRow`를 모른다. `Authorization`에 capability 2행 추가(`RUN_DISTRIBUTION_TICK`·`MANAGE_DISTRIBUTION_FAILURE` → `[Z]`), **`RoutePolicy` 무접촉**. tick 핸들러는 파라미터가 request/response 둘뿐(`@RequestBody`·`readBody` 0 — 리플렉션 단언이 잠근다) · failures는 `limit`만 화이트리스트(`getParameterValues` → 반복 키는 값 리스트 그대로) · retry는 body에서 `historyId` 하나만. Node `Number(x)` 변환은 컨트롤러 **1지점**(`numberOf`: Boolean 1/0 · 배열은 콤마 결합 · 그 밖 NaN)이고 문자열 판독은 **`NodeNumber.toNumber` 단일 출처**다. `ReasonStatus` **+7행**(`spool-disabled` 503 · `tick-failed` 500 · `no-failure` 404 · `status-changed`/`kind-changed`/`stale-cycle`/`retry-in-flight` 409) — **7토큰 전부 HTTP 도달 테스트를 가진다**. 서버측 장애 3토큰은 **retry 라우트에서만 500**이고 전역 표에 없다(`Set.contains(null)` NPE는 `reason != null` 선행 검사로 폐색). `GET /api/distribution/tick`은 **POST 매핑 추가 전후 모두 실측 404**(`text/html; charset=utf-8` — 대조군 `DELETE /api/distribution-targets/1`도 404 text/html)라 `GlobalErrorHandler.handleNotFound`에 맡기고 **전용 처리 0**. 테스트 +18(1004→**1022**): `DistributionWireTest` 11(6키·body 무효·리플렉션·실배부+멱등·경로 유출 0·GET 404·limit 5프로브+반복 키 판별·retry 404 4프로브·409 3종·500 3종+CRUD 400 그물·재전송 성공·행 감소 0) · `DistributionDisabledWireTest` 3(401/403/503 · **인가가 설정보다 먼저** · 목록 200) · `DistributionSeamWireTest` 2(`retry-in-flight` 409는 `GatedSpoolWriter` 지연 seam으로 두 요청을 겹쳐 도달 · `tick-failed` 500은 커넥션을 주지 않는 데이터소스로 도달 — **지연·주입은 테스트 전용, main에 `Thread.sleep` 0**) · `ReasonStatusTest`+2 · `AuthorizationTest`+1. 인벤토리 29→**32**(메서드명·메시지 동시 갱신) · scope 표에 계약 2파일 알파벳 위치 추가. **AC 전건 green**: `mvnw -B verify` 1022/0 · `--profile minimal` 53케이스 · `--profile default` 163케이스 · `--parity` **연속 2회** 5프로파일 **265관측 diffs 0**(default 198 / minimal 55 / auth-negative 4 / failclosed 5 / prod-cookie 3 — 236 대비 **+29**로 Node 실측(22+7)과 정확히 일치) · `--dual-run` 265관측 diffs 0(실배부 결선 상태의 자기 결정성) · npm **1328 pass / 0 fail**. 변이 5종 red 실증: (a) 인가/설정 순서 뒤집기 → minimal `r-session-not-disabled` 503!==403 · (b) tick에 `@RequestBody`+`now` 주입 → 계약 `z-body-ignored` red + 리플렉션 단언 red · (c) 500 3토큰 전역화 → `distribution-targets.contract.js` 500!==400 + 와이어 테스트 red · (d) `limit`을 `getParameter`로 첫 값만 → 반복 키 프로브 red + `WebWiringTest` 정적 게이트 red · (e) `spool-disabled` 행 삭제 → minimal 400!==503. DB 비파괴: DDL·`DELETE FROM` 0 · 이력 append-only · 리포 `news.db`·`uploads/` 무접촉 · 리포 안 스풀 파일 0 · `Adr008DisciplineTest` 증분 없음." },
    { "step": 10, "name": "parity-closeout", "status": "completed",
      "summary": "phase 마감 — 코드 변경 0(문서·계획 파일만). A의 9커맨드를 **연속 2회** 돌려 전부 exit 0·수치 동일(flake 0): Java **1022 tests / 0 failures** BUILD SUCCESS · jar **35,738,248 B**(clean 기준 · 증분 빌드는 153 B 작다는 사실을 실측해 기록) · 무인자 `profiles=5`(default cases=163 covered=24/39 · minimal 53 2/39 · auth-negative 4 · failclosed 5 · prod-cookie 3 1/39) · `--parity` **265관측 diffs 0**(198/55/4/5/3) · `--dual-run` **265관측 diffs 0**(10 인스턴스 pid·port·DATA_DIR 상이) · `npm test` **1328/1328** 불변 · lint·build clean · `contract-inventory-check --require-spec-paths` routes=39 spec-paths=39/39 · **Node 대상** `--require-full-coverage` profiles=5 cases=274 covered=39/39. **인벤토리 대조를 실제 리포트로 수행**: routeId 합집합 36 − `x-` 유사 라우트 4 = **32**로 `IMPLEMENTED_ROUTES` 32행과 일치(계약이 관측하지 않는 구현 라우트 0). 데이터 안전 전건 통과(`news.db` md5·크기·mtime 무변 · `uploads/` 무변 · 리포 내 스풀/`.tmp`/리포트 0 · 하네스 java 잔존 0 · 이 step의 OS 임시 잔존 0 · 리포트 5종에 세션 토큰·비밀번호·수집 토큰·절대경로 0). **게이트 비공허성 재확인**: `DistributionTickService.java`에 한정 이름 `@Scheduled`를 심어 `Adr008DisciplineTest.mainSourcesRunNoTimersOrRetries` red를 실증하고 원복(HEAD 블롭과 byte-identical). 문서 산출물은 `server-spring/README.md`(29→32 · 미구현 7 · `app.distribution.spool-dir` env 행 · ADR-008 예외 2파일 충족 · 배부 미검증 9항목 · 커버리지/프로파일 수치 갱신)와 `docs/ADR.md` **ADR-013 ④ 1문장**(71a+72 함께 · 결정 본문과 68·69·70 문장 무수정), 그리고 `phases/index.json`의 72 항목 note. `open_questions` (a)~(e)에 마감 결과를 덧붙이고 `forward_notes` 13항목을 작성했다." }
  ]
}
