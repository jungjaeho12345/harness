{
  "project": "기사 작성기",
  "phase": "68-spring-auth",
  "scope": "포팅 로드맵(docs/porting-plan-cpp-spring.md §7) P1의 **첫 Spring 구현 슬라이스 = auth/session 도메인**만 구현한다. 39 엔드포인트 전부가 아니라 로그인·세션·인증과 그에 필요한 최소 지원 라우트만 다룬다. 대상 계약 케이스 5파일(이미 리포에 있는 것): default/health · default/auth · default/session-guard · auth-negative/login-negative · prod-cookie/cookie-prod. 완료 판정 = 이 5파일을 **하네스가 프로파일마다 새 Spring 프로세스로 띄워** 실행했을 때 전건 green + 같은 파일을 Node에 돌린 리포트와 **contract-diff 0**. 산출물: server-spring/(Spring Boot Maven — health·session·login·logout·session-guard 재도출·계정잠금·레이트리밋·지원 라우트 users/logs-digest/articles-list) + scripts/contract-run.mjs의 --target spring(하네스가 Spring 수명을 소유, 프로파일마다 새 프로세스) + scripts/contract-parity-spring.mjs(Node↔Spring 교차 diff 오케스트레이터). 불변: 리포 news.db 무접촉(하네스가 임시 DATA_DIR 시드) · Spring DDL 0 · DELETE/DROP 0 · 앱 내 타이머/egress 0(ADR-008) · acting role은 세션에서만·매 요청 User 재도출(ADR-004). 이 phase 밖(후속): sse-stream·articles read/write·collection·distribution·media/upload·photos·receiver-config·users 전체 계약·logs/stream·CSRF Origin 가드·minimal/failclosed 프로파일.",
  "steps": [
    { "step": 0, "name": "spring-skeleton", "status": "completed", "summary": "server-spring/ Spring Boot 4.1.0(Java 21, Maven) 골격 신설. 공개 GET /api/health→200 {\"ok\":true}(HealthController). 포트/바인드 환경변수 외부화(server.port=${PORT}, server.address=${HOST:127.0.0.1}) — 하드코딩·D:/ 없음. finalName=spring-auth로 target/spring-auth.jar 고정. starter-security 미포함(webmvc + webmvc-test만). mvnw 래퍼 복사, .gitignore에 server-spring/target/. Spring Boot 4.1에서 @WebMvcTest 패키지가 org.springframework.boot.webmvc.test.autoconfigure로 이동 반영. AC 전건 green(MockMvc 테스트·jar 부팅 왕복 46990·lint), npm test 1328 불변." },
    { "step": 1, "name": "parity-harness", "status": "completed", "summary": "contract-run.mjs에 --target <node|spring> 추가(기본 node). spring이면 runProfile의 서버 spawn만 분기 — 프로파일마다 새 java -jar 프로세스 + 새 임시 시드 DB(APS_DB_FILE)로 띄운다(카운터 프로파일 간 격리). env 명시 조립(PATH·JAVA_HOME만 상속, 외부 키·NODE_*·JAVA_TOOL_OPTIONS 미전달 = egress 0), prod-cookie만 APS_PROD_COOKIE=true. jar 해석: APS_SPRING_JAR 또는 server-spring/target/spring-auth.jar, 없으면 mvn package 1회(실패는 exit 1). --target spring + --base-url-map 동시 사용 금지. contract-parity-spring.mjs 신규(큐레이션 5파일 프로파일별 Node·Spring 실행 → contract-diff.mjs compareReports 재사용 → diff 0 판정, 리포트 OS 임시만). package.json에 test:contract:spring·test:contract:parity. AC 전건 green: Spring auth-negative/prod-cookie boot-check 각각 별 프로세스·별 포트(47815·49115), Node 경로 무회귀, lint 0, npm test 1328 불변, 리포 news.db 무변." },
    { "step": 2, "name": "db-access", "status": "completed", "summary": "NewsDb 영속 계층(SQLite JDBC) + AppConfig 합성 루트 빈. pom에 sqlite-jdbc 3.47.2.0·spring-security-crypto 추가(offline 캐시 확보). NewsDb: 생성자 APS_DB_FILE 필수(빈 경로 즉시 IllegalStateException — 하드코딩 폴백 없음, 원본 news.db 무접촉), busy_timeout 5s. findUser(userId)→User(userId·name·role·department·departmentCode·active·password·failedLoginCount·lockedUntil·lastFailedLoginAt), insertUser/updateUser는 컬럼 화이트리스트만(케이스가 컬럼명을 못 정함 — 주입 표면 차단, 비화이트리스트는 IllegalArgumentException), listArticles는 Contents 전 컬럼에서 lockerSessionId·lockerClientId만 제거. DDL/행제거문 0(정적 grep 잠금 통과). NewsDbTest 9건 green(시드 조회·insert·update·미존재 0행·화이트리스트 거부·잠금컬럼·locker 투영 제거·빈 경로 실패). AC 전건 green: mvn test 10, package jar, grep clean, harness spring boot(news.db 무변), lint 0, npm test 1328 불변." },
    { "step": 3, "name": "session-core", "status": "pending" },
    { "step": 4, "name": "auth-endpoints", "status": "pending" },
    { "step": 5, "name": "guard-support-endpoints", "status": "pending" },
    { "step": 6, "name": "parity-green", "status": "pending" }
  ]
}
