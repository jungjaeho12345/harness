{
  "project": "기사 작성기",
  "phase": "75-mysql-migration",
  "scope": "C++/Spring 포팅 로드맵(`docs/porting-plan-cpp-spring.md` §7) **P2 'DB 이관'** 을 소유한다. 산출물은 넷이다: ① **`tools/news-migrator`** — `news.db`(SQLite) → **MySQL 8.0** 마이그레이터. 소스는 **읽기 전용**이고 실행 후 원본 파일이 **바이트 무변**이며, 전 테이블 행 수·전 컬럼 값을 **100% 대조**한다. ② **Flyway 기반선** — MySQL 스키마의 정본을 그 모듈이 소유한다(`server-spring`은 DDL 0을 유지한다 — `NoSchemaSqlInMainSourcesTest`가 main 소스·리소스에서 `flyway` 철자 자체를 금지하기 때문이고, 그 게이트는 완화 대상이 아니라 **설계 입력**이다). ③ **역방향 export** — MySQL → SQLite `.db`. 형식을 SQLite로 정한 이유는 대조 검증기를 그대로 재사용할 수 있고 산출물이 **Node 서버가 실제로 열 수 있는 롤백 자산**이 되기 때문이다. ④ **`server-spring`의 MySQL 기동 + 계약 패리티** — 방언 지점을 `NewsDataSource` 한 파일로 모으고, 계약 하네스가 Spring을 MySQL로 띄운 채 Node(SQLite) 대비 **313관측 diffs 0**을 낸다. **Node 서버(`server/**`·`src/**`)는 무수정 SQLite 정본으로 남는다** — 이 phase 이후 **Node=SQLite / Spring=MySQL 병존이 정상 상태**다. 계약 스위트(`contract/**`)·계약 명세(`docs/api-contract/**`)·정본 러너(`scripts/contract-run.mjs`·`scripts/contract-diff.mjs`)·`package.json`은 **한 줄도 고치지 않는다**(그 마지막 제약이 하네스 설계를 강제한다 — Node에 MySQL 드라이버를 넣을 수 없으므로 하네스는 **마이그레이터 jar를 통해서만** MySQL을 만진다). **운영 데이터의 실제 컷오버는 이 phase가 실행하지 않는다** — 절차·리허설·롤백 경로까지가 범위이고 실행은 P3다.",

  "baseline": "**브랜치는 `feat-75-mysql-migration`이고 base는 `9df5381`**(`feat-0-mvp`의 PR #120 머지 — 그 안에 74 머지 `e959a96`(PR #121)가 들어 있다). **아래 수치는 이 계획서가 2026-09-02에 `9df5381` 트리에서 포터블 JDK 25로 직접 재측정한 값이다(74 forward_notes의 인용이 아니다)**: ① `cd server-spring && JAVA_HOME=\"D:/agents/tools/jdk-25.0.4.1+1\" ./mvnw -B clean verify` → **Tests run 1366 / Failures 0 / Errors 0 / Skipped 0** BUILD SUCCESS(**4:43**) · jar **35,800,437 B** ② `SPRING_JAVA_HOME=\"D:/agents/tools/jdk-25.0.4.1+1\" node scripts/spring-contract.mjs --parity`(리포 루트 cwd) → **exit 0 · profiles=5 · 총 313관측 diffs 0**(default 246 cases=209 **covered=32/39** · minimal 55 cases=53 covered=2/39 · auth-negative 4 · failclosed 5 · prod-cookie 3 covered=1/39) ③ 리포 `news.db` **606,208 B · md5 `7247e9e0dfe5cc8cd040ebb1dc9fb967`**(하네스 실행 후 재확인 — 무변). **소스 데이터 실측**(읽기 전용 사본에서 측정): 7테이블 · **총 178행**(Article 77 · ArticleHistory 12 · Contents 77 · DistributionTarget **0** · Photo 1 · ReceiverConfig **0** · User 11) · `typeof()` 집계는 **text/null/integer 3종뿐**(real·blob 0) · `Article.markupVersion` 최대 **165,734자 / 165,802바이트**(⇒ MySQL `VARCHAR` 불가) · `Contents.createdAt` 77/77 상이 · 빈 문자열과 NULL이 같은 컬럼에 공존(`embargoAt` 빈 문자열 52 + NULL 10 · `secondEmbargoAt` 빈 문자열 67 + NULL 10) · `Contents.title` 76/77·`User.name` 11/11이 한글. **MySQL 실측**: `mysql.exe` **Ver 8.0.46 Win64**(`C:\\Program Files\\MySQL\\MySQL Server 8.0\\bin\\`) · 3306 응답(무비밀번호 접속은 `ERROR 1045` = 서버 도달) · `my.ini`에 **`lower_case_table_names=1`** · sql-mode에 **`STRICT_TRANS_TABLES`** 포함 · `default-storage-engine=INNODB` · `max_allowed_packet=64M` · `max_connections=151` · `secure-file-priv` 설정됨(⇒ `INTO OUTFILE` 경로 쓰지 마라) · `character-set-server` 미설정(= 8.0 기본 `utf8mb4`/`utf8mb4_0900_ai_ci`) · `log-bin` 활성. **root 비밀번호는 알 수 없다** — step0이 사용자 실행 부트스트랩으로 전용 계정 3종을 만든다. **툴체인**: 리포 루트 `d:/agents/harness` · 포터블 Temurin **JDK 25.0.4.1+1 = `D:/agents/tools/jdk-25.0.4.1+1`**(유일 기준선 JDK — `jdk-21.0.12+8`을 쓰지 마라) · Maven은 `server-spring/mvnw` 래퍼(시스템 java는 1.8이라 `JAVA_HOME` 필수) · `server-spring/pom.xml`의 `java.version`은 **25**이고 `maven-compiler-plugin` 블록은 **없다**(추가하지 마라) · `spikes/p0-spring/pom.xml`은 21 그대로(동결 산출물) · Spring 인스턴스 포트는 **[15000, 20000)** · `spring-contract.mjs`는 jar를 스스로 빌드하지 않는다(소스 수정 후 `package -DskipTests` 선행) · **`~/.m2`에 `com.mysql:mysql-connector-j`도 `org.flywaydb:*`도 없다** ⇒ Maven Central 네트워크가 필요하며 없으면 step0에서 blocked. **무접촉(수정·`git add` 금지)**: `contract/**` · `docs/api-contract/**` · `scripts/contract-run.mjs` · `scripts/contract-diff.mjs` · `server/**` · `src/**` · `web/**` · `client/**` · `test/**` · `package.json` · `docs/news.md` · `spikes/**` · 리포 `news.db`(읽기만) · `uploads/` · `server-spring/src/test/java/harness/news/db/NoSchemaSqlInMainSourcesTest.java` · `server-spring/src/test/java/harness/news/service/Adr008DisciplineTest.java`.",

  "order": "step0 baseline-and-credentials(기준선 재측정 + 최소권한 계정 3종 확보 절차 + 비밀 위생 스캔) → step1 dialect-semantics-probe(**11축 실측 + 영구 차등 테스트 + 매핑표 확정**) → step2 adr-and-migrator-baseline(**ADR-016 신설** + `tools/news-migrator` 모듈 + Flyway V1 + 파괴적 SQL 정적 게이트) → step3 row-copy-verify(**전 행 복사 + 100% 대조 · 원본 무변**) → step4 reverse-export(**역방향 export + 왕복 대조 0 · 안전 절단점**) → step5 dialect-seam(server-spring 방언 지점 단일화 · **동작 무변경**) → step6 mysql-runtime(MySQL 기동 + **계약이 못 보는 축의 차등 테스트**) → step7 harness-mysql-parity(**GREEN — `--db mysql --parity` 313관측 diffs 0**) → step8 cutover-runbook(컷오버·롤백 절차 + 문서 정본) → step9 closeout(마감 실측 2회 + 교차 변이 + forward_notes). **근거**: (a) **아래에서 위로**: 측정 → 스키마 정본 → 이관 → 역방향 → 서버 배선 → 서버 런타임 → 하네스 → 운영 절차. (b) **계약 green 없는 연속 구간이 없다.** 이 phase는 74와 달리 「새 계약 관측을 여는」 phase가 아니라 「기존 313관측을 다른 저장소 위에서 재현하는」 phase다 — step0~step4는 `server-spring/src/main`을 한 줄도 고치지 않으므로 313관측이 구조적으로 무회귀이고, step5~step6은 sqlite를 **기본 방언으로 유지**하므로 **313관측 diffs 0이 매 step의 실질 회귀 게이트**다. 새 green(MySQL 313관측)은 step7 한 곳에서 열린다. (c) **절단하지 않는다. 다만 step4가 명시적 안전 절단점이다.** 로드맵 P2의 문자 그대로의 AC(전 행 대조 100% + 원본 무변 + 역방향 경로 확보)는 **step4에서 이미 충족**된다. 그럼에도 자르지 않는 이유는 (i) 서버가 MySQL로 뜨지 않으면 「이관이 동작을 바꾸지 않았다」를 증명할 수단(=패리티)이 없고 그 증명이 P3 컷오버의 전제다 (ii) 마이그레이터만 남기면 소비자 없는 산출물이 된다 (iii) 10 step은 72·73(11)보다 짧다. 실행이 정체되면 **step4까지로 마감하고 step5~9를 후속 phase로 넘기는 것이 유효한 선택**이며, 그 경우 「P2 AC 충족 · 서버 전환 미완」으로 정직하게 기록한다. (d) **step1(측정)이 step2(ADR)보다 앞인 이유**: collation·타입 매핑·id 생성·접속 파라미터는 **실측이 정해야 하는 값**이고, 그것을 모른 채 쓴 ADR은 추측을 결정으로 굳힌다(73이 ADR-005를 오인용해 revise를 받은 계열의 실패다). (e) **step5(배선)와 step6(런타임)을 나눈 이유**: step5는 동작 변화 0이라 313관측 무회귀가 곧 AC가 되어 격리가 깨끗하고, step6은 MySQL 실기동이라 실패 원인이 배선인지 런타임인지 섞이면 안 된다. (f) **`NoSchemaSqlInMainSourcesTest`·`Adr008DisciplineTest`·`RoutePolicy`·`contract-run.mjs`는 어느 step도 고치지 않는다** — 이 설계가 새 DDL·타이머·스레드·라우트·판정 규칙을 만들지 않기 때문이고, 그 사실 자체가 각 step의 AC다.",

  "decisions": [
    "(1) **DB는 MySQL 8.0이다**(사용자 결정 2026-09-01). 계획서 §3-②는 MariaDB를 *권장안으로 가정*만 했고 §10 열린 질문 1이 이번에 닫힌다. 이 머신의 **8.0.46**이 대상이며 `lower_case_table_names=1`·`STRICT_TRANS_TABLES`가 실측된 전제다.",
    "(2) **Flyway는 `server-spring`에 들어가지 않는다 — 별도 모듈 `tools/news-migrator`가 소유한다.** 근거는 실측이다: `NoSchemaSqlInMainSourcesTest.FORBIDDEN`(44~53행)이 `flyway`·`liquibase`·`ddl-auto` 철자를 **main 소스와 main 리소스 양쪽에서** 금지하고, `mainResourcesDeclareNoAutomaticMigration`이 `schema.sql`·`data.sql` 부재까지 단언한다. 그 게이트를 완화하는 것은 이 phase의 목적(비파괴 보장 강화)과 정반대다. **결과: `server-spring`은 ADR-013 ②의 「DDL 0 · 부팅 시 존재만 읽기 검증」을 그대로 유지하고 그 테스트 파일은 0줄 바뀐다.** MySQL 스키마의 정본은 마이그레이터의 Flyway 마이그레이션이고 SQLite 측 정본은 여전히 `src/db/schema.js`다(= 74 forward_notes (8) ⑤가 P2에 넘긴 「스키마 소유자 이양 시점」의 답).",
    "(3) **마이그레이터는 독립 Maven 프로젝트다**(`server-spring`을 멀티모듈 reactor로 바꾸지 않는다). 근거: `scripts/spring-contract.mjs`가 `server-spring/target/*.jar` 경로에 의존하므로 reactor 전환은 하네스를 깬다. 래퍼(`mvnw`·`.mvn`)는 복사한다.",
    "(4) **패리티 하네스 설계 — 마이그레이터를 시드 경로로 재사용한다.** `package.json`이 무수정이라 Node 하네스에 MySQL 드라이버를 넣을 수 없다(내장 모듈과 자식 프로세스만 가능). 그래서 `--db mysql` 모드의 절차는 **① 지금과 똑같이 임시 `DATA_DIR`에 SQLite를 시드**(`createSchema`+`seedUsers` — 스키마·시드의 단일 출처가 `src/db/**`라는 사실을 유지한다) **② 마이그레이터 jar로 패스 전용 ephemeral MySQL DB를 만들고 그 SQLite를 적재 ③ Spring을 `DB_KIND=mysql`로 그 DB에 붙여 기동 ④ 러너는 지금과 동일하게 외부 대상 모드로 호출**이다. 이 설계의 이점 셋: 두 저장소가 **같은 시드**에서 출발함이 구조적으로 보장되고, 마이그레이터가 **매 패리티 실행마다 도그푸딩**되며, `contract-run.mjs`·`contract-diff.mjs`가 무수정으로 남는다. Node 대조 패스는 **언제나 SQLite**이므로 이 green은 정확히 **Node(SQLite) vs Spring(MySQL)** 비교다.",
    "(5) **ephemeral DB 이름 규약과 그 삭제.** 하네스가 만드는 임시 MySQL DB 이름은 **`^harness_ct_[0-9a-f]{16}$`** 이고, 그 이름에 한해서만 드롭한다. 이것은 오늘의 임시 SQLite 파일(이미 삭제된다)과 같은 지위다 — **최상위 규칙(DB 행 삭제 금지)이 지키는 대상은 뉴스 데이터이고, 그 보호는 두 겹으로 건다**: ① 접두사 정규식(하네스·마이그레이터 양쪽에 두고 각각 테스트로 잠근다) ② **MySQL grant** — `news_ct` 계정은 `` `harness\\_ct\\_%`.* `` 밖에 아무 권한이 없다. 드롭 코드는 마이그레이터의 **정확히 1파일**에만 있고 정적 게이트가 그 예외 목록의 **크기·구성**을 단언한다(`Adr008DisciplineTest`의 규율).",
    "(6) **최소 권한이 DB 비파괴의 1차 방어선이다.** 계정 3종: `news_app`(서버 — `SELECT/INSERT/UPDATE`만. `DELETE`·`DROP` 없음) · `news_migrator`(`SELECT/INSERT/CREATE/ALTER/INDEX` — `DELETE`·`DROP` 없음) · `news_ct`(ephemeral 접두사 한정 `ALL`). 유일한 행 삭제 예외인 `DELETE FROM ReceiverConfig`는 **테이블 단위 grant**로만 연다(나머지 6테이블은 DB 서버가 계속 막는다). 정적 스캔은 2차 방어선이다 — 71a 12/12·72 11/11·73 8/10·74 2건이 **전부 green인 채로 뚫린** 전례가 있기 때문에 코드 밖에도 방어선을 둔다.",
    "(7) **비밀은 리포·대화·argv·로그 어디에도 남기지 않는다.** 부트스트랩 SQL은 플레이스홀더만 담고 사용자가 root로 1회 실행한다. 실제 값은 **리포 밖 env 파일**(`D:/agents/secrets/news-mysql.env` 규약)에 두고 환경변수로만 흐른다. 비밀번호는 **커맨드라인 인자로 넘기지 않는다**(프로세스 목록 노출). `SecretHygieneTest`(step0)가 리포 전역을 스캔하고, 71a·73이 센티넬로 잠근 선례를 승계한다.",
    "(8) **방언 선택은 명시 주입이다 — 탐지 금지.** `app.db.kind`(`DB_KIND`, 기본 `sqlite`)가 정하고, `mysql`이면 `NEWS_DB_URL/USERNAME/PASSWORD`가 전부 있어야 뜬다. URL을 보고 방언을 추론하지 않고, `kind`와 URL이 모순이면 **기동을 거부**한다(포팅 불변식 7 「런타임 탐지보다 명시 주입」).",
    "(9) **sqlite 지원을 제거하지 않는다.** 근거 셋: ① 1366개 Java 테스트가 임시 SQLite 픽스처(`TempNewsDb` — **71개 테스트 파일이 쓴다**)에 서 있다 ② sqlite 기본 모드가 **롤백 레버**다(로드맵의 되돌림 지점은 P3 완료 전까지 유효하다) ③ 제거는 되돌리기 어렵고 이 phase의 판정(313관측 무회귀)을 잃는다. 제거 시점 판단은 P3에 넘긴다.",
    "(10) **`MAX_POOL_SIZE = 1`을 유지한다.** SQLite 단일 writer라는 원래 근거는 MySQL에서 사라지지만, 확대는 **동시성 동작을 바꾸는 별개 결정**이고 74 ⑤가 폐색한 락 순서 결함의 방어선(`LogsStreamWireTest` 항목 22 — 실제 Hikari 상한 1로 순환 대기를 세운다)이 그 상수에 걸려 있다. 바꾸려면 별도 ADR과 그 테스트의 재설계가 함께 필요하다 — P3 소유.",
    "(11) **collation·타입 매핑은 step1의 실측이 정한다(계획서가 값을 박지 않는다).** 유력 가설과 그 근거만 적는다: 저장·`=`·`ORDER BY`는 SQLite BINARY와 같아야 하므로 **`utf8mb4_0900_bin`**(NO PAD 계열)이 후보 1위이고 `utf8mb4_bin`(PAD SPACE 계열)은 **후행 공백을 무시할 위험**이 있어 반드시 실측으로 배제·채택해야 한다. ⚠ 이 축은 보안 축이다 — `WHERE userId = ?`가 대소문자나 후행 공백을 무시하면 **다른 계정으로 로그인**할 수 있다. 반면 SQLite `LIKE`는 ASCII 대소문자 무시가 기본이라 `_bin`에서는 검색이 갈린다 — **완전 일치하는 단일 collation이 없을 수 있고, 없으면 그것을 divergence로 기록**한다(억지로 맞추려고 `server/**`를 고치지 마라).",
    "(12) **텍스트 타입 매핑 규칙(step1이 검증한다)**: PK·`=` 조회 대상 → `VARCHAR(768)`(utf8mb4 단일 컬럼 인덱스 상한 3072바이트에 맞춘 값) · 나머지 텍스트 → `LONGTEXT` · `INTEGER PRIMARY KEY` → `BIGINT AUTO_INCREMENT` · `targetId INTEGER` → `BIGINT`. **`markupVersion`이 165,802바이트라 `VARCHAR`가 원천 불가하다는 것이 실측이다.** **날짜·시각을 `DATETIME`으로 승격하지 않는다** — 정본이 ISO 문자열을 TEXT에 넣으므로 승격하면 포맷·타임존이 왕복에서 갈린다. **보조 인덱스·FK를 만들지 않는다**(정본이 `PK 자동 인덱스만 사용`이다 — 성능 차이는 divergence로 기록하고 P3에 넘긴다).",
    "(13) **역방향 export의 형식은 SQLite `.db` 파일이다.** ① 대조 검증기를 그대로 재사용할 수 있고 ② 산출물이 **Node 서버가 실제로 여는 롤백 자산**이 되며 ③ JSON/CSV는 NULL vs 빈 문자열·숫자 표기·인코딩에서 **새 divergence 축**을 만든다(이 phase가 줄이려는 바로 그것). 왕복(`news.db` → MySQL → `export.db`) 대조 불일치 0이 step4의 AC다.",
    "(14) **MySQL 없이 스위트가 green이 되지 않게 한다(fail-closed).** MySQL 관련 테스트가 조용히 skip되면 이 phase의 모든 게이트가 공허해진다 — 정적 스캔이 매번 공허했던 것과 같은 실패 양식이다. 그래서 `MysqlConfiguredGuardTest`는 설정이 없으면 **skip이 아니라 fail**한다. 대가는 `mvnw verify`가 MySQL 서버를 요구하게 되는 것이고, 그 트레이드오프를 ADR-016에 명시한다.",
    "(15) **「계약이 잡는다」고 적지 않는다 — 실측으로 확인된 맹점이 셋이다.** `contract/lib/record.js` 119~126행이 싣는 것은 `status`·`ok`·`reason`·`bodyKeys`·케이스가 명시한 `values`·허용 헤더뿐이고, 목록 케이스는 **`idsOf(items).sort()`로 비교한다**(`articles-read.contract.js` 237·382·400행). 따라서 **① 행 정렬 순서 ② LIKE 대소문자(`photos-search`는 소문자 랜덤 토큰만 쓴다) ③ id 재사용(`receiver-config` 케이스는 id 원값을 리포트에 싣지 않는다)** 은 계약이 구조적으로 보지 못한다. **이 세 축과 바인딩 표현·NULL vs 빈 문자열·잠금 축의 유일 방어선은 step6이 만드는 Java 차등 테스트다.**",
    "(16) **`server-spring/src/main`의 SQLite 전용 표면은 정확히 5곳이다**(실측): `NewsDataSource` 60~61행(driver·URL) · 63행+82~97행(`PRAGMA busy_timeout` + read-back) · `SchemaGuard` 70행(`pragma_table_info`) · `DistributionTargetRepository` 172행 · `ReceiverConfigRepository` 160행(`last_insert_rowid()`). step5는 이 중 `SchemaGuard`를 **JDBC `DatabaseMetaData`** 로, `last_insert_rowid()` 2곳을 **이미 다른 리포지토리가 쓰는 `GeneratedKeyHolder`** 로 수렴시켜 **방언 지점을 `NewsDataSource` 하나로 줄이고**, 그 사실을 정적 스캔(예외를 **파일 집합**으로 단언 — 개수만 세지 마라)으로 잠근다."
  ],

  "excluded": [
    "(a) **운영 데이터의 실제 컷오버 실행** — 정지 창·백업·사람의 승인이 붙는 사건이고 로드맵상 P3다. 이 phase는 절차(런북)·리허설(스테이징)·롤백 경로 확보까지 소유한다.",
    "(b) **Node 서버의 MySQL 전환** — `server/**`·`src/**`는 무수정 정본이고 SQLite로 남는다. 그것이 롤백 레버다.",
    "(c) **커넥션 풀 확대·동시성 튜닝** — decisions (10). 별도 ADR과 `LogsStreamWireTest` 항목 22의 재설계가 함께 필요하다.",
    "(d) **보조 인덱스·FK 도입, 쿼리 최적화** — 정본의 인덱스 구성(PK 자동 인덱스만)과 동형을 유지하는 것이 패리티 원칙이다. 성능 축은 P3.",
    "(e) **`helmet` 등가 보안 헤더 11종·HTTPS 강제** — ADR-013이 남긴 공백이고 74 forward_notes (8) ②가 P3 소유로 못 박았다.",
    "(f) **부트 백필 2종 이식**(`backfillEmptyDepartments`·`backfillHistoryTitles`) — 누적 이월 항목이며 이관은 행을 있는 그대로 옮긴다(백필은 값을 **바꾸는** 동작이라 이관 검증과 섞으면 100% 대조가 성립하지 않는다).",
    "(g) **MySQL 운영(백업·복제·모니터링·튜닝)** — 이 phase는 개발 머신의 단일 인스턴스만 다룬다.",
    "(h) **ORM/JPA/MyBatis 도입** — ADR-013 ②의 직접 SQL 유지 결정을 승계한다. 방언 전환은 SQL을 바꾸는 일이 아니라 **접속과 5개 지점**을 바꾸는 일이다.",
    "(i) **누적 이월 divergence의 해소**(경로 정규화 · 요청 본문 크기 상한 · 304/ETag · FTP 스풀 수집 · 다중 인스턴스 tick · `HttpRequest.timeout` · `Date.parse` · `DistributionTargetService.checkName`의 `String.trim()` · Node 주석의 ADR 오인용) — 승계해 기록만 하고 새 결함으로 보고하지 않는다.",
    "(j) **계약 관측의 확장** — 새 케이스·새 라우트를 만들지 않는다. 이 phase의 판정은 **기존 313관측이 다른 저장소 위에서 그대로 재현되는가**이다."
  ],

  "open_questions": [
    "(1) **`utf8mb4_0900_bin`이 정말 SQLite BINARY와 같은가.** PAD 속성(후행 공백)·한글 정렬·`LIKE` 대소문자 세 축을 동시에 만족하는 collation이 존재하지 않을 수 있다. **미판단 시 기본 결정: step1이 3종 후보(`utf8mb4_0900_bin`·`utf8mb4_bin`·`utf8mb4_0900_ai_ci`)를 실측해 `=`/`ORDER BY` 축을 가장 잘 맞추는 것을 컬럼 기본으로 채택하고, `LIKE` 축에서 남는 차이는 divergence로 기록한다(`server/**`를 고쳐 맞추지 않는다).**",
    "(2) **`LIKE` 대소문자 차이를 표현식 단위 `COLLATE`로 복원할 것인가.** 복원하면 SQLite에 가까워지지만 리포지토리 SQL이 방언에 물들고(decisions (16)의 「방언 지점 하나」가 깨진다), 안 하면 검색 결과가 갈린다. **미판단 시 기본 결정: 복원하지 않고 divergence로 기록한다.** 단 step6의 차등 테스트가 **양쪽 기대값을 각각 명시**해 그 차이를 고정하고, 사용자·리뷰가 「검색이 대소문자를 무시해야 한다」고 판단하면 그때 표현식 `COLLATE`로 좁힌다.",
    "(3) **`news_ct` 계정의 와일드카드 grant가 `CREATE DATABASE`까지 허용하는가.** MySQL은 `` `prefix\\_%`.* `` 형태의 DB 단위 grant로 매칭되는 DB의 생성을 허용하는 것으로 알려져 있으나 **이 서버에서 실측하지 않았다**. **미판단 시 기본 결정: step0이 실측한다. 안 되면 대안 — 부트스트랩이 고정 개수(예: 12개)의 ephemeral DB를 미리 만들어 두고 하네스가 풀에서 빌려 쓰며 내용만 재적재한다(그 경우에도 재적재는 삭제가 아니라 「빈 DB 요구」 규율을 유지하도록 설계 재검토가 필요하다).**",
    "(4) **`MysqlConfiguredGuardTest`의 fail-closed가 과한가.** 이 리포의 `mvnw verify`가 MySQL 서버를 요구하게 된다(오프라인 개발 불가). **미판단 시 기본 결정: fail-closed를 채택한다**(decisions (14)). 대안은 「skip 허용 + phase AC가 `Skipped: 0`을 요구」인데, 그것은 다른 사람이 다른 날 돌릴 때 조용히 공허해지는 경로를 남긴다.",
    "(5) **ephemeral MySQL DB를 드롭해도 되는가.** 최상위 규칙의 문자만 보면 어떤 DB도 지우면 안 된다. **미판단 시 기본 결정: decisions (5)의 이중 가드(접두사 정규식 + grant 경계) 아래에서 드롭한다** — 오늘의 임시 SQLite 파일이 이미 삭제되고 있고, 안 지우면 실행마다 5~10개 스키마가 영구히 쌓인다. 대안(드롭하지 않고 사람이 주기적으로 청소)을 리뷰가 선호하면 하네스에서 드롭 호출만 빼면 되도록 설계한다.",
    "(6) **`DELETE FROM ReceiverConfig` 예외를 grant로 열 때 나머지 6테이블이 정말 막히는가.** 테이블 단위 grant의 실제 동작을 **step6이 실측**한다. **미판단 시 기본 결정: 테이블 단위 `DELETE` grant를 주고, 나머지 6테이블 삭제 시도가 거부됨을 테스트로 단언한다.** 거부가 확인되지 않으면 그 사실을 기록하고 정적 스캔에만 의존한다(그리고 그것이 약해진 방어선임을 명시한다).",
    "(7) **운영 `news` DB의 이름·문자셋을 지금 확정할 것인가.** step0 부트스트랩이 만드는 DB 이름을 `news`로 잡았으나 운영 정책(테넌시·백업 단위)이 다를 수 있다. **미판단 시 기본 결정: `news`로 만들고, 이름이 바뀌어도 환경변수 하나만 고치면 되도록 코드에 DB 이름을 박지 않는다.**",
    "(8) **이관 대상에 `sqlite_sequence` 등 내부 테이블이 필요한가.** 소스 실측에서 `sqlite_master`의 table 항목은 7개뿐이고 `sqlite_sequence`는 없다(AUTOINCREMENT 미사용). **미판단 시 기본 결정: 7테이블만 옮기고, 소스에 예상 밖 테이블이 있으면 마이그레이터가 **조용히 건너뛰지 말고 실패**한다.**",
    "(9) **`--db mysql`에서 Spring이 실수로 sqlite로 떠도 계약이 green일 수 있다.** 계약은 저장소를 보지 않는다. **미판단 시 기본 결정: step7 C의 「임시 `DATA_DIR`의 `news.db` 바이트 무변」 단언으로 막고, step7 M1 변이로 그 단언이 실제로 red를 내는지 실증한다.** 실증에 실패하면 그 사실을 forward_notes 1급 항목으로 올린다."
  ],

  "steps": [
    { "step": 0, "name": "baseline-and-credentials", "status": "pending" },
    { "step": 1, "name": "dialect-semantics-probe", "status": "pending" },
    { "step": 2, "name": "adr-and-migrator-baseline", "status": "pending" },
    { "step": 3, "name": "row-copy-verify", "status": "pending" },
    { "step": 4, "name": "reverse-export", "status": "pending" },
    { "step": 5, "name": "dialect-seam", "status": "pending" },
    { "step": 6, "name": "mysql-runtime", "status": "pending" },
    { "step": 7, "name": "harness-mysql-parity", "status": "pending" },
    { "step": 8, "name": "cutover-runbook", "status": "pending" },
    { "step": 9, "name": "closeout", "status": "pending" }
  ]
}
