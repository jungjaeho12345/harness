{
  "project": "기사 작성기",
  "phase": "3-mapping",
  "steps": [
    { "step": 0, "name": "mapping-writer-mode", "status": "completed", "summary": "useWriteController에 mode:'mapping' 추가 — 진입은 기존 openArticle(getArticle 재조회+lockArticle 'revise', PENDING_EDIT_KEY useEffect가 req.mode 그대로 전달해 무변경)을 재사용해 편집 잠금 획득·articleId 유지. 전이 없는 저장 콜백 saveMapping() 신설: saveArticle(toSaveDto, PUT/articleId 포함)→성공 시 unlockArticle+resetTabToBlank, applyAction 미호출. toSaveDto가 tab.fields.body를 그대로 markupVersion으로 실어 텍스트 블록 비파괴(appendEmbedToBody로 추가된 embed만 저장, mergeTextIntoBody 미사용). 백엔드/contract/httpModel 무변경. 신규 테스트 4개(blocksToText 불변 포함). backend 204 + web 259 통과." },
    { "step": 1, "name": "mapping-button", "status": "completed", "summary": "writerButtons.js 순수 로직만 수정: SUBMIT_LABELS에 save:'저장' 라벨 additive 추가. submitButtons()에 mode==='mapping' 분기를 함수 진입 직후 최우선 위치에 두어 status/role(DPS·RDS·DDH·R/D/Z 등)·articleId 무관하게 항상 ['save'] 반환 — 매핑은 임베드 추가 PUT이라 송고/보류/KILL·송고 가드 미적용. ORDER/order() 거름망 미경유(단일 키 직접 반환). 기존 진리표(신규·revise·portalRevise·edit RDS·edit DDH) 무회귀. writerButtons.test.js에 매핑 케이스 3개 추가(최우선성 검증 포함), SUBMIT_LABELS 단언을 키별 검사로 조정. backend 204 + web 262 통과." },
    { "step": 2, "name": "mapping-view-entry", "status": "pending" },
    { "step": 3, "name": "writer-page-mapping-ui", "status": "pending" },
    { "step": 4, "name": "context-menu-activation", "status": "pending" }
  ]
}
