{
  "project": "기사 작성기",
  "phase": "48-distribution-tick",
  "steps": [
    { "step": 0, "name": "tick-decision", "status": "completed", "summary": "시점 배부 판정 순수 모듈 완료. 신규 src/services/embargoTick.js — dueDistributionKinds(contents, distributedKinds, nowISO)와 isEmbargoComplete(contents, distributedKinds) 2개 export. 규칙: press required⇔embargoAt 설정, nonpress required⇔secondEmbargoAt 설정(2차만 기사의 press는 송고 즉시배부분이라 required 아님). due=required∧시각도래(Date.parse(when)<=Date.parse(now), NaN이면 false)∧미배부. complete=required 전부 배부이력에 있음(비-엠바고는 false — 오전이 방지). distributedKinds는 Set/배열 둘 다 허용. now 주입 필수(타이머 0, ADR-008). 신규 test/embargoTick.test.js 15건. 전체 542 pass/0 fail, lint clean." },
    { "step": 1, "name": "distribution-tick", "status": "completed", "summary": "시점 배부 오케스트레이션 완료. src/services/articleService.js에 async distributionTick({now}) 추가(반환객체 노출) + embargoTick import. 알고리즘: distributionService 미주입 시 no-op {ok:true,distributed:[],completed:[]}; articleModel.query({status:'EPS'}) 순회; 기사별 try/catch 격리; distributedKindsOf(id)=distribute 이력 action 집합; due=dueDistributionKinds(row,set,now)>0이면 distributionService.distribute(id,{kinds:due,actorUserId:null}) 위임; 배부 후 이력 재조회로 isEmbargoComplete면 getById로 여전히 EPS일 때만 present-only update({contents:{status:'DPS'}}) + record(status/embargoComplete, EPS→DPS) 후 completed push. 완결 판정은 이력 kind 기준(2차만 기사 송고 press 이력 포함). 멱등(재tick 시 due 0·이미 DPS는 제외)·DB 비파괴(present-only, 삭제 0)·시스템 actorUserId=null. 신규 test/distributionTick.test.js 11건(due 시각/완결/멱등/실패 격리/present-only/다건 독립/DB 비파괴). 전체 553 pass/0 fail, lint clean." },
    { "step": 2, "name": "tick-route-sse", "status": "completed", "summary": "transport/wiring + SSE 재발행 + 문서 완료. 변경: (a) distributionService.js — onDistributed(선택) 추가, distributed.length>0일 때 notifyDistributed({articleId,kinds}) 격리 호출. (b) controllers/index.js — onChange(선택) 옵션 추가, distributionService에 onDistributed:()=>onChange?.('distribute') 주입, article.distributionTick 노출. (c) server/index.js — POST /api/distribution/tick 라우트(게이트: DISTRIBUTION_TOKEN+x-distribution-token 일치 시 시스템 허용, 아니면 Z 세션 요구, 둘 다 아니면 401/403; role은 세션에서만·req.body.role 무시; await controllers.article.distributionTick() 후 app.notifyChange('distribute')). bootstrap은 let notifyChange 지연 바인딩으로 createControllers(onChange)→createApp 순서 문제 해소. (d) 문서: ARCHITECTURE([배부] SSE 재발행 줄+[시점배부] tick 블록), SCHEMA(EPS→DPS 완결 전이·embargoComplete 이력), README/.env.example(DISTRIBUTION_TOKEN). 테스트: server.test.js +4(tick 401/403/200/토큰게이트), controllers.test.js +3(distributionTick 노출·onChange 재발행·배부0건 미발행). web/** 무접촉. 전체 backend 560 pass/0 fail, web 1927 pass/0 fail, lint·build clean." }
  ]
}
