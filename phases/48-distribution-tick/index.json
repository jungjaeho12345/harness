{
  "project": "기사 작성기",
  "phase": "48-distribution-tick",
  "steps": [
    {
      "step": 0,
      "name": "embargo-schedule",
      "status": "completed",
      "summary": "src/services/embargoSchedule.js 순수 규칙 모듈(import 0건) — parseInstant/isDue/requiredKinds/dueKinds/missingKinds/isComplete. epoch(Date.parse) 기반 도래 판정, 오프셋 없는/파싱 불가 값은 fail-safe로 절대 도래 아님(사전식 함정 회귀 잠금), 존재 판정은 관대·시각 파싱은 엄격, 2차만 기사의 press는 requiredKinds 제외(중복 반출 금지), required=[]면 isComplete 항상 false. test/embargoSchedule.test.js 26건 green."
    },
    {
      "step": 1,
      "name": "tick-service",
      "status": "completed",
      "summary": "src/services/distributionTickService.js — createDistributionTickService.runTick({actorUserId}) 도메인 엔진(HTTP/세션/타이머 무지, 의존성 주입). EPS만 query→기사별 getById 재조회(TOCTOU 차단)→embargoSchedule.dueKinds 도래 판정→이력(eventType='distribute') 기반 멱등 배부(distributionService 위임)→배부 후 이력 재조회로 isComplete/missingKinds 판정→완결 시 전이 직전 EPS 재확인 후 present-only status:'DPS' update + eventType='status'/action='embargoComplete' append. 진입가드 spool-disabled·single-flight(tick-in-progress, finally 해제)·invalid-clock. 기사단위 try/catch 격리, 미완결 incomplete{missing} 표면화. lifecycle 전이표 무수정. test/distributionTickService.test.js 16건 green(도래분만·멱등·TOCTOU·이력기반완결·실패incomplete·single-flight·DB비파괴 등). npm test 569 pass(+16), lint 0, grep 타이머/egress/삭제 0건."
    },
    {
      "step": 2,
      "name": "tick-authz",
      "status": "completed",
      "summary": "인가 계층: authorization.js에 CAPABILITIES.runDistributionTick=['Z'](도메인 9→10) + 게이트 runDistributionTick(sessionId)→touchSession→unauthenticated/forbidden→{ok,role,userId}(actor는 세션 userId만, ADR-004). 컨트롤러 결선: distributionTickService는 distributionService 활성 분기에서만 생성, distribution.runTick(sessionId) 단일인자 진입점 노출(도메인 9→10). 순서=인가 게이트 먼저→통과 후 spool-disabled 검사(비-Z/미인증에 설정상태 누출 차단). 컨트롤러 HTTP/express/req.body 참조 0건. test: authorization.test.js +5(Z통과·비Z forbidden·미인증·assertAuthorized 회귀), controllers.test.js 도메인키 10 단언+runTick 4케이스(spool+Z ok checked:0 / 미설정 spool-disabled / 비Z forbidden 우선 / 미인증). npm test 577 pass(+8), lint 0. server/index.js·web·step0/1 산출물 diff 0."
    },
    {
      "step": 3,
      "name": "tick-route-docs",
      "status": "completed",
      "summary": "transport 결선+문서: server/index.js에 POST /api/distribution/tick 추가(얇은 transport ADR-006) — req.body 미참조, controllers.distribution.runTick(readSessionToken(req)) 단일 호출, 실패는 fail()로 매핑. STATUS_BY_REASON additive: spool-disabled:503, tick-in-progress:409(기존 값 불변, invalid-clock은 400 fallback). 로깅은 건수만(마스킹)+미완결 warn(articleId/missing kind만). SSE 재발행은 distributed>0||completed>0일 때만 notifyChange('distribute')(0건 미발행 ADR-005, 신호는 kind뿐 행데이터 없음) — phase47 '배부 후 목록 즉시 갱신 안 됨' 인수인계 종결. 문서 현행화: ARCHITECTURE.md(services에 embargoSchedule·distributionTick, [배부 tick] 데이터흐름 블록), SCHEMA.md(EPS→DPS 시스템전이·embargoComplete·distribute 이력 서술, 스키마 무변경), README.md(운영 절 배부 tick+엠바고 오프셋 ISO 주의). test/distribution-tick-api.test.js 신규 9건(미인증401·비Z403·Z200 shape·바디무시·권한상승차단·미설정503+누출차단·E2E스풀1건/DPS/이력/멱등·SSE 발행&0건미발행·GET404). npm test 586 pass(+9), test:web 1927 무회귀, lint 0/build clean, grep(tick 타이머·egress 0 / web diff 0 / req.body near tick 0)."
    }
  ]
}
