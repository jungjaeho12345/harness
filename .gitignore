node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# vite 빌드 산출물
dist/
web/dist/

# 환경변수 (시크릿) / 런타임 DB
.env
# DB 자격증명 (phase 75) — 값은 리포 밖(D:/agents/secrets/news-mysql.env)에 두는 것이 규약이고,
# 이 세 줄은 실수로 리포 안에 들어온 사본까지 막는 2차 방어선이다.
# ⚠ SecretHygieneTest 가 이 세 줄의 존재를 단언한다 — 스캐너가 건너뛰는 자리와 같은 목록이어야 한다.
*.env
secrets/
ops/mysql/*.local.sql
news.db
news.db-journal
news.db-wal

# 단일 인스턴스 잠금 파일 (phase 65) — dev 부팅은 dataDir가 cwd(리포 루트)라 여기 생긴다
instance-lock.db
instance-lock.db-journal

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# python 바이트코드 캐시 (하네스 실행기)
__pycache__/
*.pyc

# python
__pycache__/
*.pyc

# 업로드 파일 저장 디렉터리 (런타임 산출물)
uploads/

# IDE 설정 — 다른 도구 세션이 커밋에 섞은 전례가 있다(d1d5e84)
.vscode/
